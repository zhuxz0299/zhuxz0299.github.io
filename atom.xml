<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Zhuxz</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://zhuxz0299.github.io/</id>
  <link href="https://zhuxz0299.github.io/" rel="alternate"/>
  <link href="https://zhuxz0299.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, Zhuxz</rights>
  <title>Zhuxz's Blog</title>
  <updated>2026-09-05T04:29:10.326Z</updated>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Linux" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Linux/"/>
    <category term="linux" scheme="https://zhuxz0299.github.io/tags/linux/"/>
    <category term="gnome" scheme="https://zhuxz0299.github.io/tags/gnome/"/>
    <category term="desktop-entry" scheme="https://zhuxz0299.github.io/tags/desktop-entry/"/>
    <category term="icon" scheme="https://zhuxz0299.github.io/tags/icon/"/>
    <category term="wayland" scheme="https://zhuxz0299.github.io/tags/wayland/"/>
    <content>
      <![CDATA[<p>之前在 GNOME 中遇到过两次应用图标异常：一次是欧路词典 Eudic，另一次是 LocalSend。软件可以正常打开，图标文件也存在，但运行后 Dock 中显示的却是默认齿轮图标。尤其是 Eudic，即使把 <code>.desktop</code> 中的 <code>Icon=</code> 改成图标的绝对路径，再重启系统，问题依然存在。</p><p>要解释这种现象，需要先弄清楚两个过程：桌面环境如何找到应用的图标，以及如何知道一个窗口属于哪个应用。</p><div class="note info flat"><p>本文主要讨论应用列表、GNOME Dash / Dock 中的应用图标。桌面背景上的文件图标、文件管理器中的文件类型图标，以及系统托盘图标，还有各自的显示机制，不能完全套用本文的排查方法。</p></div><h2 id="应用图标的显示过程">应用图标的显示过程</h2><p>一个 Linux 图形应用通常涉及下面三个相互独立的对象：</p><table><thead><tr><th>对象</th><th>作用</th><th>例子</th></tr></thead><tbody><tr><td>可执行程序</td><td>运行应用、创建窗口</td><td><code>/usr/bin/localsend_app</code></td></tr><tr><td>Desktop Entry，即 <code>.desktop</code> 文件</td><td>描述如何启动应用，以及名称、图标等信息</td><td><code>/usr/share/applications/localsend.desktop</code></td></tr><tr><td>图标文件</td><td>提供最终显示的图片</td><td>图标主题目录中的 PNG、SVG 文件</td></tr></tbody></table><p>可执行文件能运行，只能说明程序本身可以启动；它并不保证桌面环境能找到正确的启动项和图标。</p><p>对于应用列表中的启动器，显示过程可以简化为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">读取 .desktop 文件 → 读取 Icon 字段 → 查找图标文件 → 显示图标</span><br></pre></td></tr></table></figure><p>对于 Dock 中运行着的应用，还要多做一次窗口归属判断：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">程序创建窗口</span><br><span class="line">    ↓</span><br><span class="line">桌面环境读取窗口的身份信息</span><br><span class="line">    ↓</span><br><span class="line">匹配到对应的 .desktop 文件</span><br><span class="line">    ↓</span><br><span class="line">读取 Icon 字段 → 查找图标文件 → 显示图标</span><br></pre></td></tr></table></figure><p>因此，启动器图标正常，但打开后变成齿轮，或者 Dock 中又多出一个图标时，应当优先检查窗口匹配。此时即使修改了正确启动项中的 <code>Icon=</code>，运行中的窗口也可能根本没有关联到这个启动项。</p><h2 id="Desktop-Entry：应用在桌面环境中的入口">Desktop Entry：应用在桌面环境中的入口</h2><h3 id="desktop-文件的内容"><code>.desktop</code> 文件的内容</h3><p>以 Eudic 为例，一个启动项可以写成：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Desktop Entry]</span></span><br><span class="line"><span class="attr">Type</span>=Application</span><br><span class="line"><span class="attr">Name</span>=Eudic</span><br><span class="line"><span class="attr">Exec</span>=/usr/share/eusoft-eudic/AppRun %F</span><br><span class="line"><span class="attr">Icon</span>=com.eusoft.eudic</span><br><span class="line"><span class="attr">StartupWMClass</span>=eudic</span><br><span class="line"><span class="attr">Terminal</span>=<span class="literal">false</span></span><br><span class="line"><span class="attr">Categories</span>=Dictionary<span class="comment">;Education;</span></span><br></pre></td></tr></table></figure><p>其中与本文直接相关的字段是：</p><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>Name</code></td><td>显示给用户看的应用名称</td></tr><tr><td><code>Exec</code></td><td>启动命令；这里的 <code>%F</code> 表示传入多个本地文件</td></tr><tr><td><code>Icon</code></td><td>图标名称或者绝对路径</td></tr><tr><td><code>StartupWMClass</code></td><td>为桌面环境提供窗口匹配提示</td></tr></tbody></table><h3 id="启动项所处位置">启动项所处位置</h3><p>在默认 XDG 目录配置下（见<a href="https://zhuxz0299.github.io/posts/fc95dfca.html">该文章</a>的 XDG Base Directory 章节），常见的查找位置按优先级排列为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">~/.local/share/applications/    当前用户</span><br><span class="line">/usr/local/share/applications/ 本机管理员安装</span><br><span class="line">/usr/share/applications/       系统软件包安装</span><br></pre></td></tr></table></figure><p>桌面环境用 <strong>desktop file ID</strong> 区分启动项。对于直接放在 <code>applications</code> 下的文件，这个 ID 就是文件名，例如 <code>localsend.desktop</code>。位于子目录时，还需要把相对路径中的 <code>/</code> 换成 <code>-</code>，详见 <a href="https://specifications.freedesktop.org/desktop-entry/latest-single/#desktop-file-id">Desktop File ID</a>。</p><p>因此，把系统文件复制到用户目录并保留同名，能够覆盖系统启动项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/.local/share/applications</span><br><span class="line"><span class="built_in">cp</span> -i /usr/share/applications/localsend.desktop ~/.local/share/applications/localsend.desktop</span><br></pre></td></tr></table></figure><div class="note warning flat"><p>这里的覆盖是选择整份用户文件，不是把两份文件的字段合并。如果改成另一个文件名，就可能变成另一个启动项，导致应用列表中出现重复入口。</p></div><h2 id="Icon：如何找到图片">Icon：如何找到图片</h2><h3 id="绝对路径与图标名称">绝对路径与图标名称</h3><p><code>Icon</code> 有两种常见写法：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">Icon</span>=/usr/share/pixmaps/com.eusoft.eudic.png</span><br><span class="line"><span class="comment">; 或者</span></span><br><span class="line"><span class="attr">Icon</span>=com.eusoft.eudic</span><br></pre></td></tr></table></figure><p>前者直接指定文件，后者交给图标主题机制查找。使用图标名时通常省略 <code>.png</code>、<code>.svg</code> 等扩展名；使用绝对路径时需要写出完整文件名。</p><div class="note warning flat"><p>因为<code>.desktop</code> 不是 Shell 脚本，不能指望 <code>Icon=~/Pictures/icon.png</code> 或 <code>Icon=$HOME/Pictures/icon.png</code> 自动展开。</p></div><h3 id="图标主题与-hicolor">图标主题与 hicolor</h3><p>图标主题把图片按尺寸、用途等组织起来，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">/usr/share/icons/hicolor/</span><br><span class="line">├── index.theme</span><br><span class="line">├── 48x48/apps/com.eusoft.eudic.png</span><br><span class="line">├── 256x256/apps/com.eusoft.eudic.png</span><br><span class="line">└── scalable/apps/com.eusoft.eudic.svg</span><br></pre></td></tr></table></figure><p>上面是目录结构示意，不要求一个应用同时提供这些文件。<code>apps</code> 表示应用图标，<code>256x256</code> 表示相应尺寸，<code>scalable</code> 通常用于 SVG 等可缩放图片。</p><p>按图标名查找时，桌面环境先查询当前主题，再查询继承的主题，并以 <code>hicolor</code> 作为必需的兜底主题；主题中仍找不到时，还会尝试非主题图标位置，其中包括 <code>/usr/share/pixmaps</code>。</p><p>如果要给当前用户补充图标，可以放在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">~/.local/share/icons/hicolor/256x256/apps/com.eusoft.eudic.png</span><br></pre></td></tr></table></figure><p>尺寸目录应当与实际图片尺寸相符。把图片复制到 <code>256x256</code> 目录，并不会自动把它缩放到 256 × 256。</p><h2 id="窗口如何对应到启动项">窗口如何对应到启动项</h2><h3 id="X11-与-XWayland：WM-CLASS">X11 与 XWayland：WM_CLASS</h3><p>X11 窗口通过 <code>WM_CLASS</code> 提供身份信息，其中包含两个字符串，分别是 instance 和 class，用于标识应用实例和应用类别。</p><p>在终端运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">xprop WM_CLASS</span><br></pre></td></tr></table></figure><p>然后点击目标窗口，就能读取这个属性。典型输出形如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">WM_CLASS(STRING) = &quot;example&quot;, &quot;Example&quot;</span><br></pre></td></tr></table></figure><p>GNOME 可以用这些值查找启动项文件名，也可以匹配 <code>.desktop</code> 中的 <code>StartupWMClass</code>。例如 Eudic 的那次问题中，实际确认有效的配置是：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">StartupWMClass</span>=eudic</span><br></pre></td></tr></table></figure><p>它的意思是为具有这个身份的窗口提供启动项匹配线索，并不会修改窗口自身的 <code>WM_CLASS</code>。填写时应保留实际值的大小写，不要根据应用的显示名称猜测。</p><p>还需要区分<strong>桌面会话使用的协议</strong>与<strong>某个应用使用的协议</strong>。即使登录的是 GNOME Wayland，会话中的旧应用仍可以通过 XWayland 运行。这些应用依然使用 X11 的窗口属性，所以 <code>xprop</code> 仍然有效；Eudic 当时就是这种情况。</p><h3 id="原生-Wayland：app-id">原生 Wayland：app_id</h3><p>原生 Wayland 窗口不能用 <code>xprop</code> 查询。对于常见的 <code>xdg-shell</code> 窗口，应用通过 <code>xdg_toplevel.set_app_id</code> 设置身份。协议建议这个 <code>app_id</code> 与 <code>.desktop</code> 文件去掉扩展名后的名称一致，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">窗口 app_id：org.example.Viewer</span><br><span class="line">启动项文件：org.example.Viewer.desktop</span><br></pre></td></tr></table></figure><p>这样合成器就可以把窗口与桌面入口对应起来。协议说明见 <a href="https://wayland.app/protocols/xdg-shell#xdg_toplevel:request:set_app_id">xdg_toplevel.set_app_id</a>。</p><p>在 GNOME 中可以按 <code>Alt+F2</code>，输入 <code>lg</code>，打开内置调试工具 Looking Glass，可以在 Windows 页检查目标窗口及其关联的应用。</p><div class="note info flat"><p>如果不方便修改 <code>.desktop</code> 文件名称，在 GNOME 中也可以使用 <code>StartupWMClass=&lt;app_id&gt;</code> 进行设置。这个与 GNOME 使用的 Mutter 的实现方式有关，并非 Wayland 桌面通用的解决方案。</p></div><h3 id="为什么名称不一致也可能正常">为什么名称不一致也可能正常</h3><p>桌面环境通常并非只检查一个字段。它还会利用应用 ID、已知进程、启动通知、相关窗口等信息判断归属；如果始终找不到对应的桌面入口，则会为窗口建立临时的应用对象。这可以解释为什么同一软件从不同入口启动，或者升级后，图标会不同。</p><p>因此，<code>.desktop</code> 没有 <code>StartupWMClass</code> 本身不代表有问题。名称和身份原本能正确对应的应用，通常不需要额外补这个字段。</p><h2 id="两次问题的修复">两次问题的修复</h2><h3 id="Eudic：改了-Icon-仍然显示齿轮">Eudic：改了 Icon 仍然显示齿轮</h3><p>当时系统中已经有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">/usr/share/applications/eusoft-eudic.desktop</span><br><span class="line">/usr/share/pixmaps/com.eusoft.eudic.png</span><br></pre></td></tr></table></figure><p>把 <code>Icon=</code> 改成图片的绝对路径，并重启系统，运行后的图标仍然异常。后来确认 Eudic 通过 XWayland 运行，窗口匹配需要使用 <code>eudic</code>。</p><p>最后在用户启动项的 <code>[Desktop Entry]</code> 分组中添加 <code>StartupWMClass=eudic</code> 解决了问题。</p><h3 id="LocalSend：启动项名称与应用-ID-不一致">LocalSend：启动项名称与应用 ID 不一致</h3><p>在 GNOME Wayland + Dash to Dock 环境中，通过 pacman 更新 archlinuxcn 软件包 <code>localsend 1.18.2-1</code> 打开后显示通用齿轮图标。排查记录中确认的情况是：</p><table><thead><tr><th>项目</th><th>实际值</th></tr></thead><tbody><tr><td>系统启动项</td><td><code>/usr/share/applications/localsend.desktop</code></td></tr><tr><td>程序内置应用 ID</td><td><code>org.localsend.localsend_app</code></td></tr><tr><td>图标文件</td><td>存在且正常</td></tr><tr><td>原启动项的 <code>StartupWMClass</code></td><td>未设置</td></tr></tbody></table><p>解决方式：保留启动项文件名，创建用户启动项覆盖，然后在 <code>[Desktop Entry]</code> 分组中添加：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">StartupWMClass</span>=org.localsend.localsend_app</span><br></pre></td></tr></table></figure><p>这里保留原文件名，是为了继续覆盖 <code>localsend.desktop</code>，避免额外创建第二个入口。对于应用开发者和打包者，更一致的做法是让应用报告的身份与安装的桌面入口名称相互对应。</p><h2 id="排查顺序">排查顺序</h2><table><thead><tr><th>现象</th><th>优先检查</th></tr></thead><tbody><tr><td>应用列表中没有入口</td><td>启动项路径、格式，以及 <code>Hidden</code>、<code>NoDisplay</code> 等字段</td></tr><tr><td>应用列表中的图标就不正确</td><td>实际生效的启动项、<code>Icon</code> 和图片查找</td></tr><tr><td>启动器正常，运行后变成齿轮或多一个图标</td><td>窗口身份与启动项的匹配</td></tr><tr><td>应用识别正常，但换图后仍显示旧图片</td><td>图标主题缓存及桌面环境的内存状态</td></tr></tbody></table><p>这个表是排查入口，不是仅凭外观就能得出的诊断。例如，应用列表与 Dock 都显示齿轮，可能同时涉及图片缺失和匹配失败。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/8b8a9be8.html</id>
    <link href="https://zhuxz0299.github.io/posts/8b8a9be8.html"/>
    <published>2026-09-05T02:59:35.000Z</published>
    <summary>从 desktop 启动项、图标主题与窗口身份匹配解释 Linux 应用图标的显示过程，并记录 GNOME 下 Eudic 和 LocalSend 图标异常的排查方法。</summary>
    <title>Linux 桌面图标显示</title>
    <updated>2026-09-05T04:29:10.326Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="Dev Tools" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/"/>
    <category term="Software" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/Software/"/>
    <category term="verilator" scheme="https://zhuxz0299.github.io/tags/verilator/"/>
    <category term="verilog" scheme="https://zhuxz0299.github.io/tags/verilog/"/>
    <category term="systemverilog" scheme="https://zhuxz0299.github.io/tags/systemverilog/"/>
    <category term="hdl" scheme="https://zhuxz0299.github.io/tags/hdl/"/>
    <content>
      <![CDATA[<p>Verilator 可以将 Verilog/SystemVerilog 设计编译成 C++ 或 SystemC 模型，这个模型再与 testbench 一起由普通 C++ 编译器构建成原生可执行文件，运行该文件即为进行仿真。</p><p>本文以一个计数器为例，完整走一遍“检查 RTL → 生成 C++ 模型 → 编写 C++ testbench → 构建并运行 → 查看波形”的流程。</p><div class="note info flat"><p>本文主要参考 <a href="https://www.itsembedded.com/dhd/verilator_1/">It’s Embedded! 的 Verilator 系列教程</a>以及 <a href="https://verilator.org/guide/latest/">Verilator 官方文档</a>。前者写于 2021 年，文中的总体思路仍然适用，但部分命令和功能边界已经发生变化，本文以当前 Verilator 5 的官方文档为准。</p></div><h2 id="Verilator-是什么">Verilator 是什么</h2><p><a href="https://verilator.org/guide/latest/overview.html">官方文档</a>将 Verilator 定义为编译器，而不是传统意义上的仿真器。它的典型工作流程是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">Verilog/SystemVerilog RTL</span><br><span class="line">          │</span><br><span class="line">          │ Verilator：解析、lint、优化、生成代码</span><br><span class="line">          ▼</span><br><span class="line">    C++/SystemC 模型 + 构建文件</span><br><span class="line">          │</span><br><span class="line">          │ GCC/Clang + C++ testbench + Verilator runtime</span><br><span class="line">          ▼</span><br><span class="line">       原生仿真可执行文件</span><br><span class="line">          │</span><br><span class="line">          ├── 终端中的检查结果</span><br><span class="line">          ├── VCD/FST 波形</span><br><span class="line">          └── 覆盖率等数据</span><br></pre></td></tr></table></figure><p>与传统 HDL 仿真器相比，Verilator 的特点为：</p><ul><li>生成的模型经过优化并编译为本机代码，适合大型同步数字电路、高速回归测试和软硬件协同开发；</li><li>testbench 可以直接使用 C++ 的类型、容器、文件 IO 和第三方库，也可以通过 DPI、VPI 等接口连接 HDL 与软件；</li><li><code>--lint-only</code> 可以只做静态检查，即使不使用 Verilator 仿真，也很适合放进编辑器或 CI；</li><li>Verilator 是自由开源软件，不依赖商业仿真器许可证。</li></ul><h3 id="功能边界">功能边界</h3><p>Verilator 主要面向 RTL 功能验证。它不是综合工具，也不能代替静态时序分析或布局布线后的精确时序仿真。</p><div class="note warning flat"><p>网上常把 Verilator 简化为“不支持延时的周期级仿真器”。这对早期版本大体成立，但对现代 Verilator 5 已不准确：使用 <code>--timing</code> 时，它能够处理 <code>#</code> 延时、事件控制、<code>wait</code> 和 <code>fork</code> 等结构。不过，默认模式仍然<a href="https://verilator.org/guide/latest/languages.html#unknown-states">主要采用二态模型</a>，不能完全复现传统四态事件驱动仿真器中 <code>X</code>、<code>Z</code> 的传播行为，并且部分 SystemVerilog 验证特性仍只得到有限支持。Verilator 5.050 开始提供实验性的 <code>--fourstate</code>，但官方将其标为仅供开发使用，暂时不应视为成熟的四态仿真替代方案。</p></div><h2 id="一个完整的-C-testbench-示例">一个完整的 C++ testbench 示例</h2><p>示例目录中只有两个手写文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">verilator-counter/</span><br><span class="line">├── counter.sv       # DUT</span><br><span class="line">└── sim_main.cpp     # C++ testbench</span><br></pre></td></tr></table></figure><h3 id="编写-DUT">编写 DUT</h3><p><code>counter.sv</code> 是一个带低有效异步复位和使能信号的 8 位计数器：</p><figure class="highlight verilog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">module</span> counter #(</span><br><span class="line">    <span class="keyword">parameter</span> <span class="keyword">int</span> WIDTH = <span class="number">8</span></span><br><span class="line">) (</span><br><span class="line">    <span class="keyword">input</span>  <span class="keyword">logic</span>             clk,</span><br><span class="line">    <span class="keyword">input</span>  <span class="keyword">logic</span>             rst_n,</span><br><span class="line">    <span class="keyword">input</span>  <span class="keyword">logic</span>             en,</span><br><span class="line">    <span class="keyword">output</span> <span class="keyword">logic</span> [WIDTH-<span class="number">1</span>:<span class="number">0</span>] count</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">timeunit</span> <span class="number">1</span>ns;</span><br><span class="line">    <span class="keyword">timeprecision</span> <span class="number">1</span>ns;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">always_ff</span> @(<span class="keyword">posedge</span> clk <span class="keyword">or</span> <span class="keyword">negedge</span> rst_n) <span class="keyword">begin</span></span><br><span class="line">        <span class="keyword">if</span> (!rst_n)</span><br><span class="line">            count &lt;= &#x27;<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (en)</span><br><span class="line">            count &lt;= count + <span class="number">1&#x27;b1</span>;</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">endmodule</span></span><br></pre></td></tr></table></figure><p>先单独进行 lint：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">verilator --lint-only -Wall counter.sv</span><br></pre></td></tr></table></figure><p><code>--lint-only</code> 表示只检查、不生成模型；<code>-Wall</code> 会开启一组更严格的 Verilator 警告。若命令没有输出且返回值为 0，说明没有发现问题。</p><h3 id="将-SystemVerilog-转换为-C">将 SystemVerilog 转换为 C++</h3><p>最基本的转换命令是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">verilator --cc -Wall --top-module counter counter.sv</span><br></pre></td></tr></table></figure><p>其中 <code>--cc</code> 选择 C++ 输出模式；<code>--top-module counter</code> 指定顶层模块，只有一个明确的顶层模块时可以省略，但在实际项目中显式指定通常更稳妥。命令默认把结果写入 <code>obj_dir/</code>。文件名的默认前缀为 <code>V</code> 加顶层模块名，因此这里是 <code>Vcounter</code>。</p><h4 id="需要关注的文件">需要关注的文件</h4><table><thead><tr><th>文件</th><th>何时使用</th></tr></thead><tbody><tr><td><code>Vcounter.h</code></td><td>testbench 使用的主要头文件，包含顶层端口和模型 API</td></tr><tr><td><code>Vcounter.mk</code></td><td>手动调用 GNU Make 时的构建入口</td></tr><tr><td><code>Vcounter</code></td><td>使用 <code>--exe</code> 构建出的仿真可执行文件</td></tr><tr><td><code>Vcounter___024unit.h</code></td><td><code>$unit</code> 作用域中已公开的类型或函数（可选）</td></tr><tr><td><code>Vcounter__Dpi.h</code></td><td>C++ 与 SystemVerilog 之间的 DPI 声明（可选）</td></tr></tbody></table><p>对本文的计数器而言，C++ testbench 实际只需包含 <code>Vcounter.h</code>。</p><div class="note default flat"><p>名称中的 <code>024</code> 是字符 <code>$</code> 的编码，所以 <code>___024unit</code> 表示 <code>$unit</code>。</p></div><h4 id="Vcounter-h-文件">Vcounter.h 文件</h4><p>在本例生成的 <code>Vcounter.h</code> 中，可以看到类似的声明：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Vcounter</span> : <span class="keyword">public</span> VerilatedModel &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">VL_IN8</span>(&amp;clk, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">VL_IN8</span>(&amp;rst_n, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">VL_IN8</span>(&amp;en, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">VL_OUT8</span>(&amp;count, <span class="number">7</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">eval</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">final</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">trace</span><span class="params">(VerilatedTraceBaseC* tfp, <span class="type">int</span> levels, <span class="type">int</span> options = <span class="number">0</span>)</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><code>VL_IN8</code>、<code>VL_OUT8</code> 是 Verilator 用来声明端口的宏。对 testbench 而言，需要关注上面代码提供的接口：</p><ul><li>顶层端口成为了 C++ 对象的公开成员<ul><li>DUT 是对象时可以写入 <code>dut.clk</code>、<code>dut.en</code>，并读取 <code>dut.count</code></li></ul></li><li><code>eval(), final(), trace()</code> 为最常用的类方法</li></ul><p>上面是为了便于阅读而保留的精简声明。实际生成的 <code>Vcounter</code> 还提供以下公开接口：</p><ul><li><code>Vcounter(VerilatedContext*, const char* name = &quot;TOP&quot;)</code> 将模型绑定到指定 context，<code>Vcounter(const char* name = &quot;TOP&quot;)</code> 使用默认 context，<code>~Vcounter()</code> 销毁模型；</li><li><code>eval_step()</code> 和 <code>eval_end_step()</code> 用于拆分一次求值，主要用于同一时间步联合求值多个模型；单模型通常直接调用 <code>eval()</code>；</li><li><code>eventsPending()</code> 和 <code>nextTimeSlot()</code> 用于查询延时产生的待处理事件，主要与 <code>--timing</code> 一起使用；</li><li><code>contextp()</code>、<code>name()</code>、<code>hierName()</code>、<code>modelName()</code> 和 <code>threads()</code> 用于获取 context 或模型元信息。</li></ul><h4 id="其他生成文件">其他生成文件</h4><p><code>Vcounter.cpp</code>、<code>Vcounter___024root.*</code>、各层模块对应的 <code>Vcounter_&lt;模块名&gt;.*</code>、<code>Vcounter__Syms.*</code> 和启用波形后出现的 <code>Vcounter__Trace*.*</code>，都是模型的内部实现。构建系统会自动编译它们，testbench 通常不应直接包含或访问。文件名带有 <code>__Slow</code> 表示其中存放初始化、收尾等不频繁执行的 cold routines。</p><p><code>Vcounter_classes.mk</code>、<code>Vcounter__ALL.*</code>、<code>*.o</code>、<code>*.d</code>、<code>Vcounter__ver.d</code> 和 <code>Vcounter__verFiles.dat</code> 则属于构建中间产物、依赖关系或 Verilation 记录，一般只在排查构建问题时查看。</p><h3 id="编写-C-testbench">编写 C++ testbench</h3><p>创建 <code>sim_main.cpp</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdint&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;Vcounter.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;verilated.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;verilated_fst_c.h&quot;</span></span></span><br><span class="line"></span><br><span class="line">VerilatedContext context;</span><br><span class="line">VerilatedFstC trace;</span><br><span class="line">Vcounter* dut = <span class="literal">nullptr</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">eval_and_dump</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    dut-&gt;<span class="built_in">eval</span>();</span><br><span class="line">    trace.<span class="built_in">dump</span>(context.<span class="built_in">time</span>());</span><br><span class="line">    context.<span class="built_in">timeInc</span>(<span class="number">5</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">tick</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    dut-&gt;clk = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">eval_and_dump</span>();</span><br><span class="line"></span><br><span class="line">    dut-&gt;clk = <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">eval_and_dump</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    context.<span class="built_in">commandArgs</span>(argc, argv);</span><br><span class="line">    context.<span class="built_in">traceEverOn</span>(<span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">    dut = <span class="keyword">new</span> Vcounter&#123;&amp;context&#125;;</span><br><span class="line">    dut-&gt;<span class="built_in">trace</span>(&amp;trace, <span class="number">0</span>);</span><br><span class="line">    trace.<span class="built_in">open</span>(<span class="string">&quot;waveform.fst&quot;</span>);</span><br><span class="line"></span><br><span class="line">    dut-&gt;rst_n = <span class="number">0</span>;</span><br><span class="line">    dut-&gt;en = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">tick</span>();</span><br><span class="line">    <span class="built_in">tick</span>();</span><br><span class="line"></span><br><span class="line">    dut-&gt;rst_n = <span class="number">1</span>;</span><br><span class="line">    dut-&gt;en = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> exit_code = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (std::<span class="type">uint32_t</span> expected = <span class="number">1</span>; expected &lt;= <span class="number">10</span>; ++expected) &#123;</span><br><span class="line">        <span class="built_in">tick</span>();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (dut-&gt;count != expected) &#123;</span><br><span class="line">            std::cerr &lt;&lt; <span class="string">&quot;FAIL: expected &quot;</span> &lt;&lt; expected</span><br><span class="line">                      &lt;&lt; <span class="string">&quot;, got &quot;</span> &lt;&lt; +dut-&gt;count &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">            exit_code = <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (exit_code == <span class="number">0</span>)</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;PASS: count = &quot;</span> &lt;&lt; +dut-&gt;count &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line"></span><br><span class="line">    dut-&gt;<span class="built_in">final</span>();</span><br><span class="line">    trace.<span class="built_in">close</span>();</span><br><span class="line">    <span class="keyword">delete</span> dut;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> exit_code;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>头文件说明：</p><ul><li><code>verilated.h</code>：Verilator runtime 的核心 API，包括 <code>VerilatedContext</code>；</li><li><code>verilated_fst_c.h</code>：FST 波形写入接口，只在需要 FST 波形时引入。</li></ul><p>代码说明：</p><ul><li>DUT 使用指针并在 <code>main()</code> 中创建，是为了先执行 <code>context.commandArgs()</code>，再构造 Verilated 模型</li><li><code>Vcounter dut&#123;&amp;context&#125;</code> 表示实例化 DUT 时将其显式绑定到仿真上下文<ul><li>实例化 DUT 时也可以直接写 <code>Vcounter dut</code>，此时会使用当前线程的默认 context，在示例这种简单的环境下也可以正常工作</li></ul></li><li><code>context.commandArgs(argc, argv)</code> 表示将命令行参数交给 runtime，即让编译得到的二进制文件 <code>obj_dir/Vcounter</code> 支持接受参数</li><li><code>context.traceEverOn(true)</code> 也可以写成 <code>Verilated::traceEverOn(true)</code>，同样表示对当前线程的默认 context 操作</li><li><code>dut-&gt;trace(&amp;trace, 0)</code> 中的参数 <code>0</code> 在新版本的 Verilator 中没啥用，随便填什么都一样</li><li><code>dut-&gt;eval()</code> 根据当前输入重新计算模型，调用该方法模型才会识别边沿并执行相应的时序逻辑</li><li><code>context.timeInc(5)</code> 将仿真时间推进 5 个 time precision 单位，推进时间本身不会求值</li><li><code>trace.dump(context.time())</code> 把最近一次求值后的信号状态写入当前时间点</li><li><code>dut-&gt;final()</code> 执行 HDL 中的 <code>final</code> 块并完成断言等收尾工作<ul><li>例如 RTL 或 SystemVerilog testbench 中存在以下代码：<figure class="highlight verilog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">begin</span></span><br><span class="line">    <span class="built_in">$display</span>(<span class="string">&quot;simulation finished&quot;</span>);</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>手写 C++ 主循环时，调用 <code>dut-&gt;final()</code> 才会执行这个 <code>final</code> 块</li></ul></li></ul><h3 id="转换与构建">转换与构建</h3><p>把 DUT、testbench 和波形支持一起构建：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">verilator --cc --exe --build -Wall --trace-fst \</span><br><span class="line">  --top-module counter counter.sv sim_main.cpp</span><br></pre></td></tr></table></figure><p>可以生成可直接运行的仿真器：<code>./obj_dir/Vcounter</code>。如果不使用 <code>--build</code>，也可以手动执行生成的构建规则：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">verilator --cc --exe -Wall --trace-fst \</span><br><span class="line">  --top-module counter counter.sv sim_main.cpp</span><br><span class="line">make -C obj_dir -f Vcounter.mk Vcounter</span><br></pre></td></tr></table></figure><h3 id="波形生成说明">波形生成说明</h3><p>Verilator 生成波形需要同时完成两件事：</p><ol><li>Verilation 时用 <code>--trace-fst</code> 或 <code>--trace-vcd</code> 将波形支持编译进模型；</li><li>testbench 中创建相应的 trace 对象，打开文件，并在 <code>eval()</code> 后调用 <code>dump()</code>。</li></ol><p>生成得到的 <code>.fst</code> 以及 <code>.vcd</code> 文件都可以用 gtkwave 打开。FST 和 VCD 两种格式比较：</p><table><thead><tr><th>格式</th><th>Verilator 参数</th><th>C++ 类型</th><th>特点</th></tr></thead><tbody><tr><td>FST</td><td><code>--trace-fst</code></td><td><code>VerilatedFstC</code></td><td>二进制、文件通常更小，适合日常使用</td></tr><tr><td>VCD</td><td><code>--trace-vcd</code></td><td><code>VerilatedVcdC</code></td><td>文本格式、兼容工具更多，但文件通常更大</td></tr></tbody></table><p>对于大型设计，波形 IO 可能显著降低仿真速度并占用大量磁盘。可以使用 <code>--trace-depth 1</code> 限制层数，延后 <code>trace.open()</code> 的时间，或仅在失败用例中开启波形。官方 FAQ 的<a href="https://verilator.org/guide/latest/faq.html#how-do-i-generate-waveforms-traces-in-c">波形章节</a>列出了更多优化方法。</p><h2 id="常用参数">常用参数</h2><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>--lint-only</code></td><td>只做 lint，不生成模型</td></tr><tr><td><code>-Wall</code></td><td>启用更严格的 Verilator 警告</td></tr><tr><td><code>--cc</code> / <code>--sc</code></td><td>生成 C++ 模型 / SystemC 模型</td></tr><tr><td><code>--exe</code></td><td>生成可执行文件目标；还需在命令行中传入实现主循环的 C++ 文件</td></tr><tr><td><code>--build</code></td><td>生成代码后自动调用 GNU Make 构建</td></tr><tr><td><code>--top-module &lt;name&gt;</code></td><td>指定顶层模块，并影响默认输出前缀</td></tr><tr><td><code>--Mdir &lt;dir&gt;</code></td><td>修改输出目录，默认为 <code>obj_dir</code></td></tr><tr><td><code>--trace-fst</code> / <code>--trace-vcd</code></td><td>将对应的波形支持编译进模型</td></tr><tr><td><code>--trace-depth &lt;n&gt;</code></td><td>限制波形记录的模块层数</td></tr><tr><td><code>-CFLAGS &quot;&lt;flags&gt;&quot;</code></td><td>将参数传给 C++ 编译器</td></tr><tr><td><code>-LDFLAGS &quot;&lt;flags&gt;&quot;</code></td><td>将参数传给 C++ 链接阶段</td></tr><tr><td><code>-j 0</code></td><td>构建时使用可用的 CPU 线程；也可以给出明确的线程数</td></tr><tr><td><code>-I&lt;dir&gt;</code></td><td>添加 HDL <code>include</code> 和模块搜索目录</td></tr><tr><td><code>-D&lt;name&gt;=&lt;value&gt;</code></td><td>定义 HDL 预处理宏</td></tr><tr><td><code>-G&lt;name&gt;=&lt;value&gt;</code></td><td>覆盖顶层模块参数，例如 <code>-GWIDTH=16</code></td></tr><tr><td><code>-f &lt;file&gt;</code></td><td>从文件中读取源文件列表和参数</td></tr><tr><td><code>--timing</code></td><td>启用 HDL 延时、事件控制、<code>wait</code>、<code>fork</code> 等时序语义</td></tr><tr><td><code>--binary</code></td><td>自动生成 <code>main()</code> 并构建；等价于组合使用 <code>--main --exe --build --timing</code></td></tr></tbody></table><p>需要特别区分 HDL 和 C++ 两个处理阶段。直接写在 Verilator 命令行中的 <code>-I</code> 和 <code>-D</code> 用于 Verilog/SystemVerilog 源码；通过 <code>-CFLAGS</code> 传入的 <code>-I</code>、<code>-D</code> 则用于生成的 C++ 模型和 testbench。<code>-LDFLAGS</code> 用于指定 C++ 库目录、待链接的库及其他链接选项。</p><p>完整选项及其精确语义应以<a href="https://verilator.org/guide/latest/exe_verilator.html">官方参数手册</a>为准。</p><h3 id="binary-与-C-testbench-的选择"><code>--binary</code> 与 C++ testbench 的选择</h3><p><code>--binary</code> 在 testbench 本身使用 SystemVerilog 编写，并且不需要 C++ 主动驱动端口时使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">verilator --binary -Wall --trace-fst \</span><br><span class="line">  --top-module tb_counter counter.sv tb_counter.sv</span><br><span class="line">./obj_dir/Vtb_counter</span><br></pre></td></tr></table></figure><p><code>--binary</code> 会生成基本的 <code>main()</code> 和支持 timing 的事件循环。HDL testbench 可以用 <code>initial</code>、<code>#5</code>、<code>@(posedge clk)</code>、<code>$dumpfile</code>、<code>$dumpvars</code> 和 <code>$finish</code> 控制仿真。</p><p>若需要复用 C++ 库、连接软件模型、自定义激励与 scoreboard，或精确控制多个 DUT，则使用本文示例的 <code>--cc --exe --build</code> 加自定义 <code>sim_main.cpp</code>。</p><h2 id="从示例扩展到验证工程">从示例扩展到验证工程</h2><p>本文的 testbench 直接按周期驱动端口并比较结果，适合入门和较小的模块。设计变复杂后，如果继续把所有激励与检查堆在一个 <code>main()</code> 循环里，代码会很快变得难以复用。</p><p><a href="https://www.itsembedded.com/dhd/verilator_3/">It’s Embedded! 的 Part 3</a>将这种写法称为传统的、基于时间的验证；<a href="https://www.itsembedded.com/dhd/verilator_4/">Part 4</a>则进一步展示了事务级 C++ testbench。文章给出的拆分方式为：</p><ul><li>a block that generates stimulus data for the DUT (transaction generator / sequence)</li><li>a block that drives the aforementioned data onto the DUT (driver)</li><li>a block that observes the DUT’s outputs and generates result data packets (monitor)</li><li>a block that collects various data packets, then compares them for correctness (scoreboard)</li><li>a block that collects various data packets, and calculates functional coverage (coverage)</li></ul>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/69ebbf1e.html</id>
    <link href="https://zhuxz0299.github.io/posts/69ebbf1e.html"/>
    <published>2026-08-18T07:13:00.000Z</published>
    <summary>从 HDL 到 C++ 模型，介绍 Verilator 的基本工作流、常用参数、C++ testbench 与波形生成</summary>
    <title>Verilator 使用入门</title>
    <updated>2026-09-04T12:58:26.803Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Linux" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Linux/"/>
    <category term="linux" scheme="https://zhuxz0299.github.io/tags/linux/"/>
    <category term="wayland" scheme="https://zhuxz0299.github.io/tags/wayland/"/>
    <category term="gpu" scheme="https://zhuxz0299.github.io/tags/gpu/"/>
    <category term="x11" scheme="https://zhuxz0299.github.io/tags/x11/"/>
    <category term="mesa" scheme="https://zhuxz0299.github.io/tags/mesa/"/>
    <category term="drm" scheme="https://zhuxz0299.github.io/tags/drm/"/>
    <category term="va-api" scheme="https://zhuxz0299.github.io/tags/va-api/"/>
    <category term="vulkan" scheme="https://zhuxz0299.github.io/tags/vulkan/"/>
    <content>
      <![CDATA[<h2 id="整体概览">整体概览</h2><p>首先展示整体工作流程，流程比较粗糙，不一定严谨。</p><img src='../../figure/linux_/Linux-显示系统与-GPU-栈/linux-desktop-application-stack.webp' width=1200 style="display: block; margin-left: auto; margin-right: auto;"><h2 id="解释说明">解释说明</h2><p>这里是对里面的每个部分做一些细节性的解释。</p><h3 id="视频加速-API-用户态视频驱动">视频加速 API/用户态视频驱动</h3><h4 id="VA-API">VA-API</h4><p>VA 就是 Video Acceleration 的缩写。</p><p>VA-API 是 Linux 桌面最通用的视频加速接口，涵盖：</p><ul><li>视频解码</li><li>视频编码</li><li>缩放、反交错、色彩转换、HDR tone mapping 等后处理</li><li>以硬件 surface 形式传递图像</li></ul><p>它通过 libva 加载具体厂商驱动，例如：应用 → <code>libva.so</code> → <code>radeonsi_drv_video.so</code> → <code>amdgpu</code> → AMD VCN。</p><ul><li><code>libva.so</code> 是 VA-API 的通用加载器和用户态接口</li><li><code>radeonsi_drv_video.so</code> 就是图中“用户态视频驱动”里 Mesa VA driver 的具体文件名</li><li><code>amdgpu</code> 在图中“GPU 内核驱动”里面</li><li>AMD VCN 是更底层的硬件引擎</li></ul><p>关于对应的 nvidia-vaapi-driver 驱动：是一个社区项目，它把 VA-API 解码请求转到 NVIDIA NVDEC 的兼容驱动；它只支持解码，不支持 VA-API 编码，而且主要针对 Firefox。普通 NVIDIA 播放器或转码程序还是建议直接使用 NVDEC。</p><h4 id="Vulkan-Video">Vulkan Video</h4><p>Vulkan Video 把编解码变成 Vulkan 队列、资源与同步模型的一部分。它的优势是：</p><ul><li>跨厂商</li><li>可以和 Vulkan 渲染、计算、图像处理共享资源</li><li>开发者能精细控制队列、内存与同步</li><li>适合播放器、游戏引擎和低拷贝媒体管线</li></ul><p>代价是 API 更底层、更复杂，驱动和应用支持仍在快速演进。</p><h4 id="VDPAU">VDPAU</h4><ul><li>NVIDIA 发起、后来获得跨厂商实现<ul><li>和 VA-API 类似，有 <code>libvdpau.so</code> 作为通用加载器，可以加载各个厂商的后端</li></ul></li><li>开放 Unix 视频解码/呈现 API<ul><li>也就是通常不支持编码</li></ul></li><li>如今主要承担 NVIDIA 原生兼容和旧应用支持</li></ul><h4 id="NVDEC-和-NVENC">NVDEC 和 NVENC</h4><p>NVIDIA 的原生接口。严谨的说是这样：</p><table><thead><tr><th>名称</th><th>严格含义</th></tr></thead><tbody><tr><td>NVDEC</td><td>NVIDIA GPU 中的硬件视频解码引擎</td></tr><tr><td>NVDECODE API</td><td>访问 NVDEC 的软件编程接口</td></tr><tr><td>NVENC</td><td>NVIDIA GPU 中的硬件视频编码引擎</td></tr><tr><td>NVENCODE API</td><td>访问 NVENC 的软件编程接口</td></tr><tr><td>Video Codec SDK</td><td>包含上述 API、头文件、示例和文档的 SDK</td></tr></tbody></table><p>同时在用户态驱动方面，因为 NVDECODE API 是 NVIDIA 专有的接口，因此不需要像 VA-API 那样弄一些插件式的后端。</p><h4 id="其它">其它</h4><ul><li>V4L2：主要用于 Rockchip、Allwinner、NXP、Qualcomm、树莓派等 SoC</li><li>QSV/oneVPL：Intel 的视频接口</li><li>AMF：AMD 的厂商接口</li></ul><p>在普通视频播放的场景下，VA-API 已经比较好用了，所以一般没有必要用 Intel 和 AMD 的厂商接口。同时在 Windows 中，这两个接口更为常用。</p><h3 id="显示协议">显示协议</h3><ul><li>Display Server：负责应用和显示系统之间的通信。管理窗口资源、输入事件、buffer 提交等。</li><li>Compositor：合成器。把多个窗口的 buffer 合成最终桌面画面，处理透明、阴影、缩放、动画，最后提交给 DRM/KMS。</li><li>WM/Window Manager：窗口管理器。负责窗口位置、焦点、移动、最大化、工作区、标题栏等。</li></ul><h3 id="图形渲染-API-用户态图形驱动">图形渲染 API/用户态图形驱动</h3><p>OpenGL 和 Vulkan 的核心工作，是让程序用一种相对统一的方式命令 GPU 完成渲染。它们处在应用/渲染引擎与具体显卡驱动之间。这个层级的 API 在不同系统为：</p><ul><li>Windows：Direct3D</li><li>macOS/iOS：Metal</li><li>Linux：主要是 Vulkan、OpenGL、OpenGL ES</li></ul><p>这些 API 由显卡厂商或者 Mesa 开源驱动直接实现，能直接表达 GPU 的资源、流水线以及命令。</p><p>同时这些 API 画出来的结果要交给 Linux 桌面显示系统，还需要一个接入层。常用接入层包括：EGL、GLX、Vulkan WSI，它们与图形渲染 API 的对应关系为：<br>OpenGL</p><ul><li>GLX：OpenGL 连接 X11</li><li>EGL：OpenGL/OpenGL ES 连接 Wayland/X11/GBM 等</li></ul><p>Vulkan</p><ul><li>WSI (Vulkan Window System Integration)：连接 Wayland / X11 / KMS / Android 等</li></ul><h3 id="GPU-内核驱动">GPU 内核驱动</h3><p>用户态驱动和内核驱动对应关系：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">AMD：</span><br><span class="line">  用户态图形：Mesa RADV / radeonsi</span><br><span class="line">  用户态视频：Mesa VA</span><br><span class="line">  内核：amdgpu</span><br><span class="line"></span><br><span class="line">Intel：</span><br><span class="line">  用户态图形：Mesa ANV / Iris</span><br><span class="line">  用户态视频：intel-media-driver / Mesa</span><br><span class="line">  内核：i915 / xe</span><br><span class="line"></span><br><span class="line">NVIDIA 专有：</span><br><span class="line">  用户态图形/视频/计算：NVIDIA userspace stack</span><br><span class="line">  内核：nvidia</span><br><span class="line"></span><br><span class="line">NVIDIA 开源 Nouveau：</span><br><span class="line">  用户态图形：Mesa NVK / nouveau</span><br><span class="line">  内核：nouveau</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/c70aea57.html</id>
    <link href="https://zhuxz0299.github.io/posts/c70aea57.html"/>
    <published>2026-08-05T11:33:25.000Z</published>
    <summary>梳理 Linux 桌面从应用程序、图形与视频加速 API、显示协议及合成器，到用户态和内核态 GPU 驱动的整体工作栈。</summary>
    <title>Linux 显示系统与 GPU 栈</title>
    <updated>2026-09-04T12:14:18.243Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Linux" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Linux/"/>
    <category term="linux" scheme="https://zhuxz0299.github.io/tags/linux/"/>
    <category term="xdg" scheme="https://zhuxz0299.github.io/tags/xdg/"/>
    <category term="mime" scheme="https://zhuxz0299.github.io/tags/mime/"/>
    <category term="freedesktop" scheme="https://zhuxz0299.github.io/tags/freedesktop/"/>
    <category term="xdg-utils" scheme="https://zhuxz0299.github.io/tags/xdg-utils/"/>
    <content>
      <![CDATA[<div class="note info flat"><p>主要参考：</p><ul><li><a href="https://wiki.gentoo.org/wiki/XDG">XDG-gentoo wiki</a></li><li><a href="https://www.freedesktop.org/wiki/">Welcome to freedesktop.org</a></li><li><a href="https://wiki.archlinux.org/title/XDG_Base_Directory">ArchWiki - XDG Base Directory</a></li><li><a href="https://specifications.freedesktop.org/basedir/latest/">XDG Base Directory Specification</a></li><li><a href="https://wiki.archlinux.org/title/XDG_user_directories">ArchWiki - XDG user directories</a></li><li><a href="https://wiki.archlinux.org/title/Xdg-utils">ArchWiki - xdg-utils</a></li><li><a href="https://wiki.archlinux.org/title/XDG_Desktop_Portal">ArchWiki - XDG Desktop Portal</a></li></ul></div><h2 id="XDG-是什么">XDG 是什么</h2><p>XDG 是 X Desktop Group 的缩写，目前是 <a href="http://freedesktop.org">freedesktop.org</a> 体系下的一组跨桌面规范、约定和工具的统称。由于当前的桌面协议不仅仅有 X11，Wayland 也被更加广泛的使用，因此 <a href="http://freedesktop.org">freedesktop.org</a> 现在将 XDG 解释为 Cross-Desktop Group。</p><h2 id="XDG-Base-Directory">XDG Base Directory</h2><p>这个规范和 FHS 有点类似，不过它主要是在用户的层级规定各种文件应该放在哪些文件夹下。XDG Base Directory Specification 的总体思路为，将文件进行分类，每种类别对应一个环境变量，而每个环境变量通常有默认值，这个默认值就是 XDG 设定的默认路径；同时还有一些环境变量定义的是搜索路径。</p><h3 id="用户文件夹">用户文件夹</h3><ul><li><code>$XDG_CONFIG_HOME</code><ul><li>用户级别的配置文件写入的地方 (类似于 <code>/etc</code>).</li><li>默认为 <code>$HOME/.config</code></li><li>早期的应用通常直接将配置文件作为 dotfile 放在 <code>$HOME</code> 目录下，目前很多应用已经转向 XDG 标准</li></ul></li><li><code>$XDG_CACHE_HOME</code><ul><li>用户级别的缓存写入的地方 (类似于 <code>/var/cache</code>).</li><li>默认为 <code>$HOME/.cache</code></li></ul></li><li><code>$XDG_DATA_HOME</code><ul><li>用户级别的数据文件写入的地方 (类似于 <code>/usr/share</code>).</li><li>默认为 <code>$HOME/.local/share</code></li><li>例如由 <code>uv</code> 下载的 python 就会放到这底下；从 Windows 复制过来的字体文件也通常推荐放到这底下</li></ul></li><li><code>$XDG_STATE_HOME</code><ul><li>用户级别的状态文件写入的地方 (类似于 <code>/var/lib</code>).</li><li>默认为 <code>$HOME/.local/state</code></li></ul></li><li><code>$XDG_RUNTIME_DIR</code><ul><li>用来放一些用户级的运行时数据，默认由 <code>pam_systemd</code> 设置</li><li><code>pam_systemd</code> 将其设置为 <code>/run/user/$UID</code></li></ul></li><li><code>$HOME/.local/bin</code><ul><li>存放用户级别的可执行文件的地方</li><li>Note: 没有 <code>$XDG_BIN_HOME</code> 这个环境变量</li></ul></li></ul><h3 id="系统文件夹">系统文件夹</h3><ul><li><code>$XDG_DATA_DIRS</code><ul><li>定义了在 <code>$XDG_DATA_HOME</code> 基础目录之外搜索数据文件时按优先级排序的基础目录集合</li><li>默认为 <code>/usr/local/share/:/usr/share/</code></li></ul></li><li>定义了在 <code>$XDG_CONFIG_HOME</code> 基础目录之外搜索配置文件的偏好顺序基础目录集合<ul><li>默认为 <code>/etc/xdg</code></li></ul></li></ul><h2 id="xdg-utils">xdg-utils</h2><p>xdg-utils 提供了用于管理 XDG MIME 应用程序的官方工具集。</p><div class="note info flat"><p>MIME 是什么：MIME 的全称是 Multipurpose Internet Mail Extensions，直译为 多用途互联网邮件扩展。</p><p>它最初是为电子邮件设计的，用来描述附件或正文内容的类型，例如：<code>text/plain</code>, <code>text/html</code>, <code>image/png</code>, <code>application/pdf</code>。后来 MIME 类型被广泛用于 Web、桌面系统和文件管理器，不再只和邮件有关。它现在可以理解为一种标准化的“内容类型标识”。</p></div><h3 id="xdg-utils-下的具体工具">xdg-utils 下的具体工具</h3><ul><li>xdg-desktop-menu - 安装桌面菜单项</li><li>xdg-desktop-icon - 将桌面条目复制到用户的桌面</li><li>xdg-email - 在用户首选的电子邮件客户端中撰写新邮件，可预先填写主题及其他信息</li><li>xdg-icon-resource - 安装图标资源</li><li>xdg-mime - 查询与安装 MIME 类型及关联</li><li>xdg-open - 在用户首选应用程序中打开文件或 URI</li><li>xdg-screensaver - 启用、禁用或暂停屏幕保护程序</li><li>xdg-settings - 获取或设置默认网页浏览器及 URL 处理器</li></ul><p>大部分工具平时基本不会手动调用，例如安装桌面菜单、复制 <code>.desktop</code> 文件到桌面、安装图标资源等工作通常会由安装程序自动执行。</p><h3 id="识别桌面使用的环境变量">识别桌面使用的环境变量</h3><p>xdg-utils 通过以下顺序检查桌面环境变量来判断当前使用的桌面，用于后续调用特定桌面工具（例如在 GNOME 中 <code>xdg-mime</code> 和 <code>xdg-open</code> 调用的工具都是 <code>gio</code>）：</p><ol><li>检查标准环境变量 <code>XDG_CURRENT_DESKTOP</code></li><li>检查传统的 fallback 或者查看是否存在某些桌面自定义的专有环境变量<ul><li>例如 KDE 会设置 <code>KDE_FULL_SESSION=true</code></li></ul></li><li>检查遗留变量 <code>DESKTOP_SESSION</code></li></ol><p>如果上述变量被检测到，xdg-utils 会设置变量 <code>DE</code> 为对应桌面；若上述变量均未被检测到，则用户可以手动设置 <code>DE</code>。</p><p>各个桌面与环境变量的对应关系如下表：</p><table><thead><tr><th>Desktop Environment</th><th><code>XDG_CURRENT_DESKTOP</code></th><th><code>DE</code></th><th><code>DESKTOP_SESSION</code></th></tr></thead><tbody><tr><td>–</td><td><code>X-Generic</code></td><td><code>generic</code></td><td>–</td></tr><tr><td>Cinnamon</td><td><code>Cinnamon</code>, <code>X-Cinnamon</code></td><td><code>cinnamon</code></td><td>–</td></tr><tr><td>Deepin</td><td><code>Deepin</code>, <code>DEEPIN</code>, <code>deepin</code></td><td><code>deepin</code></td><td>–</td></tr><tr><td>Enlightenment</td><td><code>ENLIGHTENMENT</code></td><td><code>enlightenment</code></td><td>–</td></tr><tr><td>GNOME</td><td><code>GNOME</code></td><td><code>gnome</code></td><td><code>gnome</code></td></tr><tr><td>GNOME Flashbackashback</td><td><code>GNOME-Flashback</code>, <code>GNOME-Flashback:GNOME</code></td><td><code>gnome</code></td><td><code>gnome</code></td></tr><tr><td>KDE Plasmaasma</td><td><code>KDE</code></td><td><code>kde</code></td><td>–</td></tr><tr><td>LXDE</td><td><code>LXDE</code></td><td><code>lxde</code></td><td><code>LXDE</code></td></tr><tr><td>LXQt</td><td><code>LXQt</code></td><td><code>lxqt</code></td><td>–</td></tr><tr><td>MATE</td><td><code>MATE</code></td><td><code>mate</code></td><td><code>mate</code></td></tr><tr><td>Xfce</td><td><code>XFCE</code></td><td><code>xfce</code></td><td><code>xfce</code>, <code>xfce4</code>, <code>Xfce Session</code></td></tr></tbody></table><h3 id="xdg-mime">xdg-mime</h3><p><code>xdg-mime</code> 支持：</p><ol><li>查询文件属于什么 MIME 类型<ul><li>例如 <code>xdg-mime query filetype photo.jpg</code>，输出可能为 <code>image/jpeg</code></li></ul></li><li>查询某种 MIME 类型的默认应用<ul><li>例如 <code>xdg-mime query default image/jpeg</code>，输出可能为 <code>org.kde.gwenview.desktop</code></li></ul></li><li>设置某种 MIME 类型的默认应用<ul><li>例如 <code>xdg-mime default org.kde.dolphin.desktop inode/directory</code></li></ul></li></ol><p><code>xdg-mime</code> 建立的是 MIME 类型与 <code>.desktop</code> 应用入口之间的关联，而非直接把文件扩展名绑定到可执行程序。</p><ul><li>例如 <code>org.kde.gwenview.desktop</code> 就是一个 <code>.desktop</code> 文件的文件名，也是其 Desktop File ID</li><li>上述 <code>.desktop</code> 文件的搜索路径是 <code>$XDG_DATA_HOME/applications/</code> 以及 <code>$XDG_DATA_DIRS</code> 中每个目录的 <code>applications/</code> 子目录，展开默认环境变量之后有搜索顺序：<ol><li><code>~/.local/share/applications/</code></li><li><code>/usr/local/share/applications/</code></li><li><code>/usr/share/applications/</code></li></ol></li></ul><h4 id="xdg-mime-的实际调用工具">xdg-mime 的实际调用工具</h4><p><code>xdg-mime</code> 在查询某个文件的 MIME 类型时，会调用桌面环境下对应的工具：</p><table><thead><tr><th>Desktop Environment</th><th>Program</th></tr></thead><tbody><tr><td>Cinnamon</td><td><code>gio</code></td></tr><tr><td>GNOME</td><td><code>gio</code></td></tr><tr><td>GNOME Flashbackashback</td><td><code>gio</code></td></tr><tr><td>LXDE</td><td><code>gio</code></td></tr><tr><td>MATE</td><td><code>gio</code></td></tr><tr><td>Xfce</td><td><code>gio</code></td></tr><tr><td>Deepin</td><td>-</td></tr><tr><td>Enlightenment</td><td>-</td></tr><tr><td>LXQt</td><td>-</td></tr><tr><td>KDE Plasmaasma</td><td><code>kmimetypefinder</code></td></tr></tbody></table><p>如果没有专用工具，会进入通用处理路径：</p><ol><li>如果系统中存在 <code>mimetype</code>，则使用</li><li>如果系统中存在 <code>file</code>，则使用</li></ol><h3 id="xdg-open">xdg-open</h3><p><code>xdg-open</code> 同样也是调用底层的工具工作：</p><table><thead><tr><th>Desktop Environment</th><th>Program</th></tr></thead><tbody><tr><td>Cinnamon</td><td><code>gio</code></td></tr><tr><td>GNOME</td><td><code>gio</code></td></tr><tr><td>GNOME Flashback</td><td><code>gio</code></td></tr><tr><td>MATE</td><td><code>gio</code></td></tr><tr><td>Deepin</td><td><code>dde-open</code></td></tr><tr><td>Enlightenment</td><td><code>enlightenment_open</code></td></tr><tr><td>KDE Plasma</td><td><code>kde-open</code></td></tr><tr><td>LXDE</td><td><code>pcmanfm</code></td></tr><tr><td>LXQt</td><td>–</td></tr><tr><td>Xfce</td><td><code>exo-open</code></td></tr></tbody></table><p>假如没有专用打开工具，同样也进入通用路径：</p><ol><li>通过 <code>xdg-mime</code> 查询该文件类型对应的默认 <code>.desktop</code> 应用，运行 <code>.desktop</code> 文件中的启动命令</li><li>使用 <code>run-mailcap</code> 工具</li><li>使用 <code>mimeopen</code> 工具</li></ol><h3 id="xdg-settings">xdg-settings</h3><p>用于设置默认浏览器以及某个 URI scheme 的默认处理应用：</p><ul><li><code>xdg-settings set default-web-browser org.mozilla.firefox.desktop</code></li><li><code>xdg-settings set default-url-scheme-handler mailto net.thunderbird.Thunderbird.desktop</code></li></ul>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/fc95dfca.html</id>
    <link href="https://zhuxz0299.github.io/posts/fc95dfca.html"/>
    <published>2026-07-24T06:35:22.000Z</published>
    <summary>介绍 XDG 的由来与 Base Directory 目录规范，并梳理 xdg-utils 如何在不同 Linux 桌面环境中管理 MIME 类型、默认应用和文件打开方式。</summary>
    <title>XDG-Linux 桌面的通用规范与工具</title>
    <updated>2026-09-04T12:14:18.243Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Linux" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Linux/"/>
    <category term="linux" scheme="https://zhuxz0299.github.io/tags/linux/"/>
    <category term="fhs" scheme="https://zhuxz0299.github.io/tags/fhs/"/>
    <category term="filesystem" scheme="https://zhuxz0299.github.io/tags/filesystem/"/>
    <category term="directory" scheme="https://zhuxz0299.github.io/tags/directory/"/>
    <content>
      <![CDATA[<div class="note info flat"><p>官方文档：<a href="https://specifications.freedesktop.org/fhs/latest/index.html">HFS 3.0</a>，<a href="https://fhs.pages.freedesktop.org/-/fhs-spec/-/jobs/93479708/artifacts/build/fhs/xmlto-html/fhs-multipage.html">HFS 4.0 草案</a>。前者的内容部分有些过时，后者还未完成，因此本文同时参考两者。</p></div><h2 id="FHS-的内容与目的">FHS 的内容与目的</h2><p>FHS 是 Filesystem Hierarchy Standard，它规定了在 UNIX-like 操作系统中，各类文件和目录原则上应当放在哪里，即 <code>/run</code>、<code>/tmp</code>、<code>/opt</code>、<code>/srv</code> 等目录各自承担什么职责。其官网定义为：</p><blockquote><p>This standard consists of a set of requirements and guidelines for file and directory placement under UNIX-like operating systems.</p></blockquote><p>FHS 希望让用户或者程序看到一个文件的用途，大致就能知道它应该在哪里；看到一个目录，也能大致知道里面是什么。其官网描述为：</p><blockquote><p>This standard enables:</p><ul><li>Software to predict the location of installed files and directories, and</li><li>Users to predict the location of installed files and directories.</li></ul></blockquote><h2 id="FHS-划分的基本思路">FHS 划分的基本思路</h2><p>以下为 FHS 4.0 草案的划分规则：</p><table><thead><tr><th>分类</th><th>含义</th></tr></thead><tbody><tr><td>Shareable / machine-specific</td><td>能否被多台机器共同使用</td></tr><tr><td>Executable / data</td><td>能否直接作为程序执行</td></tr><tr><td>Architecture-dependent / independent</td><td>是否依赖 CPU 架构或 ABI</td></tr><tr><td>Static / mutable</td><td>正常运行过程中是否会变化</td></tr></tbody></table><p>其中：</p><ul><li>static 又分为固定文件(fixed)和管理员可编辑(editable)的配置<ul><li>由这个划分规则：<code>/usr</code> 里面主要是静态且不应当手动编辑的文件；<code>/etc</code> 里面则是静态的且可以由系统管理员配置的文件</li></ul></li><li>mutable 又分为持久状态(persistent)、短暂运行状态( transient)和临时文件(temporary)<ul><li>由这个划分规则：可变的长期状态放在 <code>/var</code>（例如安装包 cache）；本次启动的状态放在 <code>/run</code>；普通临时文件放在 <code>/tmp</code></li></ul></li></ul><h2 id="三种软件布局">三种软件布局</h2><p>这个是 FHS 4.0 草案相比 3.0 的改进：不再假设所有 Unix 系统必须使用同一棵软件目录树，而是引入了 profile。发行版可以选择不同的软件安装布局，但仍遵循相同的文件分类原则。</p><h3 id="Merged-usr">Merged <code>/usr</code></h3><p>应该是现代 Linux 发行版最常见的布局。</p><p>Merged <code>/usr</code> 将发行版提供的软件统一安装到 <code>/usr</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">/usr/bin</span><br><span class="line">/usr/sbin</span><br><span class="line">/usr/libexec</span><br><span class="line">/usr/include</span><br><span class="line">/usr/lib</span><br><span class="line">/usr/share</span><br></pre></td></tr></table></figure><p>为了兼容旧程序，发行版可以继续提供 <code>/bin</code>、<code>/sbin</code> 和 <code>/lib</code>，但通常是软链接：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/bin   -&gt; usr/bin</span><br><span class="line">/sbin  -&gt; usr/sbin</span><br><span class="line">/lib   -&gt; usr/lib</span><br></pre></td></tr></table></figure><p>某些发行版还会进一步合并：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">/usr/sbin    -&gt; bin</span><br><span class="line">/usr/libexec -&gt; lib</span><br></pre></td></tr></table></figure><h3 id="Split-usr">Split <code>/usr</code></h3><p>传统布局，FHS 3.0 里面默认就是这种布局。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">/bin       启动早期和救援所需程序</span><br><span class="line">/usr/bin   其余普通程序</span><br><span class="line"></span><br><span class="line">/sbin      启动早期和救援所需管理程序</span><br><span class="line">/usr/sbin  其余管理程序</span><br><span class="line"></span><br><span class="line">/lib       /bin、/sbin 所需基础库</span><br><span class="line">/usr/lib   其余库</span><br></pre></td></tr></table></figure><p>这种布局主要源于早期系统没有初始内存盘（initial ramdisk，应该就是 <code>initramfs</code>），因此在 <code>/usr</code> 尚未挂载时，根文件系统必须包含足够的命令和库来完成启动和挂载操作。（来源：<a href="https://fhs.pages.freedesktop.org/-/fhs-spec/-/jobs/93479708/artifacts/build/fhs/xmlto-html/ch05s03.html#:~:text=Rationale">Rationale</a>）</p><h3 id="Store-based">Store-based</h3><p>相对比较小众，典型代表为 <code>NixOS</code>。</p><p>Store-based 布局给每个软件包分配独立的安装前缀，例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/nix/store/&lt;hash&gt;-package</span><br><span class="line">/gnu/store/&lt;hash&gt;-package</span><br><span class="line">/store/&lt;package&gt;</span><br></pre></td></tr></table></figure><p>不同版本或不同构建参数的软件可以同时存在：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">/nix/store/abc123-gcc-14</span><br><span class="line">/nix/store/def456-gcc-15</span><br></pre></td></tr></table></figure><p>系统再通过：</p><ul><li><code>PATH</code>；</li><li>库搜索路径；</li><li>符号链接集合，即 symlink farm；</li><li>每个服务或用户的独立环境；</li></ul><p>向程序和用户提供统一的软件视图。</p><h2 id="根目录中的主要目录">根目录中的主要目录</h2><div class="note primary flat"><p>这一章主要基于 Merged <code>/usr</code> 布局考虑。</p></div><h3 id="boot：启动所需文件"><code>/boot</code>：启动所需文件</h3><p>保存 bootloader 在启动过程中需要读取的内容，具体内容在 <a href="https://zhuxz0299.github.io/posts/36b44dc6.html">boot 文件夹与 EFI 分区</a> 这篇文章中有非常详细的讨论。</p><h3 id="dev：设备和类似设备的接口"><code>/dev</code>：设备和类似设备的接口</h3><p><code>/dev</code> 通过文件形式向程序提供设备和部分内核接口，这个接口使得各个设备可以像文件一样被读取写入。假设有一个串口 <code>/dev/ttyUSB0</code>，可以像写入文件一样往里面写东西：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    FILE *fp = fopen(<span class="string">&quot;/dev/ttyUSB0&quot;</span>, <span class="string">&quot;r+&quot;</span>);</span><br><span class="line">    <span class="type">char</span> buf[<span class="number">100</span>];</span><br><span class="line">    fread(buf, <span class="number">1</span>, <span class="keyword">sizeof</span>(buf), fp);</span><br><span class="line">    fclose(fp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>或者也可以调用更底层的 POSIX 接口：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> fd = open(<span class="string">&quot;/dev/ttyUSB0&quot;</span>, O_RDWR);</span><br><span class="line">    <span class="type">char</span> buf[<span class="number">100</span>];</span><br><span class="line">    read(fd, buf, <span class="number">100</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>/dev</code> 底下的常见条目包括（底下只列了一点，事实上非常多）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">/dev/null</span><br><span class="line">/dev/zero</span><br><span class="line">/dev/full</span><br><span class="line">/dev/urandom</span><br><span class="line">/dev/tty</span><br><span class="line">/dev/nvme0n1</span><br><span class="line">/dev/sda</span><br></pre></td></tr></table></figure><p>现代 Linux 通常由内核、<code>devtmpfs</code> 和设备管理服务动态维护 <code>/dev</code>。应用程序不应随意在 <code>/dev</code> 中创建普通文件或 socket；运行时 socket 更适合放在 <code>/run</code>。</p><h3 id="proc：进程和内核运行信息"><code>/proc</code>：进程和内核运行信息</h3><p><code>/proc</code> 通常挂载 procfs，是一个 API 文件系统，即将一部分信息和控制接口以文件和目录的形式暴露给用户空间，而非存储介质上真的有这些文件。</p><p>常见内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">/proc/&lt;pid&gt;/       某个进程的信息</span><br><span class="line">/proc/cpuinfo      CPU 信息</span><br><span class="line">/proc/meminfo      内存信息</span><br><span class="line">/proc/cmdline      内核启动参数</span><br><span class="line">/proc/mounts       当前挂载信息</span><br><span class="line">/proc/sys/         可调节的内核参数</span><br></pre></td></tr></table></figure><p>这些“文件”的内容通常由内核实时生成。</p><h3 id="sys：设备和内核对象信息"><code>/sys</code>：设备和内核对象信息</h3><p><code>/sys</code> 和 <code>/proc</code> 非常类似，只是挂载的是 sysfs。两者以及 <code>/dev</code> 的区别为：</p><table><thead><tr><th>目录</th><th>主要内容</th><th>典型用途</th></tr></thead><tbody><tr><td><code>/proc</code></td><td>进程、系统运行状态、内核参数</td><td>查看进程内存、CPU 信息、修改 sysctl 参数</td></tr><tr><td><code>/sys</code></td><td>设备、驱动、总线、内核对象</td><td>查看硬件属性、绑定驱动、控制设备</td></tr><tr><td><code>/dev</code></td><td>设备节点</td><td>实际与设备进行数据传输或操作</td></tr></tbody></table><h3 id="etc：系统级配置"><code>/etc</code>：系统级配置</h3><div class="note default flat"><p><code>/etc</code> 最初来自拉丁语 et cetera，意思是“等等、其他杂项”，后续逐渐演进成当前用途，可以记忆为 Editable Text Configuration。</p></div><p><code>/etc</code> 保存：当前主机的、系统范围的、管理员可编辑的配置。</p><p>例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">/etc/fstab</span><br><span class="line">/etc/hostname</span><br><span class="line">/etc/passwd</span><br><span class="line">/etc/ssh/</span><br><span class="line">/etc/systemd/</span><br><span class="line">/etc/pacman.conf</span><br></pre></td></tr></table></figure><p>判断某个文件是否应该放在 <code>/etc</code>，可以考虑：</p><ul><li>它是否影响整台机器？</li><li>它是否应当由管理员编辑？</li><li>它是否属于配置，而不是程序自动产生的状态？</li></ul><p>如果答案是肯定的，它通常属于 <code>/etc</code>。</p><h3 id="home：普通用户主目录"><code>/home</code>：普通用户主目录</h3><p>通常每个交互用户拥有一个 home：<code>/home/&lt;user_name&gt;</code>，其中保存：</p><ul><li>用户文档；</li><li>下载内容；</li><li>用户自己的程序；</li><li>用户级配置；</li><li>用户级缓存和状态。</li></ul><h3 id="media：可移动介质挂载点"><code>/media</code>：可移动介质挂载点</h3><p>用于挂载用户插入的可移动介质，例如：<code>/media/&lt;user&gt;/&lt;volume&gt;</code>。常见对象包括：</p><ul><li>U 盘；</li><li>移动硬盘；</li><li>光盘；</li></ul><p>实际桌面 Linux 也经常使用：<code>/run/media/&lt;user&gt;/&lt;volume&gt;</code></p><h3 id="mnt：管理员临时挂载点"><code>/mnt</code>：管理员临时挂载点</h3><div class="note default flat"><p><code>/mnt</code> 是 mount 的缩写。</p></div><p><code>/mnt</code> 用于管理员临时挂载某个文件系统，比较常见的用途为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mount /dev/nvme0n1p3 /mnt</span><br></pre></td></tr></table></figure><p>以此安装系统、进入 chroot 或手工检查分区。</p><h3 id="srv：系统服务直接提供的数据"><code>/srv</code>：系统服务直接提供的数据</h3><div class="note default flat"><p><code>/srv</code> 是 service 的缩写。感觉这个东西在个人电脑上不太常用。</p></div><p><code>/srv</code> 保存由当前机器上的服务使用，而且管理员或用户需要直接理解其目录结构的数据。例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/srv/www/</span><br><span class="line">/srv/ftp/</span><br><span class="line">/srv/example.com/</span><br></pre></td></tr></table></figure><h3 id="tmp：短期临时文件"><code>/tmp</code>：短期临时文件</h3><p><code>/tmp</code> 用于程序的普通临时文件，默认随时都可能被清理掉。而且这个目录通常对所有用户可写，Codex 就很喜欢在权限不够的时候在这里面操作。</p><h3 id="run：本次启动的运行状态"><code>/run</code>：本次启动的运行状态</h3><p><code>/run</code> 保存 transient mutable data，即：</p><ul><li>会在运行过程中变化；</li><li>只在当前启动周期内有效；</li></ul><p>常见内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">/run/&lt;daemon&gt;.pid</span><br><span class="line">/run/&lt;application&gt;/</span><br><span class="line">/run/user/&lt;uid&gt;/</span><br><span class="line">/run/systemd/</span><br><span class="line">/run/dbus/</span><br><span class="line">/run/lock/</span><br></pre></td></tr></table></figure><h4 id="run-lock"><code>/run/lock</code></h4><p>用于保存系统级资源锁文件。例如多个程序共享串口或某个物理设备时，可以用锁文件表示该资源当前被占用。锁文件是当前运行状态，不应跨重启保留，因此它们应位于 <code>/run/lock</code>，而不是旧式的 <code>/var/lock</code>。</p><h3 id="var：持久可变数据"><code>/var</code>：持久可变数据</h3><p>保存会在正常运行中改变，并且需要跨进程运行或系统重启保留的数据。</p><h4 id="var-cache：可重新生成的缓存"><code>/var/cache</code>：可重新生成的缓存</h4><p>缓存具有两个关键性质：</p><ul><li>删除它不会造成真正的数据丢失；</li><li>程序能够重新计算或重新下载。</li></ul><p>常见文件夹有：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">/var/cache/pacman/</span><br><span class="line">/var/cache/dnf/</span><br><span class="line">/var/cache/fontconfig/</span><br><span class="line">/var/cache/man/</span><br></pre></td></tr></table></figure><p>由于某些包管理器不会自动删除之前的包，所以这个文件夹可能会非常大。这些东西可以手动清理掉，问题不大，后续可以重新下载。</p><h4 id="var-lib：应用和系统的持久状态"><code>/var/lib</code>：应用和系统的持久状态</h4><p>保存程序内部使用、跨重启保留的状态，例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">/var/lib/systemd/</span><br><span class="line">/var/lib/bluetooth/</span><br><span class="line">/var/lib/docker/</span><br><span class="line">/var/lib/libvirt/</span><br><span class="line">/var/lib/NetworkManager/</span><br></pre></td></tr></table></figure><p>每个应用通常使用 <code>/var/lib/&lt;application&gt;/</code>。</p><h4 id="var-log：日志"><code>/var/log</code>：日志</h4><p>保存系统和应用的日志。例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/var/log/pacman.log</span><br><span class="line">/var/log/audit/</span><br><span class="line">/var/log/nginx/</span><br></pre></td></tr></table></figure><p>如果应用自己维护日志文件，应使用 <code>/var/log/&lt;application&gt;/</code>；如果应用只把日志发送给 syslog、journald 或其他集中式日志服务，则不需要另外创建自己的 <code>/var/log</code> 目录。</p><h4 id="var-spool：等待处理的任务"><code>/var/spool</code>：等待处理的任务</h4><p>spool 数据是已经提交、尚未处理完的真实工作。例如：</p><ul><li>等待打印的文档；</li><li>等待发送的邮件；</li><li>等待执行的定时任务；</li></ul><p>一般结构为：<code>/var/spool/&lt;application&gt;/</code></p><p>缓存与 spool 的关键区别：</p><table><thead><tr><th>类型</th><th>能否随意删除</th></tr></thead><tbody><tr><td>cache</td><td>通常可以，之后能重新生成</td></tr><tr><td>spool</td><td>通常不可以，删除可能丢失尚未完成的任务</td></tr></tbody></table><h4 id="var-run-与-var-lock"><code>/var/run</code> 与 <code>/var/lock</code></h4><p>主要作为兼容路径存在：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">/var/run  -&gt; /run</span><br><span class="line">/var/lock -&gt; /run/lock</span><br></pre></td></tr></table></figure><h3 id="usr：发行版提供的静态软件层次"><code>/usr</code>：发行版提供的静态软件层次</h3><div class="note default flat"><p><code>/usr</code> 最初通常解释为 user，因为在早期 Unix 中，<code>/usr</code> 用来存放用户的主目录，作用相当于现在的 <code>/home</code>。而在现代语境下，可以将 <code>/usr</code> 解释为 Unix System Resources。</p></div><p>在 Merged <code>/usr</code> 模型中，<code>/usr</code> 是发行版提供的软件和静态数据的主要位置。它通常具有以下特征：</p><ul><li>正常运行过程中基本不改变(static)；</li><li>可由包管理器统一维护；</li><li>原则上可以只读挂载(fixed)；</li><li>同一发行版和版本的兼容机器可以共享(Shareable)；</li></ul><h4 id="usr-bin-与-usr-sbin：普通命令与系统管理命令"><code>/usr/bin</code> 与 <code>/usr/sbin</code>：普通命令与系统管理命令</h4><p><code>/usr/bin</code> 保存大多数可以由用户直接执行的命令。草案要求：</p><ul><li>文件必须是真正可由 execve 执行的程序；</li><li>不得存放运行时不断变化的数据；</li><li>不得在其中创建子目录。</li></ul><p>不能创建子目录的原因之一是 shell 通过 <code>PATH</code> 搜索命令时只匹配直接条目，不会递归搜索。</p><p>在 Merged <code>/usr</code> 系统中为了兼容一些旧程序，会有软链接：<code>/bin -&gt; usr/bin</code></p><p><code>/usr/sbin</code> 放：</p><ul><li>只对管理员有实际用途的命令；</li><li>实现系统服务的可执行程序。</li></ul><p><code>/usr/sbin</code> 程序通常对普通用户没有用途，因此历史上不一定进入普通用户的 <code>PATH</code>。但实际上有些发行版直接将两者合一 <code>/usr/sbin -&gt; bin</code>。</p><h4 id="usr-lib-与-usr-lib-qual-：库和架构相关数据"><code>/usr/lib</code> 与 <code>/usr/lib&lt;qual&gt;</code>：库和架构相关数据</h4><p>主要保存各种库，还有软件包架构数据、某些程序内部使用文件。例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">/usr/lib/libc.so</span><br><span class="line">/usr/lib/libssl.so</span><br><span class="line">/usr/lib/&lt;application&gt;/</span><br><span class="line">/usr/lib/modules/</span><br></pre></td></tr></table></figure><h4 id="usr-libexec：程序内部使用的可执行文件"><code>/usr/libexec</code>：程序内部使用的可执行文件</h4><p>保存主要由其他程序调用，而不是由用户直接运行的辅助程序。例如，一个大型应用可能具有：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/usr/libexec/&lt;application&gt;/&lt;helper&gt;</span><br></pre></td></tr></table></figure><p>与 <code>/usr/bin</code> 不同，<code>/usr/libexec</code> 允许创建子目录，因为其中的程序通常通过完整路径启动，而不是通过 <code>PATH</code> 搜索。某些发行版使用软链接将其链接到 <code>lib</code>。</p><h4 id="usr-include：开发头文件"><code>/usr/include</code>：开发头文件</h4><p>该文件夹下内容可以理解为 Linux 系统中面向编译器的公共接口说明书；由于 Unix 的底层接口主要采用 C ABI，所以其中绝大多数内容表现为 C/C++ 头文件：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/usr/include/stdio.h</span><br><span class="line">/usr/include/linux/</span><br><span class="line">/usr/include/c++/</span><br></pre></td></tr></table></figure><p>这些文件用于编译程序。头文件可能与架构有关，因此 <code>/usr/include</code> 属于 Architecture-dependent。</p><h4 id="usr-share：架构无关的静态数据"><code>/usr/share</code>：架构无关的静态数据</h4><p>保存数据为：</p><ul><li>与 CPU 架构无关；</li><li>正常运行过程中不变化；</li><li>可以在兼容机器之间共享；</li></ul><p>例如：</p><ul><li><code>/usr/share/doc</code>：软件包的普通文档</li><li><code>/usr/share/color</code>：软件包提供的色彩管理数据，例如 ICC profile</li><li><code>/usr/share/locale</code>：保存本地化数据，例如程序的翻译消息等</li></ul><h4 id="usr-local：管理员本地安装的软件"><code>/usr/local</code>：管理员本地安装的软件</h4><p>是未由发行版包管理器管理、由本机管理员自行编译安装的软件的默认前缀。比如执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">./configure</span><br><span class="line">make</span><br><span class="line"><span class="built_in">sudo</span> make install</span><br></pre></td></tr></table></figure><p>如果没有指定其他 prefix，许多构建系统默认安装到 <code>/usr/local</code></p><h3 id="opt：按软件包隔离的本地软件"><code>/opt</code>：按软件包隔离的本地软件</h3><ul><li>FHS 3.0 将 <code>/opt</code> 定义为第三方附加软件包的位置。</li><li>FHS 4.0 草案重新解释：现在第三方软件更常通过发行版附加仓库安装，因此 <code>/opt</code> 更适合保存“本地构建、未纳入包管理器，并希望每个软件包独立管理的软件”。</li></ul><p>目前 QQ、微信、WPS 这种软件会把自己放到 <code>/opt</code> 底下。</p><h2 id="传统兼容符号链接">传统兼容符号链接</h2><p>现代 Merged <code>/usr</code> 布局的系统仍然保留大量 Split <code>/usr</code> 布局的旧路径，用于兼容已有程序，主要为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">/bin       -&gt; usr/bin</span><br><span class="line">/sbin      -&gt; usr/sbin 或 usr/bin</span><br><span class="line">/lib       -&gt; usr/lib</span><br><span class="line">/var/run   -&gt; /run</span><br><span class="line">/var/lock  -&gt; /run/lock</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/ca5c3d69.html</id>
    <link href="https://zhuxz0299.github.io/posts/ca5c3d69.html"/>
    <published>2026-07-22T10:25:26.000Z</published>
    <summary>介绍 FHS 对 Unix-like 系统目录结构的分类原则、三种软件布局，以及 Linux 根目录下各主要目录的用途。</summary>
    <title>FHS-Filesystem Hierarchy Standard</title>
    <updated>2026-09-04T12:14:18.243Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Linux" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Linux/"/>
    <category term="linux" scheme="https://zhuxz0299.github.io/tags/linux/"/>
    <category term="swap" scheme="https://zhuxz0299.github.io/tags/swap/"/>
    <category term="systemd" scheme="https://zhuxz0299.github.io/tags/systemd/"/>
    <category term="suspend" scheme="https://zhuxz0299.github.io/tags/suspend/"/>
    <category term="hibernate" scheme="https://zhuxz0299.github.io/tags/hibernate/"/>
    <category term="s2idle" scheme="https://zhuxz0299.github.io/tags/s2idle/"/>
    <content>
      <![CDATA[<div class="note info flat"><p>主要参考：</p><ul><li><a href="https://docs.kernel.org/admin-guide/pm/sleep-states.html">Linux Kernel 文档 - System Sleep States</a></li><li><a href="https://www.freedesktop.org/software/systemd/man/latest/systemd-sleep.conf.html">systemd-sleep.conf</a>：睡眠、休眠等动作本身的策略配置。</li><li><a href="https://man7.org/linux/man-pages/man5/logind.conf.5.html">logind.conf</a>：合盖、电源键等事件触发什么动作。</li><li><a href="https://wiki.archlinux.org/title/Power_management/Suspend_and_hibernate">ArchWiki - Power management/Suspend and hibernate</a></li></ul></div><p>本文分成两部分：前半部分解释睡眠、休眠、<code>s2idle</code>、<code>deep</code>、S3 等概念；后半部分以 Arch Linux 为例，整理一套实际配置流程。</p><p>默认场景如下：</p><ul><li>使用 <code>systemd</code></li><li>使用 <code>GRUB</code></li><li>使用 <code>mkinitcpio</code></li><li>使用独立 <code>swap</code> 分区，而不是 <code>swapfile</code></li></ul><p>如果使用的是 <code>swapfile</code>，休眠还需要额外配置 <code>resume_offset=</code>，本文不展开。</p><h2 id="睡眠、休眠与-S-状态">睡眠、休眠与 S 状态</h2><h3 id="suspend-与-hibernate-的区别">suspend 与 hibernate 的区别</h3><p>日常说的“睡眠”一般对应 <code>suspend</code>：系统暂停用户态程序，把大部分设备切到低功耗状态，内存里的状态仍然保留。优点是进入和唤醒都快；缺点是断电后内存内容会丢失，所以没有保存的工作也会丢失。</p><p>日常说的“休眠”对应 <code>hibernate</code>：系统把内存镜像写入磁盘，通常写到 <code>swap</code>，然后关机或进入接近关机的低功耗状态。优点是彻底断电也能恢复；缺点是进入和恢复都更慢，而且要求磁盘上有足够空间保存休眠镜像。</p><p>还有两个组合模式：</p><ul><li><code>hybrid-sleep</code>：同时把状态写入磁盘并保持内存供电。正常唤醒时像睡眠一样快，彻底断电后仍然可以从磁盘恢复。</li><li><code>suspend-then-hibernate</code>：先睡眠，过一段时间后自动唤醒一次，再转入休眠。笔记本合盖后常用这个模式，短时间打开时恢复很快，长时间放包里又不至于耗光电。</li></ul><p>在 systemd 中，对应命令是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">suspend</span></span><br><span class="line"><span class="built_in">sudo</span> systemctl hibernate</span><br><span class="line"><span class="built_in">sudo</span> systemctl hybrid-sleep</span><br><span class="line"><span class="built_in">sudo</span> systemctl suspend-then-hibernate</span><br></pre></td></tr></table></figure><h3 id="ACPI-的-S0-到-S5">ACPI 的 S0 到 S5</h3><p>很多文章会把 Linux 睡眠问题和 ACPI S 状态混在一起说。它们有关系，但不是一回事。ACPI S 状态大致描述的是整机电源状态：</p><table><thead><tr><th>状态</th><th>常见含义</th><th>说明</th></tr></thead><tbody><tr><td>S0</td><td>Working</td><td>正常运行状态</td></tr><tr><td>S0ix</td><td>Modern Standby / Low Power Idle</td><td>机器仍处在 S0 框架内，但尽量让 CPU 和设备进入很深的空闲状态</td></tr><tr><td>S1</td><td>Standby</td><td>CPU 停止执行，内存保留，平台仍保留较多供电</td></tr><tr><td>S2</td><td>更深的 Standby</td><td>比 S1 更深，CPU 上下文通常不保留，实际机器上很少见</td></tr><tr><td>S3</td><td>Suspend to RAM</td><td>传统睡眠，内存自刷新，大多数设备断电或进入低功耗</td></tr><tr><td>S4</td><td>Suspend to Disk / Hibernate</td><td>内存镜像写入磁盘，也就是休眠</td></tr><tr><td>S5</td><td>Soft Off</td><td>软关机</td></tr></tbody></table><p>现在经常看到的争论是 S0ix 和 S3：很多新笔记本更偏向 Windows Modern Standby，也就是 S0ix 这一套；传统 Linux 用户熟悉的“睡眠”则常常指 S3 Suspend to RAM。</p><h3 id="Linux-里看到的是-s2idle、shallow、deep">Linux 里看到的是 s2idle、shallow、deep</h3><p>Linux 内核在 <code>/sys/power/mem_sleep</code> 里暴露的是另一组名字：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /sys/power/mem_sleep</span><br></pre></td></tr></table></figure><p>可能看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[s2idle] deep</span><br></pre></td></tr></table></figure><p>方括号表示当前默认值。上面的输出表示机器支持 <code>s2idle</code> 和 <code>deep</code>，当前默认使用 <code>s2idle</code>。</p><p>几个名字的含义如下：</p><table><thead><tr><th>Linux 名称</th><th>大致对应</th><th>说明</th></tr></thead><tbody><tr><td><code>s2idle</code></td><td>suspend-to-idle / S0ix 路线</td><td>纯软件主导的浅睡眠。内核冻结用户态、暂停计时、挂起设备，然后让 CPU 进入深度 idle。是否省电非常依赖设备和驱动是否配合</td></tr><tr><td><code>shallow</code></td><td>standby</td><td>对应较浅的平台待机状态，常见机器上不一定出现</td></tr><tr><td><code>deep</code></td><td>suspend-to-RAM / S3</td><td>传统 S3 睡眠。内存保留，大量设备和平台逻辑下电，通常更省电，但更依赖 BIOS/UEFI 和 ACPI 实现质量</td></tr></tbody></table><div class="note warning flat"><p><code>s2idle</code> 里的 <code>s2</code> 不是 ACPI S2。它是 suspend-to-idle 的缩写，和 ACPI 表里的 S2 状态不是同一个概念。Linux 里通常也不会显示 <code>s3deep</code>，而是显示 <code>deep</code>。</p></div><h3 id="为什么会“睡死”">为什么会“睡死”</h3><p>所谓“睡死”，通常指机器合盖或手动睡眠后无法正常唤醒：黑屏、键盘灯不亮、风扇异常、只能长按电源键重启。它不是一个单独的错误类型，而是睡眠和唤醒链路里某一段没有配合好。</p><p>常见原因有几类：</p><ul><li>固件问题：有些机器的 BIOS/UEFI 主要按 Windows Modern Standby 测试，S3 路径没有被充分维护。Linux 强行使用 <code>deep</code> 时，平台可能进得去但回不来。</li><li>驱动问题：GPU、Wi-Fi、蓝牙、NVMe、USB 控制器等设备在 suspend/resume 时没有正确保存和恢复状态。</li><li>唤醒源配置异常：某些 USB、蓝牙、网卡或触摸板可能无法正确唤醒机器，或者反复把机器唤醒，表现出来也像“睡眠不正常”。</li><li>内核、固件、NVIDIA 驱动等版本组合问题：睡眠恢复是非常吃平台细节的路径，版本变化可能导致回归。</li></ul><p>所以没有一个对所有机器都正确的选择。经验上：</p><ul><li><code>s2idle</code> 唤醒通常更快，也更符合新平台设计，但可能耗电明显。</li><li><code>deep</code> 往往更省电，但如果固件 S3 实现有问题，更容易“睡死”。</li></ul><p>正确做法不是盲目迷信某一种，而是先看机器支持什么，再分别测试。</p><h2 id="实际配置">实际配置</h2><h3 id="查看和切换睡眠模式">查看和切换睡眠模式</h3><p>查看当前支持的 <code>mem_sleep</code> 模式：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /sys/power/mem_sleep</span><br></pre></td></tr></table></figure><p>输出示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[s2idle] deep</span><br></pre></td></tr></table></figure><p>含义是：当前默认是 <code>s2idle</code>，同时支持 <code>deep</code>。</p><p>可以临时切换后测试，重启后会恢复默认值：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> deep | <span class="built_in">sudo</span> <span class="built_in">tee</span> /sys/power/mem_sleep</span><br><span class="line"><span class="built_in">echo</span> s2idle | <span class="built_in">sudo</span> <span class="built_in">tee</span> /sys/power/mem_sleep</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">suspend</span></span><br></pre></td></tr></table></figure><p>如果某个模式下出现无法唤醒、明显发热、掉电异常，就不要急着写成永久配置。</p><p>永久配置推荐写 systemd drop-in：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p /etc/systemd/sleep.conf.d</span><br><span class="line">sudoedit /etc/systemd/sleep.conf.d/90-sleep.conf</span><br></pre></td></tr></table></figure><p>如果确认 <code>deep</code> 在这台机器上稳定：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Sleep]</span></span><br><span class="line"><span class="attr">MemorySleepMode</span>=deep</span><br></pre></td></tr></table></figure><p>如果 <code>deep</code> 会睡死，或者机器本来就是按 Modern Standby 设计，可以用：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Sleep]</span></span><br><span class="line"><span class="attr">MemorySleepMode</span>=s2idle</span><br></pre></td></tr></table></figure><p>如果 <code>cat /sys/power/mem_sleep</code> 里根本没有 <code>deep</code>，那 systemd 配置也变不出来。需要去 BIOS/UEFI 里找类似选项：</p><ul><li><code>S3</code></li><li><code>Linux S3</code></li><li><code>Modern Standby</code></li><li><code>Sleep State</code></li></ul><p>有些机器的固件直接不再暴露 S3，这时 Linux 侧只能使用 <code>s2idle</code>。</p><h3 id="配置-Linux-支持休眠">配置 Linux 支持休眠</h3><p>休眠要解决两个问题：</p><ol><li>内存镜像写到哪里，通常是 <code>swap</code>。</li><li>下次开机早期，initramfs 怎么知道去哪里找这份镜像。</li></ol><p>先确认当前启用的 swap，并查看目标 swap 分区的 UUID：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">swapon --show</span><br><span class="line"><span class="built_in">sudo</span> blkid /dev/你的swap分区</span><br><span class="line"><span class="comment"># 例如：</span></span><br><span class="line"><span class="built_in">sudo</span> blkid /dev/nvme0n1p5</span><br></pre></td></tr></table></figure><p>输出里会有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">UUID=&quot;9e09ab44-748d-4905-8d09-fe92243ae6f5&quot; TYPE=&quot;swap&quot;</span><br></pre></td></tr></table></figure><p>把这个 UUID 写进 GRUB 内核参数：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudoedit /etc/default/grub</span><br></pre></td></tr></table></figure><p>在 <code>GRUB_CMDLINE_LINUX_DEFAULT</code> 里追加 <code>resume=UUID=...</code>，不要覆盖原来已有的参数：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">GRUB_CMDLINE_LINUX_DEFAULT</span>=<span class="string">&quot;quiet resume=UUID=9e09ab44-748d-4905-8d09-fe92243ae6f5&quot;</span></span><br></pre></td></tr></table></figure><p>然后重新生成 GRUB 配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> grub-mkconfig -o /boot/grub/grub.cfg</span><br></pre></td></tr></table></figure><p>然后检查 <code>mkinitcpio</code> 的 <code>HOOKS</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep <span class="string">&#x27;^HOOKS=&#x27;</span> /etc/mkinitcpio.conf</span><br></pre></td></tr></table></figure><p>如果 <code>HOOKS</code> 里已经有 <code>systemd</code>，通常不需要再单独加入 <code>resume</code> hook。例如：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">HOOKS</span>=(base systemd autodetect microcode modconf kms keyboard keymap sd-vconsole block filesystems fsck)</span><br></pre></td></tr></table></figure><p>如果走的是传统 busybox 路线，也就是 <code>HOOKS</code> 里没有 <code>systemd</code>，则需要加入 <code>resume</code>。位置应当在相关磁盘、解密、LVM 等 hook 之后，并且在 <code>filesystems</code> 之前。例如：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">HOOKS</span>=(base udev autodetect microcode modconf kms keyboard keymap consolefont block resume filesystems fsck)</span><br></pre></td></tr></table></figure><p>如果修改了 <code>mkinitcpio.conf</code>，记得重建 initramfs：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mkinitcpio -P</span><br></pre></td></tr></table></figure><div class="note info flat"><p>较新的 systemd 和 mkinitcpio 在 UEFI 环境下可以通过 EFI 变量记录休眠位置，某些情况下不再必须手写 <code>resume=</code>。但手动写 <code>resume=UUID=...</code> 更明确，也更方便排错和指定具体 swap 分区。</p></div><p>改完 GRUB 后必须重启。重启后检查当前内核参数，再单独测试休眠：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/cmdline</span><br><span class="line"><span class="built_in">sudo</span> systemctl hibernate</span><br></pre></td></tr></table></figure><p><code>/proc/cmdline</code> 里应当能看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">resume=UUID=9e09ab44-748d-4905-8d09-fe92243ae6f5</span><br></pre></td></tr></table></figure><p><code>systemctl hibernate</code> 应当先测通。否则后面的“合盖后先睡眠再休眠”也很难稳定。</p><h3 id="配置先睡眠再休眠">配置先睡眠再休眠</h3><p><code>suspend-then-hibernate</code> 的行为由 <code>/etc/systemd/sleep.conf.d/*.conf</code> 控制。比如希望先睡眠，30 分钟后进入休眠：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudoedit /etc/systemd/sleep.conf.d/90-sleep.conf</span><br></pre></td></tr></table></figure><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Sleep]</span></span><br><span class="line"><span class="attr">MemorySleepMode</span>=deep</span><br><span class="line"><span class="attr">AllowSuspendThenHibernate</span>=<span class="literal">yes</span></span><br><span class="line"><span class="attr">HibernateDelaySec</span>=<span class="number">30</span>min</span><br></pre></td></tr></table></figure><p>如果插着电源时也希望到点后进入休眠，再加 <code>HibernateOnACPower=yes</code>。几个参数的分工是：</p><ul><li><code>MemorySleepMode=deep</code>：执行 suspend 阶段时使用哪种 <code>mem_sleep</code>。</li><li><code>AllowSuspendThenHibernate=yes</code>：允许使用 <code>suspend-then-hibernate</code>。</li><li><code>HibernateDelaySec=30min</code>：先睡眠多久，再自动转入休眠。</li><li><code>HibernateOnACPower=yes</code>：接通 AC 电源时也执行延时休眠。</li></ul><p>最后手动测试：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl suspend-then-hibernate</span><br></pre></td></tr></table></figure><h3 id="配置合盖动作">配置合盖动作</h3><p><code>sleep.conf.d</code> 只决定“睡眠动作怎么执行”，不决定“什么时候触发”。笔记本合盖由 <code>systemd-logind</code> 处理。</p><p>创建 drop-in 后，根据需要选择一个动作：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p /etc/systemd/logind.conf.d</span><br><span class="line">sudoedit /etc/systemd/logind.conf.d/90-lid.conf</span><br></pre></td></tr></table></figure><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Login]</span></span><br><span class="line"><span class="comment"># 合盖后普通睡眠</span></span><br><span class="line"><span class="attr">HandleLidSwitch</span>=suspend</span><br><span class="line"></span><br><span class="line"><span class="comment"># 合盖后直接休眠</span></span><br><span class="line"><span class="comment"># HandleLidSwitch=hibernate</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 合盖后先睡眠，过一段时间再休眠</span></span><br><span class="line"><span class="comment"># HandleLidSwitch=suspend-then-hibernate</span></span><br></pre></td></tr></table></figure><p>如果选择 <code>suspend-then-hibernate</code>，还需要和前面的 sleep 配置配合使用：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Sleep]</span></span><br><span class="line"><span class="attr">MemorySleepMode</span>=deep</span><br><span class="line"><span class="attr">AllowSuspendThenHibernate</span>=<span class="literal">yes</span></span><br><span class="line"><span class="attr">HibernateDelaySec</span>=<span class="number">30</span>min</span><br></pre></td></tr></table></figure><p>两者关系可以这样理解：</p><ul><li><code>/etc/systemd/logind.conf.d/90-lid.conf</code>：合盖时触发什么动作。</li><li><code>/etc/systemd/sleep.conf.d/90-sleep.conf</code>：这个动作具体怎么执行。</li></ul><p>修改后最省心的生效方式是重启。尤其是前面已经改过 GRUB，本来就需要重启。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/7e0b85a4.html</id>
    <link href="https://zhuxz0299.github.io/posts/7e0b85a4.html"/>
    <published>2026-05-08T12:12:53.000Z</published>
    <summary>整理 Linux 下睡眠、休眠、s2idle、S3/deep 的区别，并以 Arch Linux 为例配置休眠和合盖后先睡眠再休眠。</summary>
    <title>Linux 睡眠休眠设置</title>
    <updated>2026-05-08T13:05:22.956Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Programming Language" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Programming-Language/"/>
    <category term="python" scheme="https://zhuxz0299.github.io/tags/python/"/>
    <category term="nodejs" scheme="https://zhuxz0299.github.io/tags/nodejs/"/>
    <category term="csharp" scheme="https://zhuxz0299.github.io/tags/csharp/"/>
    <category term="java" scheme="https://zhuxz0299.github.io/tags/java/"/>
    <category term="rust" scheme="https://zhuxz0299.github.io/tags/rust/"/>
    <content>
      <![CDATA[<div class="note primary flat"><p>文章考虑当前机器的环境，记录一下各种语言的版本管理与包管理方法。</p></div><h2 id="Python">Python</h2><p>主要使用 <code>uv</code>，但是更古早的 <code>python -m venv .venv</code> 的写法依然被兼容。</p><h3 id="版本管理">版本管理</h3><p>在使用 <code>uv</code> 的项目里，项目级 Python 版本通常由 <code>.python-version</code> 和 <code>pyproject.toml</code> 共同表达。<code>.python-version</code> 给工具一个本地切换提示，<code>pyproject.toml</code> 里的 <code>requires-python</code> 则声明项目需要的 Python 版本范围。</p><p>常用版本管理的命令为：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">uv python install 3.11</span><br><span class="line">uv python pin 3.11</span><br><span class="line">uv run python --version</span><br></pre></td></tr></table></figure><p><code>uv python pin 3.11</code> 会在项目目录写入 <code>.python-version</code>。之后在项目里执行 <code>uv run</code>、<code>uv sync</code> 时，<code>uv</code> 会按项目配置选择合适的 Python 解释器并维护虚拟环境。</p><h3 id="包管理">包管理</h3><p><code>uv</code> 项目的依赖声明放在 <code>pyproject.toml</code>，完整解析结果放在 <code>uv.lock</code>。常用命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">uv <span class="built_in">sync</span></span><br><span class="line">uv add pandas</span><br><span class="line">uv remove pandas</span><br><span class="line">uv run python main.py</span><br></pre></td></tr></table></figure><p><code>uv sync</code> 根据 <code>pyproject.toml</code> 和 <code>uv.lock</code> 创建或更新环境；<code>uv add</code> 修改依赖声明并更新锁文件；<code>uv run</code> 在项目环境里运行命令。<code>.venv/</code> 是被创建的本地虚拟环境目录。</p><p>也有一些项目只使用 <code>requirements.txt</code>。这种方式更轻量，通常配合 <code>pip</code> 使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">python -m venv .venv</span><br><span class="line"><span class="built_in">source</span> .venv/bin/activate</span><br><span class="line">python -m pip install -r requirements.txt</span><br></pre></td></tr></table></figure><h3 id="配置例子">配置例子</h3><p>以 <code>~/Data/git-remote/satellite-computing/COMET</code> 为例，其有如下文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">.python-version</span><br><span class="line">pyproject.toml</span><br><span class="line">uv.lock</span><br></pre></td></tr></table></figure><ul><li>其中 <code>.python-version</code> 内容为 <code>3.11</code></li><li><code>pyproject.toml</code> 里声明项目需要的 Python 版本和直接依赖：<figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[project]</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;constellation-simulator-with-input&quot;</span></span><br><span class="line"><span class="attr">version</span> = <span class="string">&quot;0.1.0&quot;</span></span><br><span class="line"><span class="attr">requires-python</span> = <span class="string">&quot;&gt;=3.11&quot;</span></span><br><span class="line"><span class="attr">dependencies</span> = [</span><br><span class="line">    <span class="string">&quot;matplotlib&gt;=3.10.8&quot;</span>,</span><br><span class="line">    <span class="string">&quot;numpy==2.3.5&quot;</span>,</span><br><span class="line">    <span class="string">&quot;pandas&gt;=2.3.3&quot;</span>,</span><br><span class="line">    <span class="string">&quot;pyyaml==6.0.3&quot;</span>,</span><br><span class="line">    <span class="string">&quot;scipy&gt;=1.16.3&quot;</span>,</span><br><span class="line">    <span class="string">&quot;tqdm==4.67.1&quot;</span>,</span><br><span class="line">    <span class="string">&quot;gurobipy==12.0.1&quot;</span>,</span><br><span class="line">]</span><br></pre></td></tr></table></figure></li></ul><h2 id="Node-js">Node.js</h2><p>当前博客仓库就是用了 Node.js。</p><h3 id="版本管理-2">版本管理</h3><p>当前 Node.js 版本切换主要由 <code>fnm</code> 完成：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">fnm list</span><br><span class="line">fnm install 24</span><br><span class="line">fnm use 24</span><br><span class="line">fnm default 24</span><br></pre></td></tr></table></figure><p>如果项目里有 <code>.node-version</code> 或 <code>.nvmrc</code>，进入项目后 <code>~/.zshrc</code> 中的 <code>eval &quot;$(fnm env --use-on-cd)&quot;</code> 会自动执行版本切换。如果本机没有需要的版本，会申请下载。没有对 <code>~/.zshrc</code> 进行配置的话，也可以进入项目后执行 <code>fnm use</code>。</p><p>如果项目里没有 <code>.node-version</code> 或 <code>.nvmrc</code>，那就使用当前 shell 默认的 Node.js 版本。</p><h3 id="包管理-2">包管理</h3><h4 id="npm">npm</h4><p>当前博客仓库使用 npm：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install</span><br></pre></td></tr></table></figure><p>严格按锁文件恢复依赖时可以用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm ci</span><br></pre></td></tr></table></figure><p><code>package.json</code> 声明脚本、项目元数据和直接依赖；<code>package-lock.json</code> 锁定 npm 解析出来的完整依赖树；<code>node_modules/</code> 是本地安装的包目录。</p><h4 id="pnpm-Yarn-Corepack">pnpm / Yarn / Corepack</h4><p>npm、pnpm、Yarn 都共用 <code>package.json</code>。也就是说，<code>dependencies</code>、<code>devDependencies</code>、<code>scripts</code>、<code>engines</code> 这些依赖和脚本声明都写在同一个文件里；不同包管理器的主要区别在锁文件和额外配置：</p><table><thead><tr><th>包管理器</th><th>常见锁文件</th><th>常见额外配置</th></tr></thead><tbody><tr><td>npm</td><td><code>package-lock.json</code></td><td><code>.npmrc</code></td></tr><tr><td>pnpm</td><td><code>pnpm-lock.yaml</code></td><td><code>.npmrc</code>、<code>package.json</code> 里的 <code>pnpm</code> 字段</td></tr><tr><td>Yarn</td><td><code>yarn.lock</code></td><td><code>.yarnrc.yml</code></td></tr></tbody></table><p>pnpm / Yarn 的版本推荐通过 <a href="https://github.com/nodejs/corepack">Corepack</a> 管理使用。</p><p>常用命令大概是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">corepack <span class="built_in">enable</span></span><br><span class="line">corepack use pnpm@latest</span><br><span class="line">corepack use yarn@stable</span><br></pre></td></tr></table></figure><p><code>corepack enable</code> 把 pnpm / Yarn 的 shim 放到当前 Node.js 安装旁边，不执行切换版本工作。版本选择发生在后面执行 <code>pnpm install</code>、<code>pnpm dev</code>、<code>yarn install</code> 这类命令时：Corepack 会向上查找最近的 <code>package.json</code>，读取其中的 <code>packageManager</code> 字段，使用对应版本。</p><p><code>corepack use ...</code> 会把选择写入 <code>package.json</code> 的 <code>packageManager</code> 字段并执行一次安装。例如：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;packageManager&quot;</span><span class="punctuation">:</span> <span class="string">&quot;pnpm@10.11.0&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>之后项目通常还会对应出现 <code>pnpm-lock.yaml</code> 或 <code>yarn.lock</code>。所以判断一个 Node.js 项目实际使用哪套包管理器，最可靠的线索是组合看锁文件和 <code>package.json</code> 里的 <code>packageManager</code>。</p><div class="note warning flat"><p>需要注意，Corepack 曾经随 Node.js 14.19.0 到 24.x 分发；从 <a href="https://nodejs.org/download/release/latest/docs/api/corepack.html">Node.js 25</a> 开始不再随 Node.js 一起分发，需要通过 <code>npm install -g corepack</code> 或系统包管理器另装。</p></div><h3 id="配置例子-2">配置例子</h3><h4 id="例子1">例子1</h4><p>博客仓库的 <code>package.json</code> 主要包含脚本和 Hexo 依赖：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;hexo-site&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;private&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;hexo generate&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;server&quot;</span><span class="punctuation">:</span> <span class="string">&quot;hexo server&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;dev&quot;</span><span class="punctuation">:</span> <span class="string">&quot;hexo clean &amp;&amp; hexo generate &amp;&amp; hexo server&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;dependencies&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;hexo&quot;</span><span class="punctuation">:</span> <span class="string">&quot;^8.0.0&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;hexo-theme-butterfly&quot;</span><span class="punctuation">:</span> <span class="string">&quot;5.5.2&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;katex&quot;</span><span class="punctuation">:</span> <span class="string">&quot;^0.16.28&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这个仓库有 <code>package-lock.json</code>，所以它当前是 npm 锁文件路线。</p><h4 id="例子2">例子2</h4><p><code>/home/zxz/Data/git-remote/nimo/oscope/</code> 就是一个 pnpm 项目。它有 <code>pnpm-lock.yaml</code>，并且 <code>package.json</code> 里写了：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;packageManager&quot;</span><span class="punctuation">:</span> <span class="string">&quot;pnpm@10.28.1&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;engines&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;node&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&gt;= 22.22.0&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;npm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&gt;= 10.0.0&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;pnpm&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&gt;= 9&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这里的 <code>packageManager: &quot;pnpm@10.28.1&quot;</code> 是给 Corepack 用的精确版本声明；<code>engines.pnpm: &quot;&gt;= 9&quot;</code> 应该是最低兼容范围或提示。进入项目后，只要执行过 <code>corepack enable</code>，再运行 <code>pnpm install</code>，Corepack 就会根据 <code>packageManager</code> 使用 <code>pnpm@10.28.1</code>。</p><h2 id="C-NET">C# / .NET</h2><h3 id="版本管理-3">版本管理</h3><p>常用版本检查命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">dotnet --version</span><br><span class="line">dotnet --list-sdks</span><br><span class="line">dotnet --list-runtimes</span><br></pre></td></tr></table></figure><p>如果同一台机器装了多个 SDK，项目可以用 <code>global.json</code> 固定构建时使用的 SDK：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dotnet new globaljson --sdk-version 8.0.125</span><br></pre></td></tr></table></figure><p>如果没有 <code>global.json</code>，实际构建就会按 .NET CLI 的 SDK 选择规则使用当前 <code>dotnet</code> 能解析到的 SDK。</p><p>.NET 的版本管理至少要分清三件事：</p><table><thead><tr><th>概念</th><th>作用</th><th>当前例子</th></tr></thead><tbody><tr><td>SDK</td><td>决定执行 <code>dotnet build</code>、<code>dotnet restore</code>、<code>dotnet run</code> 时使用哪套 CLI/MSBuild/编译工具链</td><td><code>8.0.125</code></td></tr><tr><td>TargetFramework</td><td>写在 <code>.csproj</code> 里，决定项目面向哪个目标框架，以及编译时能使用哪些 API</td><td><code>net8.0</code>、<code>net6.0</code></td></tr><tr><td>Runtime</td><td>程序真正启动后使用的运行时实现</td><td><code>Microsoft.NETCore.App 8.0.25</code></td></tr></tbody></table><p>所以 <a href="https://learn.microsoft.com/en-us/dotnet/core/tools/global-json"><code>global.json</code></a> 用来选择 .NET SDK；<a href="https://learn.microsoft.com/en-us/dotnet/standard/frameworks"><code>TargetFramework</code></a> 写在项目文件中，用来指定目标框架；<a href="https://learn.microsoft.com/en-us/dotnet/standard/assembly/reference-assemblies">reference assemblies</a> / targeting pack 提供编译时可见的 API 接口，但它们本身不能被当作运行时实现加载。</p><p>例如某解决方案底下有三个 <code>.csproj</code>，其中 <code>TargetFramework</code> 既有 <code>net8.0</code>，也有 <code>net6.0</code>，这里并非指定 SDK 版本，而是规定了每个项目的目标框架。</p><p>在 Arch Linux 上，大致对应关系是：</p><table><thead><tr><th>需求</th><th>常见包</th></tr></thead><tbody><tr><td>构建 .NET 8 项目</td><td><code>dotnet-sdk-8.0</code></td></tr><tr><td>编译面向 <code>net8.0</code>、<code>net6.0</code> 的普通项目</td><td><code>dotnet-targeting-pack-8.0</code>、<code>dotnet-targeting-pack-6.0</code></td></tr><tr><td>编译 <a href="http://ASP.NET">ASP.NET</a> Core / Web 项目</td><td><code>aspnet-targeting-pack-8.0</code>、<code>aspnet-targeting-pack-6.0</code> 等</td></tr><tr><td>运行 framework-dependent 的 .NET 8 程序</td><td><code>dotnet-runtime-8.0</code></td></tr><tr><td>运行 <a href="http://ASP.NET">ASP.NET</a> Core 程序</td><td><code>aspnet-runtime-8.0</code>、<code>aspnet-runtime-6.0</code> 等</td></tr></tbody></table><p>NuGet 依赖会由 <code>dotnet restore</code> 自动恢复，但 SDK、Runtime、targeting pack 这类系统级组件不会被项目自动用 <code>pacman</code> 安装。遇到缺少目标框架或运行时的报错时，需要根据项目的 <code>global.json</code>、<code>TargetFramework</code> 和项目类型手动安装对应包。</p><div class="note default flat"><p>从这里可以看出，.NET 提供了 SDK 选择机制，但是没有像其他语言一样提供不同版本 SDK / Runtime / targeting pack 的安装器。Windows 中，这部分工作应该是默认交给 Visual Studio 了。</p></div><h3 id="包管理-3">包管理</h3><p>.NET 包管理走 NuGet，依赖通常写在 <code>.csproj</code> 的 <code>PackageReference</code> 里。常用命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">dotnet restore</span><br><span class="line">dotnet add package YamlDotNet</span><br><span class="line">dotnet list package</span><br><span class="line">dotnet remove package YamlDotNet</span><br><span class="line">dotnet build</span><br></pre></td></tr></table></figure><p>当解决方案里有多个项目时，<code>.sln</code> 负责组织项目；<code>.csproj</code> 负责声明具体项目的目标框架、NuGet 包依赖和项目引用。</p><h3 id="配置例子-3">配置例子</h3><p>可以参考的 .NET 项目是 <code>/home/zxz/Data/git-remote/TboxWebdav</code>，里面有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">TboxWebdav.sln</span><br><span class="line">TboxWebdav.Server.AspNetCore/TboxWebdav.Server.AspNetCore.csproj</span><br><span class="line">TboxWebdav.Server/TboxWebdav.Server.csproj</span><br><span class="line">WebStreamCaching/WebStreamCaching.csproj</span><br></pre></td></tr></table></figure><p>例如 <code>TboxWebdav.Server.AspNetCore.csproj</code>：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">Project</span> <span class="attr">Sdk</span>=<span class="string">&quot;Microsoft.NET.Sdk.Web&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">PropertyGroup</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">TargetFramework</span>&gt;</span>net8.0<span class="tag">&lt;/<span class="name">TargetFramework</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">Nullable</span>&gt;</span>enable<span class="tag">&lt;/<span class="name">Nullable</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">ImplicitUsings</span>&gt;</span>enable<span class="tag">&lt;/<span class="name">ImplicitUsings</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">PropertyGroup</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">ItemGroup</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">PackageReference</span> <span class="attr">Include</span>=<span class="string">&quot;System.CommandLine&quot;</span> <span class="attr">Version</span>=<span class="string">&quot;2.0.0-beta4.22272.1&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">PackageReference</span> <span class="attr">Include</span>=<span class="string">&quot;YamlDotNet&quot;</span> <span class="attr">Version</span>=<span class="string">&quot;16.3.0&quot;</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">ItemGroup</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">ItemGroup</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">ProjectReference</span> <span class="attr">Include</span>=<span class="string">&quot;..\TboxWebdav.Server\TboxWebdav.Server.csproj&quot;</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">ItemGroup</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">Project</span>&gt;</span></span><br></pre></td></tr></table></figure><p>这里有三类信息：</p><table><thead><tr><th>配置</th><th>作用</th></tr></thead><tbody><tr><td><code>TargetFramework</code></td><td>这个项目面向哪个 .NET 目标框架，例如 <code>net8.0</code></td></tr><tr><td><code>PackageReference</code></td><td>NuGet 包依赖</td></tr><tr><td><code>ProjectReference</code></td><td>项目引用，例如这里就会引用 <code>TboxWebdav.Server.csproj</code> 构建出来的 <code>.dll</code> 文件</td></tr></tbody></table><p>这个项目里没有看到 <code>Directory.Packages.props</code> 或 <code>packages.lock.json</code>，所以这里只记录它们的用途，不写具体配置：</p><table><thead><tr><th>文件</th><th>作用</th></tr></thead><tbody><tr><td><code>Directory.Packages.props</code></td><td>可以集中管理多个项目的 NuGet 包版本</td></tr><tr><td><code>packages.lock.json</code></td><td>可以锁定 NuGet restore 结果</td></tr></tbody></table><h2 id="Java">Java</h2><h3 id="版本管理-4">版本管理</h3><p>当前机器的 Java 版本管理目前是 Arch Linux 的系统级方案：通过系统包安装 JDK，再用 <code>archlinux-java</code> 查看或切换默认 Java 环境。</p><p>常用检查和切换命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">java -version</span><br><span class="line">javac -version</span><br><span class="line">archlinux-java status</span><br><span class="line"><span class="built_in">sudo</span> archlinux-java <span class="built_in">set</span> java-21-openjdk</span><br></pre></td></tr></table></figure><h3 id="包管理-4">包管理</h3><p>Java 项目的包管理通常由 Maven 或 Gradle 承担。Maven 的项目入口一般是 <code>pom.xml</code>，Gradle 的项目入口一般是 <code>build.gradle</code> 或 <code>build.gradle.kts</code>。</p><table><thead><tr><th>文件/工具</th><th>作用</th></tr></thead><tbody><tr><td><code>pom.xml</code></td><td>Maven 项目配置和依赖声明</td></tr><tr><td><code>build.gradle(.kts)</code></td><td>Gradle 项目配置和依赖声明</td></tr><tr><td>Gradle Wrapper</td><td>项目自带的 Gradle 版本入口</td></tr></tbody></table><h2 id="Rust">Rust</h2><h3 id="版本管理-5">版本管理</h3><p>Rust 的工具链比较完整：<code>rustup</code> 管工具链，<code>cargo</code> 管项目和依赖。</p><p>常用检查和切换命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">rustup show</span><br><span class="line">rustup show active-toolchain</span><br><span class="line">rustup default stable</span><br><span class="line">rustup toolchain install nightly</span><br><span class="line">rustup override <span class="built_in">set</span> nightly</span><br></pre></td></tr></table></figure><p><code>rustup default</code> 设置全局默认工具链；<code>rustup override set</code> 可以在某个目录下设置局部工具链。当前参考项目里没有 <code>rust-toolchain.toml</code>，所以更接近“默认 stable 工具链 + 项目内 <code>rust-version</code> 提示”的用法。</p><h3 id="包管理-5">包管理</h3><p>Rust 项目的包管理由 Cargo 负责。常用命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">cargo build</span><br><span class="line">cargo run</span><br><span class="line">cargo <span class="built_in">test</span></span><br><span class="line">cargo <span class="built_in">fmt</span></span><br><span class="line">cargo clippy</span><br></pre></td></tr></table></figure><p><code>Cargo.toml</code> 声明项目元数据、workspace 和直接依赖；<code>Cargo.lock</code> 锁定完整依赖树。应用项目通常应该提交 <code>Cargo.lock</code>，<code>target/</code> 是构建产物，不应该提交。</p><h3 id="配置例子-4">配置例子</h3><p>项目 <code>/home/zxz/Data/git-remote/security-scan/scan</code> 里有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Cargo.toml</span><br><span class="line">Cargo.lock</span><br><span class="line">migration/Cargo.toml</span><br></pre></td></tr></table></figure><p>主 <code>Cargo.toml</code>：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[package]</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;scan&quot;</span></span><br><span class="line"><span class="attr">version</span> = <span class="string">&quot;0.1.0&quot;</span></span><br><span class="line"><span class="attr">edition</span> = <span class="string">&quot;2024&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[workspace]</span></span><br><span class="line"><span class="attr">members</span> = [<span class="string">&quot;.&quot;</span>, <span class="string">&quot;migration&quot;</span>]</span><br><span class="line"></span><br><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">anyhow</span> = <span class="string">&quot;1.0.102&quot;</span></span><br><span class="line"><span class="attr">clap</span> = &#123; version = <span class="string">&quot;4.6.0&quot;</span>, features = [<span class="string">&quot;derive&quot;</span>] &#125;</span><br><span class="line"><span class="attr">reqwest</span> = &#123; version = <span class="string">&quot;0.13.2&quot;</span>, features = [<span class="string">&quot;cookies&quot;</span>, <span class="string">&quot;form&quot;</span>] &#125;</span><br><span class="line"><span class="attr">serde</span> = <span class="string">&quot;1.0.228&quot;</span></span><br><span class="line"><span class="attr">tokio</span> = &#123; version = <span class="string">&quot;1.50.0&quot;</span>, features = [</span><br><span class="line">  <span class="string">&quot;macros&quot;</span>,</span><br><span class="line">  <span class="string">&quot;process&quot;</span>,</span><br><span class="line">  <span class="string">&quot;rt&quot;</span>,</span><br><span class="line">  <span class="string">&quot;rt-multi-thread&quot;</span></span><br><span class="line">] &#125;</span><br></pre></td></tr></table></figure><p><code>migration/Cargo.toml</code> 里还写了最低 Rust 版本：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[package]</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;migration&quot;</span></span><br><span class="line"><span class="attr">edition</span> = <span class="string">&quot;2024&quot;</span></span><br><span class="line"><span class="attr">rust-version</span> = <span class="string">&quot;1.85.0&quot;</span></span><br><span class="line"><span class="attr">publish</span> = <span class="literal">false</span></span><br></pre></td></tr></table></figure><p>这里的分工是：</p><table><thead><tr><th>文件/字段</th><th>作用</th></tr></thead><tbody><tr><td><code>Cargo.toml</code></td><td>项目元数据、workspace、直接依赖</td></tr><tr><td><code>edition</code></td><td>Rust 语言 edition，例如 <code>2024</code></td></tr><tr><td><code>rust-version</code></td><td>声明 crate 需要的最低 Rust 版本</td></tr><tr><td><code>Cargo.lock</code></td><td>锁定完整依赖树</td></tr><tr><td><code>rustup</code></td><td>管理本地 Rust 工具链</td></tr></tbody></table>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/97f29cc2.html</id>
    <link href="https://zhuxz0299.github.io/posts/97f29cc2.html"/>
    <published>2026-05-08T05:33:36.000Z</published>
    <summary>以当前工作机为样本，梳理 Python、Node.js、C#/.NET、Java、Rust、C/C++ 的版本管理与包管理方式</summary>
    <title>常见编程语言版本管理与包管理</title>
    <updated>2026-05-22T12:40:34.343Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="System Knowledge" scheme="https://zhuxz0299.github.io/categories/System-Hardware/System-Knowledge/"/>
    <category term="linux" scheme="https://zhuxz0299.github.io/tags/linux/"/>
    <category term="esp" scheme="https://zhuxz0299.github.io/tags/esp/"/>
    <category term="file-system" scheme="https://zhuxz0299.github.io/tags/file-system/"/>
    <content>
      <![CDATA[<div class="note info flat"><p>本文基于我当前这台 <strong>Win11 + Arch Linux</strong> 双系统电脑上的 <code>/boot</code> 与 <code>/boot/efi</code> 目录进行分析。<br>参考资料包括：</p><ul><li><a href="https://www.gnu.org/software/grub/manual/grub/grub.html">GNU GRUB Manual</a></li><li><a href="https://wiki.archlinux.org/title/Arch_boot_process">ArchWiki: Arch boot process</a></li><li><a href="https://wiki.archlinux.org/title/GRUB">ArchWiki: GRUB</a></li><li><a href="https://pykickstart.readthedocs.io/en/latest/kickstart-docs.html">Pykickstart Documentation</a></li><li><a href="https://uefi.org/specs/UEFI/2.11/03_Boot_Manager.html">UEFI Specification 2.11: Boot Manager</a></li><li><a href="https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/bcdboot-command-line-options-techref-di?view=windows-11">Microsoft Learn: BCDBoot Command-Line Options</a></li><li><a href="https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/bcd-system-store-settings-for-uefi?view=windows-11">Microsoft Learn: BCD System Store Settings for UEFI</a></li><li><a href="https://github.com/rhboot/shim/blob/main/README.fallback">rhboot/shim: README.fallback</a></li></ul></div><h2 id="总体结构">总体结构</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> -lh /boot</span><br></pre></td></tr></table></figure><p>输出大致如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">total 86M</span><br><span class="line">drwxr-xr-x 5 nobody nobody 4.0K Jan  1  1970 efi</span><br><span class="line">drwxr-xr-x 1 nobody nobody  112 May  1 21:23 grub</span><br><span class="line">-rw------- 1 nobody nobody  20M May  3 12:22 initramfs-linux-lts.img</span><br><span class="line">-rw------- 1 nobody nobody  21M May  3 12:22 initramfs-linux.img</span><br><span class="line">-rw-r--r-- 1 nobody nobody  15M Feb 28 01:22 intel-ucode.img</span><br><span class="line">-rw-r--r-- 1 nobody nobody  17M May  3 12:22 vmlinuz-linux</span><br><span class="line">-rw-r--r-- 1 nobody nobody  16M May  3 12:22 vmlinuz-linux-lts</span><br></pre></td></tr></table></figure><p>这里主要有三类内容：</p><ul><li><code>/boot</code> 自身存放 Linux 启动早期需要的文件，例如内核、initramfs、CPU 微码等。</li><li><code>/boot/grub</code> 存放 GRUB 的配置、模块、字体、主题以及和启动菜单有关的文件。</li><li><code>/boot/efi</code> 存放 UEFI 启动相关文件，例如 GRUB、Windows Boot Manager 等 <code>.efi</code> 启动程序。</li></ul><h3 id="boot-与-boot-efi-的挂载问题"><code>/boot</code> 与 <code>/boot/efi</code> 的挂载问题</h3><p><code>/boot/efi</code> 与 <code>/boot</code> 可能挂载到不同的地方：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">findmnt -T /boot</span><br><span class="line">findmnt -T /boot/efi</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">TARGET SOURCE             FSTYPE OPTIONS</span><br><span class="line">/      /dev/nvme0n1p6[/@] btrfs  ro,nosuid,nodev,relatime,ssd,discard=async,space_cache=v2,subvolid=256,subvol=/@</span><br><span class="line"></span><br><span class="line">TARGET    SOURCE         FSTYPE OPTIONS</span><br><span class="line">/boot/efi /dev/nvme0n1p1 vfat   ro,nosuid,nodev,relatime,fmask=0022,dmask=0022,codepage=437,iocharset=ascii,shortname=mixed,utf8,errors=remount-ro</span><br></pre></td></tr></table></figure><p>也就是说，当前机器的 <code>/boot</code> 并不是一个单独的分区，而是位于 Linux 根文件系统所在的 Btrfs 子卷中；<code>/boot/efi</code> 才是单独挂载的 vfat 分区。</p><p>对于 <code>/boot</code> 与 EFI 分区的关系，常见情况大致有下面几种：</p><ul><li>像当前这台机器一样，<code>/boot</code> 跟随根文件系统一起挂载，位于 Btrfs 子卷 <code>@</code> 中；<code>/boot/efi</code> 单独挂载到 EFI 系统分区。</li><li>某些 Arch Linux 安装教程会建议把整个 <code>/boot</code> 都放到 EFI 分区里。这样内核、initramfs、GRUB 入口等都会位于同一个 FAT 格式的 ESP 中，路径上看起来会更集中。</li><li>Fedora/Anaconda 这类安装器也存在把启动相关分区单独处理的操作：<code>/boot</code> 可以是一个标准 Linux 分区，而 <code>/boot/efi</code> 依然挂载到 EFI 系统分区。 信息来源：<a href="https://pykickstart.readthedocs.io/en/latest/kickstart-docs.html">Pykickstart 文档</a>。</li></ul><div class="note default flat"><p><code>/boot</code> 和 <code>/boot/efi</code> 服务的对象并不一样：<code>/boot/efi</code> 主要服务于 UEFI 固件，<code>/boot</code> 中的内核、initramfs、GRUB 配置则主要服务于 Linux 启动流程。</p></div><h2 id="boot-文件夹"><code>/boot</code> 文件夹</h2><h3 id="boot-vmlinuz-linux-与-boot-vmlinuz-linux-lts"><code>/boot/vmlinuz-linux</code> 与 <code>/boot/vmlinuz-linux-lts</code></h3><p>这两个文件都是 Linux 内核镜像，只是对应的内核版本不同：</p><p>通过 <code>file</code> 命令可以看到：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">file /boot/vmlinuz-linux /boot/vmlinuz-linux-lts</span><br></pre></td></tr></table></figure><p>输出中比较关键的部分是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">/boot/vmlinuz-linux:     Linux kernel x86 boot executable, bzImage, version 7.0.3-arch1-2</span><br><span class="line">/boot/vmlinuz-linux-lts: Linux kernel x86 boot executable, bzImage, version 6.18.26-2-lts</span><br></pre></td></tr></table></figure><p>也就是说，二者本质上都是可被引导加载程序加载的 Linux 内核镜像。其中 <code>vmlinuz-linux</code> 是 Arch Linux 的普通内核，<code>vmlinuz-linux-lts</code> 是 LTS 内核。</p><p>当前系统实际运行的是普通内核：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">uname</span> -a</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Linux archll 7.0.3-arch1-2 #1 SMP PREEMPT_DYNAMIC Fri, 01 May 2026 15:49:22 +0000 x86_64 GNU/Linux</span><br></pre></td></tr></table></figure><p>LTS 内核可以在普通内核更新出现问题的时候作为备用选项。</p><h3 id="boot-initramfs-linux-img-与-boot-initramfs-linux-lts-img"><code>/boot/initramfs-linux.img</code> 与 <code>/boot/initramfs-linux-lts.img</code></h3><p>这两个文件是对应内核使用的 initramfs：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">vmlinuz-linux      &lt;-&gt; initramfs-linux.img</span><br><span class="line">vmlinuz-linux-lts  &lt;-&gt; initramfs-linux-lts.img</span><br></pre></td></tr></table></figure><p>initramfs 可以理解为“内核刚启动时使用的临时根文件系统”。</p><blockquote><p>来源：<a href="https://wiki.archlinux.org/title/Arch_boot_process#initramfs">Arch boot process - ArchWiki</a></p><p>An initramfs (initial RAM file system) image is a cpio archive providing the necessary files for early userspace to successfully start the late userspace.</p></blockquote><p>也就是说，initramfs 本质上是一个 cpio 归档，它为“早期用户空间”提供必要文件。内核先进入这个临时环境，完成一些真正进入系统前必须做的事情，然后再切换到正常启动后的用户空间。</p><p>为什么需要这个临时小系统？因为内核刚被加载时，还不一定能直接挂载真正的根文件系统。</p><p>比如当前机器的根文件系统位于 Btrfs 子卷 <code>@</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">root=UUID=dccef9f1-ac99-48c0-aca6-9c6d96bdef9c</span><br><span class="line">rootflags=subvol=@</span><br></pre></td></tr></table></figure><p>内核需要先准备好块设备、文件系统、udev 规则、可能的休眠恢复逻辑等，才能顺利找到并挂载真正的 <code>/</code>。</p><blockquote><p>来源：<a href="https://wiki.archlinux.org/title/Arch_boot_process#initramfs">Arch boot process - ArchWiki</a></p><p>With the concept of initramfs it is possible to handle even more complex setups, like e.g. booting from an external drive, stacked devices (logical volumes, software RAIDs, compression, encryption) or running a tiny SSH server in early userspace for remote unlocking or maintenance tasks of the root file system.</p></blockquote><p>也就是说，寻找根文件系统这件事可能很复杂：根分区可能位于外接硬盘、LVM、软件 RAID、加密设备、压缩文件系统等环境中。把这些逻辑放进 initramfs，就可以在真正的根文件系统挂载之前，先由早期用户空间处理这些准备工作。</p><div class="note default flat"><p>有些 Arch 机器的 <code>/boot</code> 里还会看到 <code>initramfs-linux-fallback.img</code>。它是更通用的备用 initramfs，通常会包含更多模块，所以体积可能比普通 <code>initramfs-linux.img</code> 大很多。它主要用于默认 initramfs 缺模块或生成异常时临时救急；但如果已经有 LTS 内核、Btrfs 快照，或者能用 Arch live 系统进去 <code>arch-chroot</code> 后重新执行 <code>mkinitcpio -P</code>，它的日常存在感就没那么强。当前这台机器没有 fallback 文件，是因为 <code>/etc/mkinitcpio.d/linux.preset</code> 里只启用了 <code>default</code> preset，没有启用 <code>fallback</code>。</p></div><h3 id="boot-intel-ucode-img"><code>/boot/intel-ucode.img</code></h3><p><code>intel-ucode.img</code> 是 Intel CPU 的 microcode 文件。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">file /boot/intel-ucode.img</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ASCII cpio archive (SVR4 with no CRC)</span><br></pre></td></tr></table></figure><p>CPU microcode 可以理解为 CPU 内部执行某些底层操作时使用的“微型固件”。厂商可以通过 microcode 更新修复处理器层面的问题，例如硬件 bug、安全漏洞或兼容性问题。</p><p>它通常会在普通 initramfs 之前被加载。如果是 AMD 平台，这里一般会看到 <code>amd-ucode.img</code>。</p><h2 id="boot-grub-文件夹"><code>/boot/grub/</code> 文件夹</h2><p><code>/boot/grub</code> 是 GRUB 的配置、模块、字体、主题等文件所在的位置。</p><p><a href="https://www.gnu.org/software/grub/manual/grub/grub.html">GNU GRUB 手册</a>对 GRUB 的定位是：它是一个引导加载程序，负责加载并把控制权交给操作系统内核。当前这个目录大致如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">/boot/grub</span><br><span class="line">├── fonts</span><br><span class="line">├── grub-btrfs.cfg</span><br><span class="line">├── grub.cfg</span><br><span class="line">├── grubenv</span><br><span class="line">├── locale</span><br><span class="line">├── themes</span><br><span class="line">└── x86_64-efi</span><br></pre></td></tr></table></figure><h3 id="grub-cfg"><code>grub.cfg</code></h3><p><code>grub.cfg</code> 是 GRUB 的主配置文件，里面记录启动菜单、内核路径、initramfs 路径、内核参数等。</p><p>在 Arch 系系统上，这个文件通常由 <code>grub-mkconfig</code> 生成，而不是手写。典型命令类似：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grub-mkconfig -o /boot/grub/grub.cfg</span><br></pre></td></tr></table></figure><p>这个文件会告诉 GRUB：菜单里有哪些系统或内核可选；选中某一项时，应该加载哪个 <code>vmlinuz-*</code> 文件、哪个 <code>initramfs-*</code> 文件，以及传给内核哪些参数。这个文件并不建议手动编辑，应当使用上面的命令生成。其内容来源为：</p><ul><li><code>/etc/default/grub</code>：控制 GRUB 的全局设置，例如默认启动项、菜单等待时间、内核参数、字体、是否启用 <code>os-prober</code> 等。</li><li><code>/etc/grub.d/</code>：一组生成菜单项的脚本。<code>grub-mkconfig</code> 会按编号顺序执行这些脚本，并把它们的输出拼成最终的 <code>grub.cfg</code>。</li></ul><p>当前这台机器的 <code>/etc/default/grub</code> 中比较关键的几项是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">GRUB_DEFAULT=0</span><br><span class="line">GRUB_TOP_LEVEL=<span class="string">&quot;/boot/vmlinuz-linux&quot;</span></span><br><span class="line">GRUB_TIMEOUT=5</span><br><span class="line">GRUB_CMDLINE_LINUX_DEFAULT=<span class="string">&quot;loglevel=3 quiet resume=UUID=9e09ab44-748d-4905-8d09-fe92243ae6f5&quot;</span></span><br><span class="line">GRUB_FONT=<span class="string">&quot;/boot/grub/fonts/NotoSansMono32.pf2&quot;</span></span><br><span class="line">GRUB_PRELOAD_MODULES=<span class="string">&quot;part_gpt part_msdos&quot;</span></span><br><span class="line">GRUB_TIMEOUT_STYLE=menu</span><br><span class="line">GRUB_GFXMODE=auto</span><br><span class="line">GRUB_GFXPAYLOAD_LINUX=keep</span><br><span class="line">GRUB_DISABLE_RECOVERY=<span class="literal">true</span></span><br><span class="line">GRUB_DISABLE_OS_PROBER=<span class="literal">false</span></span><br></pre></td></tr></table></figure><p>这些配置会反映到生成后的 <code>grub.cfg</code> 中。</p><h4 id="字体与屏幕显示">字体与屏幕显示</h4><p>GRUB 菜单字体为手动设置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GRUB_FONT=<span class="string">&quot;/boot/grub/fonts/NotoSansMono32.pf2&quot;</span></span><br></pre></td></tr></table></figure><p>因为之前的 GRUB 默认字体在高分辨率屏幕上可能显得太小，启动菜单看不清。这个配置对应到 <code>grub.cfg</code> 的内容为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">if loadfont /@/boot/grub/fonts/NotoSansMono32.pf2 ; then</span><br><span class="line">  set gfxmode=auto</span><br><span class="line">  load_video</span><br><span class="line">  insmod gfxterm</span><br><span class="line">fi</span><br></pre></td></tr></table></figure><p>和显示相关的还有：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">GRUB_GFXMODE=auto</span><br><span class="line">GRUB_GFXPAYLOAD_LINUX=keep</span><br></pre></td></tr></table></figure><p><code>GRUB_GFXMODE=auto</code> 表示 GRUB 菜单阶段的图形模式自动选择；<code>GRUB_GFXPAYLOAD_LINUX=keep</code> 表示进入 Linux 内核后尽量沿用 GRUB 阶段的图形模式。它们主要影响启动菜单和内核早期输出阶段的显示效果。</p><h4 id="顶层-Arch-Linux-启动项">顶层 Arch Linux 启动项</h4><p>进入 GRUB 选择系统的界面时，最上层的 <code>menuentry 'Arch Linux'</code> 来自 <code>/etc/grub.d/10_linux</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">menuentry &#x27;Arch Linux&#x27; &#123;</span><br><span class="line">        linux   /@/boot/vmlinuz-linux root=UUID=dccef9f1-ac99-48c0-aca6-9c6d96bdef9c rw rootflags=subvol=@  loglevel=3 quiet resume=UUID=9e09ab44-748d-4905-8d09-fe92243ae6f5</span><br><span class="line">        initrd  /@/boot/intel-ucode.img /@/boot/initramfs-linux.img</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这两行基本就是 Linux 启动项的核心：</p><ul><li><code>linux ...</code> 指定要加载的内核，也就是 <code>/boot/vmlinuz-linux</code>。后面跟着的参数来自 <code>GRUB_CMDLINE_LINUX_DEFAULT</code>。</li><li><code>initrd ...</code> 指定启动早期要加载的镜像。可以看出这里先加载 <code>intel-ucode.img</code>，再加载 <code>initramfs-linux.img</code>。</li></ul><p>这里之所以顶层菜单是普通内核，和 <code>/etc/default/grub</code> 中的这行配置有关：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GRUB_TOP_LEVEL=<span class="string">&quot;/boot/vmlinuz-linux&quot;</span></span><br></pre></td></tr></table></figure><p><a href="https://wiki.archlinux.org/title/GRUB#Setting_the_top-level_menu_entry">ArchWiki 的 GRUB 页面</a>提到，<code>grub-mkconfig</code> 默认会用 <code>sort -V</code> 对内核列表排序，并把排在最前面的内核作为 top-level entry。因为 <code>/boot/vmlinuz-linux-lts</code> 会排在 <code>/boot/vmlinuz-linux</code> 前面，导致默认会使用 LTS 内核。设置 <code>GRUB_TOP_LEVEL=&quot;/boot/vmlinuz-linux&quot;</code> 后，就可以明确指定普通内核作为最上层的 Arch 启动项。</p><h4 id="Advanced-options-for-Arch-Linux">Advanced options for Arch Linux</h4><p>第二个启动选项是一个子菜单，类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">submenu &#x27;Advanced options for Arch Linux&#x27; &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个目录里会同时放普通内核和 LTS 内核。普通内核启动项和顶层 <code>Arch Linux</code> 基本一致；另一个则是 LTS 内核：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">menuentry &#x27;Arch Linux, with Linux linux-lts&#x27; &#123;</span><br><span class="line">        linux   /@/boot/vmlinuz-linux-lts root=UUID=dccef9f1-ac99-48c0-aca6-9c6d96bdef9c rw rootflags=subvol=@  loglevel=3 quiet resume=UUID=9e09ab44-748d-4905-8d09-fe92243ae6f5</span><br><span class="line">        initrd  /@/boot/intel-ucode.img /@/boot/initramfs-linux-lts.img</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这就对应了前面说的普通内核与 LTS 内核两套文件。</p><h4 id="Windows-Boot-Manager">Windows Boot Manager</h4><p>因为 <code>/etc/default/grub</code> 中设置了：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GRUB_DISABLE_OS_PROBER=<span class="literal">false</span></span><br></pre></td></tr></table></figure><p>所以 <code>grub-mkconfig</code> 会通过 <code>/etc/grub.d/30_os-prober</code> 生成 Windows 启动项：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">menuentry &#x27;Windows Boot Manager (on /dev/nvme0n1p1)&#x27; &#123;</span><br><span class="line">        insmod part_gpt</span><br><span class="line">        insmod fat</span><br><span class="line">        search --no-floppy --fs-uuid --set=root 58A4-CF48</span><br><span class="line">        chainloader /EFI/Microsoft/Boot/bootmgfw.efi</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的 <code>chainloader</code> 表示 GRUB 不直接启动 Windows 内核，而是把控制权交给 Windows 自己的 Boot Manager，也就是 EFI 分区里的 <code>\EFI\Microsoft\Boot\bootmgfw.efi</code>。</p><h4 id="UEFI-Firmware-Settings">UEFI Firmware Settings</h4><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">menuentry &#x27;UEFI Firmware Settings&#x27; &#123;</span><br><span class="line">        fwsetup</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个入口可以从 GRUB 菜单直接进入主板 UEFI 设置界面。</p><div class="note default flat"><p>这里说的主板 UEFI 设置界面就是俗称的 BIOS。老机器的主板固件通常是 BIOS，而新机器的主板固件已经基本是 UEFI，大家依然习惯叫那个设置界面 BIOS 是出于习惯。</p></div><h4 id="Arch-Linux-snapshots">Arch Linux snapshots</h4><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">submenu &#x27;Arch Linux snapshots&#x27; &#123;</span><br><span class="line">    configfile &quot;$&#123;prefix&#125;/grub-btrfs.cfg&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个入口来自 <code>/etc/grub.d/41_snapshots-btrfs</code>，它会继续读取 <code>/boot/grub/grub-btrfs.cfg</code>，把 Btrfs 快照启动项挂到 GRUB 菜单里。</p><h4 id="GRUB-命令行">GRUB 命令行</h4><p>除了这些显式的 <code>menuentry</code> 和 <code>submenu</code>，GRUB 菜单本身还提供命令行环境。这个不是某个 Linux 或 Windows 启动项，而是 GRUB 自己提供的交互界面。在菜单界面按 <code>c</code> 通常可以进入 GRUB command-line，按 <code>e</code> 则可以临时编辑当前选中的启动项。</p><p>这个命令行可以手动执行 <code>ls</code>、<code>set</code>、<code>linux</code>、<code>initrd</code>、<code>boot</code>、<code>chainloader</code> 等 GRUB 命令，用于查看磁盘、手动指定内核/initramfs，或者在启动项配置损坏时临时把系统拉起来。它属于排错工具，日常正常启动一般不会用到。</p><h3 id="grub-btrfs-cfg"><code>grub-btrfs.cfg</code></h3><p><code>grub-btrfs.cfg</code> 就是前面 <code>Arch Linux snapshots</code> 子菜单实际读取的配置文件。它里面列出了 Timeshift/Btrfs 快照对应的启动项，并且每个快照下通常也会提供普通内核和 LTS 内核两种入口。</p><p>这些快照可以用于滚挂之后的系统回滚。</p><h3 id="grubenv"><code>grubenv</code></h3><p><code>grubenv</code> 是 GRUB 的环境块文件，用来在多次启动之间保存少量状态。</p><blockquote><p>来源：<a href="https://www.gnu.org/software/grub/manual/grub/html_node/Environment-block.html">The GRUB environment block - GNU GRUB Manual</a></p><p>The environment block is a preallocated 1024-byte file, which normally lives in <code>/boot/grub/grubenv</code>.</p><p>GRUB provides an “environment block” which can be used to save a small amount of state.</p></blockquote><p>这个文件平时基本不用手动关心。它主要服务于一些“记住状态”的功能，例如保存上次选择的启动项、设置下一次启动临时进入某个菜单项等。</p><h3 id="x86-64-efi"><code>x86_64-efi/</code></h3><p><code>x86_64-efi/</code> 目录中放的是 GRUB 在 x86_64 UEFI 平台下使用的模块。例如当前能看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">acpi.mod</span><br><span class="line">ahci.mod</span><br><span class="line">all_video.mod</span><br><span class="line">btrfs.mod</span><br><span class="line">boot.mod</span><br><span class="line">...</span><br></pre></td></tr></table></figure><blockquote><p>来源：<a href="https://www.gnu.org/software/grub/manual/grub/html_node/Modules.html">Modules - GNU GRUB Manual</a> 与 <a href="https://www.gnu.org/software/grub/manual/grub/html_node/insmod.html">insmod - GNU GRUB Manual</a></p><p>Modules can be loaded via the <code>insmod</code> command.</p><p>Insert the dynamic GRUB module called <code>module</code>.</p></blockquote><p>也就是说，<code>insmod</code> 就是 GRUB 用来加载动态模块的命令。这个命令和前面 <code>grub.cfg</code> 里看到的是同一件事：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">insmod part_gpt</span><br><span class="line">insmod btrfs</span><br><span class="line">insmod fat</span><br><span class="line">insmod gfxterm</span><br></pre></td></tr></table></figure><p>这些命令会从类似 <code>x86_64-efi/</code> 这样的模块目录中加载对应模块。比如 <code>insmod btrfs</code> 让 GRUB 能读取 Btrfs 文件系统，<code>insmod fat</code> 用于读取 EFI 分区所在的 FAT 文件系统，<code>insmod gfxterm</code> 则和图形菜单显示有关。GRUB 之所以能识别分区、读取不同文件系统、显示图形菜单、加载 Linux 内核，背后靠的就是这些平台模块和文件系统模块。</p><h3 id="fonts-、locale-、themes"><code>fonts/</code>、<code>locale/</code>、<code>themes/</code></h3><p>这几个目录主要服务于 GRUB 菜单显示：</p><ul><li><code>fonts/</code>：字体文件，例如 <code>unicode.pf2</code>、<code>NotoSansMono32.pf2</code></li><li><code>locale/</code>：语言翻译文件，例如 <code>zh_CN.mo</code>、<code>en@cyrillic.mo</code></li><li><code>themes/</code>：主题资源</li></ul><p>它们不是 Linux 内核启动的核心，但会影响 GRUB 菜单的显示效果。例如如果觉得GRUB菜单中原本的 <code>unicode.pf2</code> 字体太小导致看不清，就可以塞一个 <code>NotoSansMono32.pf2</code> 这样的更大的字体进去。</p><h2 id="boot-efi-文件夹"><code>/boot/efi</code> 文件夹</h2><p><code>/boot/efi</code> 是 EFI System Partition，也就是 ESP 的挂载点。当前机器上它来自 <code>/dev/nvme0n1p1</code>，文件系统是 vfat：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/boot/efi /dev/nvme0n1p1 vfat</span><br></pre></td></tr></table></figure><p>它和普通 Linux 目录最大的区别是：<strong>UEFI 固件可以直接读取它</strong>。</p><p>固件刚启动时，还没有进入 Linux，也不认识 Linux 的 Btrfs 子卷布局，更不会直接去加载 <code>/boot/vmlinuz-linux</code>。它首先会从 NVRAM 启动项或者默认路径（可以在 UEFI 固件设置里面调整）中找到某个 <code>.efi</code> 程序，然后把控制权交给这个 EFI 程序。</p><p>对当前这台机器来说，<code>/boot/efi</code> 顶层结构大致如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">/boot/efi</span><br><span class="line">├── Boot</span><br><span class="line">├── bootmgr</span><br><span class="line">├── BOOTNXT</span><br><span class="line">├── EFI</span><br><span class="line">└── System Volume Information</span><br></pre></td></tr></table></figure><h3 id="boot-efi-EFI"><code>/boot/efi/EFI</code></h3><p>这是 ESP 中最核心的内容。结构为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">/boot/efi/EFI</span><br><span class="line">├── Boot</span><br><span class="line">│   └── bootx64.efi</span><br><span class="line">├── GRUB</span><br><span class="line">│   └── grubx64.efi</span><br><span class="line">├── Microsoft</span><br><span class="line">│   ├── Boot</span><br><span class="line">│   └── Recovery</span><br><span class="line">└── OEM</span><br><span class="line">    ├── Boot</span><br><span class="line">    └── Recovery</span><br></pre></td></tr></table></figure><h4 id="boot-efi-EFI-GRUB"><code>/boot/efi/EFI/GRUB/</code></h4><p>这是 Arch 的 GRUB 在 EFI 分区里的目录。当前里面的关键文件是 <code>grubx64.efi</code>，可以把它理解成 UEFI 世界里的“GRUB 启动程序本体”。UEFI 固件先执行这个 <code>.efi</code> 文件，然后 GRUB 再读取 <code>/boot/grub/grub.cfg</code>，显示启动菜单，并继续加载 Linux 内核和 initramfs。</p><p>它通常来自类似下面的安装命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=GRUB</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>--target=x86_64-efi</code> 表示安装 UEFI x86_64 平台的 GRUB。</li><li><code>--efi-directory=/boot/efi</code> 表示 EFI 分区挂载点是 <code>/boot/efi</code>。</li><li><code>--bootloader-id=GRUB</code> 通常会对应到 ESP 中的 <code>EFI/GRUB/</code> 目录。</li></ul><p>所以 Linux 这边的启动主线可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">UEFI 固件</span><br><span class="line">  -&gt; /boot/efi/EFI/GRUB/grubx64.efi</span><br><span class="line">  -&gt; /boot/grub/grub.cfg</span><br><span class="line">  -&gt; /boot/vmlinuz-linux</span><br><span class="line">  -&gt; /boot/intel-ucode.img + /boot/initramfs-linux.img</span><br><span class="line">  -&gt; Linux 根文件系统</span><br></pre></td></tr></table></figure><h4 id="boot-efi-EFI-Boot"><code>/boot/efi/EFI/Boot/</code></h4><p>这是 UEFI 的默认/后备启动目录。简单说，当正式的 NVRAM 启动项不可用时，固件可能会尝试从这里找一个通用 fallback 入口。当前这台机器里面只有一个 <code>bootx64.efi</code>。</p><p>这套路径是 UEFI 规范层面的约定，UEFI 规范中按机器架构定义了不同的默认文件名，例如 x86_64 使用 <code>BOOTX64.EFI</code>，IA32 使用 <code>BOOTIA32.EFI</code>：</p><blockquote><p>来源：<a href="https://uefi.org/specs/UEFI/2.11/03_Boot_Manager.html">UEFI Specification 2.11 - Boot Manager</a></p><p>x64 | <code>BOOTx64.EFI</code><br>32-bit | <code>BOOTIA32.EFI</code></p></blockquote><blockquote><p>来源：<a href="https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/bcdboot-command-line-options-techref-di?view=windows-11">BCDBoot Command-Line Options - Microsoft Learn</a></p><p>The default firmware settings should open the file: <code>\efi\boot\bootx64.efi</code> in the ESP.</p></blockquote><p>不过在另一台电脑中，文件夹下有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">BOOTIA32.EFI</span><br><span class="line">BOOTX64.EFI</span><br><span class="line">fbia32.efi</span><br><span class="line">fbx64.efi</span><br></pre></td></tr></table></figure><p>其中 <code>BOOTX64.EFI</code> 和 <code>BOOTIA32.EFI</code> 对应的就是 UEFI fallback 路径中不同架构的默认入口；<code>fbx64.efi</code>、<code>fbia32.efi</code> 则通常和 shim/fallback 机制有关。</p><blockquote><p>来源：<a href="https://github.com/rhboot/shim/blob/main/README.fallback">README.fallback - rhboot/shim</a></p><p>fallback will look for every directory in <code>\EFI\</code> with a <code>BOOT$&#123;ARCH&#125;.CSV</code> in it.</p></blockquote><p>也就是说，<code>fbx64.efi</code> 这类文件会在启动项丢失时扫描 ESP 里的 <code>BOOT.CSV</code> 之类的文件，并尝试重新创建 NVRAM 启动项。它们更多是发行版或引导器安装过程写入 ESP 的结果，而不是主板固件出厂时就固定存在的文件。</p><h4 id="boot-efi-EFI-Microsoft"><code>/boot/efi/EFI/Microsoft/</code></h4><p>这是 Windows 在 UEFI 模式下的启动目录。当前下面主要有 <code>Boot/</code> 和 <code>Recovery/</code> 两个子目录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/boot/efi/EFI/Microsoft</span><br><span class="line">├── Boot</span><br><span class="line">└── Recovery</span><br></pre></td></tr></table></figure><p>其中最重要的是 <code>Boot/</code> 里的 <code>bootmgfw.efi</code>。UEFI 固件启动 Windows 时，通常会先找到这个 <code>.efi</code> 文件：</p><blockquote><p>来源：<a href="https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/bcd-system-store-settings-for-uefi?view=windows-11">BCD System Store Settings for UEFI - Microsoft Learn</a></p><p>The path to the Windows Boot Manager is <code>\EFI\Microsoft\Boot\bootmgfw.efi</code>.</p></blockquote><p>也就是说，Windows 这边的启动主线可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">UEFI 固件</span><br><span class="line">  -&gt; /boot/efi/EFI/Microsoft/Boot/bootmgfw.efi</span><br><span class="line">  -&gt; BCD 启动配置</span><br><span class="line">  -&gt; Windows Boot Loader</span><br><span class="line">  -&gt; Windows</span><br></pre></td></tr></table></figure><p><code>Boot/</code> 目录里还会有一些配套文件，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">BCD</span><br><span class="line">BCD.LOG</span><br><span class="line">BOOTSTAT.DAT</span><br><span class="line">bootmgfw.efi</span><br><span class="line">bootmgr.efi</span><br><span class="line">memtest.efi</span><br><span class="line">SecureBootRecovery.efi</span><br><span class="line">winsipolicy.p7b</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>其中 <code>bootmgfw.efi</code> 是 Windows Boot Manager 入口，<code>BCD</code> 是 Windows 启动配置数据库，<code>BCD.LOG*</code> 是 BCD 日志，<code>memtest.efi</code>、<code>SecureBootRecovery.efi</code> 等是测试或恢复相关工具，其它字体、语言目录、<code>kd_*.dll</code> 这类文件更多是启动界面、恢复环境或底层诊断支持文件。</p><p><code>Recovery/</code> 对应 Windows 恢复链，当前里面主要也是 <code>BCD</code> 和 <code>BCD.LOG*</code>。它不是完整复制一套正常启动目录，更像是恢复环境使用的启动配置，这里知道有这么一层即可。</p><h4 id="boot-efi-EFI-OEM"><code>/boot/efi/EFI/OEM/</code></h4><p><code>OEM/</code> 通常是整机厂商留下的启动或恢复目录。看起来和 <code>/boot/efi/EFI/Microsoft/</code> 大差不差。</p><h3 id="其它顶层文件与目录">其它顶层文件与目录</h3><p>除了最核心的 <code>EFI/</code> 目录，ESP 顶层还有一些不太影响当前启动主线的文件和目录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">/boot/efi/Boot</span><br><span class="line">/boot/efi/bootmgr</span><br><span class="line">/boot/efi/BOOTNXT</span><br><span class="line">/boot/efi/System Volume Information</span><br></pre></td></tr></table></figure><p>其中 <code>Boot/</code> 和 <code>bootmgr</code> 更像是 Windows BIOS/legacy 兼容布局，或者某次部署、修复、OEM 过程留下的启动环境文件。Microsoft 的 BCDBoot 文档对不同固件类型下的文件布局有这样的说明：</p><blockquote><p>来源：<a href="https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/bcdboot-command-line-options-techref-di?view=windows-11">BCDBoot Command-Line Options - Microsoft Learn</a></p><p>BIOS: Copies the boot files to either the <code>\Boot</code> directory on the system partition, or the partition specified by the <code>/s</code> option.</p><p>UEFI: Copies the boot files to either the <code>\EFI\Microsoft\Boot</code> directory, or the partition specified by the <code>/s</code> option.</p><p>ALL: Copies both the BIOS and UEFI files to the system partition or the partition specified by the <code>/s</code> option.</p></blockquote><p>所以，ESP 顶层的 <code>Boot/</code> 和 <code>bootmgr</code> 与 <code>EFI/Microsoft/Boot</code> 本来都属于 Windows boot-environment 文件，只是面向的启动布局不同，这也导致 <code>Boot/</code> 和 <code>EFI/Microsoft/Boot</code> 里面的东西基本一样。<code>BOOTNXT</code> 可以先理解为 Windows 维护的下一次启动/临时启动状态痕迹；<code>System Volume Information</code> 则是 Windows 常见的卷级系统信息目录，目前里面也没什么内容。</p><h2 id="小结">小结</h2><p>最终，当前这台机器的双系统启动关系大概是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Arch:</span><br><span class="line">UEFI -&gt; EFI/GRUB/grubx64.efi -&gt; /boot/grub/grub.cfg -&gt; vmlinuz-linux + initramfs-linux.img</span><br><span class="line"></span><br><span class="line">Windows 直接启动:</span><br><span class="line">UEFI -&gt; EFI/Microsoft/Boot/bootmgfw.efi -&gt; BCD -&gt; Windows Boot Loader</span><br><span class="line">Windows 通过 GRUB 启动:</span><br><span class="line">UEFI -&gt; EFI/GRUB/grubx64.efi -&gt; /boot/grub/grub.cfg -&gt; EFI/Microsoft/Boot/bootmgfw.efi -&gt; BCD -&gt; Windows Boot Loader</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/36b44dc6.html</id>
    <link href="https://zhuxz0299.github.io/posts/36b44dc6.html"/>
    <published>2026-05-04T03:26:50.000Z</published>
    <summary>看一下 boot 文件夹下与 EFI 分区中都会存放什么东西，及其与开机启动的关系</summary>
    <title>boot 文件夹与 EFI 分区</title>
    <updated>2026-05-22T05:52:00.869Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="System Knowledge" scheme="https://zhuxz0299.github.io/categories/System-Hardware/System-Knowledge/"/>
    <category term="shell" scheme="https://zhuxz0299.github.io/tags/shell/"/>
    <category term="ssh" scheme="https://zhuxz0299.github.io/tags/ssh/"/>
    <category term="terminal" scheme="https://zhuxz0299.github.io/tags/terminal/"/>
    <category term="tty" scheme="https://zhuxz0299.github.io/tags/tty/"/>
    <category term="pty" scheme="https://zhuxz0299.github.io/tags/pty/"/>
    <content>
      <![CDATA[<div class="note info flat"><p>内容翻译自 <a href="https://dev.to/napicella/linux-terminals-tty-pty-and-shell-192e">Linux terminals, tty, pty and shell</a>，有一定修改。</p></div><h2 id="什么是终端（terminal）">什么是终端（terminal）</h2><p>历史上，终端是一种相对简单的机电设备，具备输入接口（如键盘）和输出接口（如显示器或纸张）。它通过两个逻辑通道连接到另一个设备（如计算机），其唯一功能是：</p><ul><li>将按键发送至第一行</li><li>从第二行开始读取并在纸上打印出来</li></ul><p>这类设备的商业名称是电传打字机（Teletypewriter，其中最著名的品牌为 Teletype，以至于可以将两者等价），其简称为 TTY。这些机器为早期的大型机和小型计算机提供用户界面，将键入的数据发送至计算机并打印出响应。</p><p>要理解现代终端的工作原理，就需要先简单了解过去电传打字机的工作方式。每台机器通过两根信号线连接到电脑：一根用于向计算机发送指令，另一根用于接收计算机的输出。这两根信号线被汇聚到一根串口线中，并插入计算机的通用异步收发器（UART）接口。</p><p>计算机中运行着 UART 驱动程序，负责从硬件设备读取数据。读取到的字符序列会被传递给 TTY 驱动程序，由该驱动应用“线路规程（Line discipline）”。线路规程负责转换特殊字符（例如换行符、退格键），并将接收到的字符回显（Echoing）给终端（teletype），以便用户能看到自己刚才输入了什么。</p><p>此外，线路规程还负责对这些字符进行缓冲（Buffer）。当用户按下回车键时，缓冲区内的数据会被传递给当前 TTY 关联会话的前台进程（Foreground process）。作为用户，你可以同时并行执行多个进程，但同一时刻只能与其中一个进行交互，并让其他进程在后台运行（或等待）。上述定义的这一整套技术栈，统称为一个 TTY 设备（TTY device）。</p><p>接收这些数据的前台进程，通常是一个名为 Shell 的计算机程序。</p><div class="note default flat"><p>提示（Gotcha）：在日常交流中，“终端”和“TTY 设备”这两个词基本是可以互换使用的，因为它们指代的其实是同一个东西。</p></div><h2 id="什么是-shell">什么是 shell</h2><p>Shell 属于用户态（User space）应用程序，它们调用内核 API 的方式与其他普通应用程序完全相同。</p><p>Shell 通过提示用户进行输入、解析这些输入，并处理来自底层操作系统的输出来管理用户与系统之间的交互（这非常类似于“读取-求值-输出”循环，即 REPL）。</p><p>例如，如果你输入了 <code>cat file | grep hello</code>，Bash 会解析这条指令，判断出它需要运行 <code>cat</code> 程序并将 <code>file</code> 作为参数传递给它，然后将输出结果通过管道（<code>pipe</code>）传递给 <code>grep</code>。</p><p>Shell 还可以控制程序的执行（这项功能被称为作业控制，Job control）：它可以终止程序（<code>Ctrl + C</code>）、挂起程序（<code>Ctrl + Z</code>），以及将程序设置为在前台（<code>fg</code>）或后台（<code>bg</code>）运行。</p><p>此外，Shell 也可以在非交互模式下运行，即通过执行包含一连串命令的脚本（script）来工作。Bash、Zsh、Fish 和 sh 都是不同流派（或风格）的 Shell。</p><h2 id="什么是终端模拟器（terminal-emulator）">什么是终端模拟器（terminal emulator）</h2><p>让我们把目光转向离现在更近的时代。计算机开始变得越来越小，所有的组件都被塞进了一个单一的机箱里。</p><p>这是终端首次不再是通过 UART 连接到计算机的物理设备。终端变成了一段软件程序，它在软件层面上模拟物理终端设备的行为，因此得名“终端模拟器”。请注意，尽管它们是模拟出来的，但在过去和现在，它们在系统中仍被称为 Teletypes（TTY）。</p><p>第一代的终端模拟器是直接运行在系统内核中的。 这个内核程序会直接将字符发送给 TTY 驱动，并从驱动中读取字符打印到屏幕上。</p><div class="note default flat"><p>别被“模拟器”这个词给骗了，这种内核级的终端模拟器和过去的物理终端一样“笨”，它只是监听来自硬件键盘的事件，并将其向下发送给驱动程序。唯一的区别在于，这里不再有物理设备或线缆连接到 TTY 驱动上了。</p></div><p>那么如何才能看到一个由内核模拟的 TTY 呢？如果你的机器上运行着 Linux 操作系统，请按下 <code>Ctrl+Alt+F4</code>。你就会看到一个由内核模拟出来的纯文本 TTY！你可以通过按下 <code>Ctrl+Alt</code> 配合功能键（<code>F3</code> 到 <code>F6</code>）来切换到其他的 TTY。按下 <code>Ctrl+Alt+F1</code>，你就能回到图形用户界面。</p><div class="note warning flat"><p>上面关于调出内核模拟的 TTY 的使用按键与原文不同。通过在现代 Linux 电脑上测试，发现 Ctrl+Alt+(F1/F2) 通常分配给了图形界面会话；后面几个按键（如 F3~F6）才会进入纯文本的 TTY 虚拟控制台。</p></div><p>让我们来回顾一下到目前为止的核心概念：</p><ul><li>终端（Terminal）和 TTY 这两个词可以互换使用。</li><li>电传打字机（Teletype/TTY）原本是为电报设计的物理机电设备，后来被改造用于向大型机发送输入并获取输出。</li><li>终端模拟器泛指一切用软件模拟物理终端的程序。早期它作为一个模块运行在内核中（虚拟控制台），而现代则演变成了运行在图形界面上的应用程序。</li></ul><h2 id="什么是伪终端（PTY）">什么是伪终端（PTY）</h2><p>随着图形界面的普及，我们不再满足于只对着全屏的黑底白字敲命令。这就催生了现代的用户态终端模拟器（例如我们在桌面环境下常用的 iTerm2、Ghostty 或 Windows Terminal）。</p><p>但是，底层的命令行程序（如 Bash、Vim）非常“死板”，它们只认传统的 TTY 设备，不知道如何直接与这些现代桌面软件沟通。为了在这两者之间搭桥，操作系统引入了伪终端（PTY）的通信机制。PTY 本质上是内核提供的一对虚拟设备接口（包含主控端 Master 和从设备端 Slave）。它本身并不是那个带图形界面的桌面程序，而是一条专门为桌面程序准备的“数据管道”。</p><p>将其与早期（内核模拟的）TTY 进行比较，两者的核心架构发生了本质变化：</p><ul><li>在过去，内核既要负责底层的线路规程，又要负责直接控制显卡在屏幕上画出字符；</li><li>而在现代 PTY 架构中，终端界面的渲染和按键捕获被彻底移出了内核。内核现在只负责维护 PTY 这根“通信管道”和原有的 TTY 逻辑（如会话管理），而真正与用户交互、画出窗口的，变成了普通的用户态程序。</li></ul><p>为什么要做这种架构迁移？因为运行在内核中的程序拥有极高的特权。如果早期的内核终端驱动出了 Bug，整个系统都可能直接崩溃（Kernel Panic）；但在 PTY 架构下，如果你的终端软件（比如 Ghostty）卡死了，它只会影响这个软件本身。内核和底层的通信管道依然安全，最坏的情况下，你只需 kill 掉卡死的软件重新打开即可。</p><p>毫无疑问，这是一个极其优雅的设计。PTY 的存在，使得终端模拟器可以放心地向用户态迁移并发展出千变万化的形态（多标签页、GPU 加速渲染），同时又完美欺骗了底层的 Shell，让它们依然以为自己连接着一台老式的物理电传打字机。</p><h2 id="它是如何运作的？（宏观架构）">它是如何运作的？（宏观架构）</h2><p>终端模拟器（或任何其他程序）可以向系统内核申请一对字符设备文件，分别称为 PTY Master（主控端） 和 PTY Slave（从设备端）。在这条管道的一头（Master 端）连接着终端模拟器，而在另一头（Slave 端）连接着 Shell。在 Master 和 Slave 之间，横跨着底层的 TTY 驱动程序（包含线路规程、会话管理等功能），它负责在 PTY 的两端之间双向拷贝数据。</p><h3 id="当我们敲击键盘时，到底发生了什么？">当我们敲击键盘时，到底发生了什么？</h3><p>平时我们总说“我打开了一个终端”或“我开了一个 Bash”，但在用户态的终端模拟器（如 Ghostty 等应用程序）中，底层的真实执行顺序如下：</p><ol><li>界面启动与申请管道：一个模拟终端的图形界面（GUI）程序启动（如 Ghostty）。它在屏幕上画出 UI，并向操作系统申请一对 PTY。</li><li>启动底层后备进程：它将 bash 作为一个子进程启动。</li><li>绑定输入输出：这个 bash 进程的标准输入（stdin）、标准输出（stdout）和标准错误（stderr），全都会被重定向并绑定到那个 PTY Slave 端口上。</li><li>监听按键：Ghostty 监听你的键盘事件，并将敲击的字符发送给 PTY Master。</li><li>内核拦截与回显：内核中的线路规程（Line discipline）截获这些字符并将它们放入缓冲区。只有当你按下回车键时，它才会把字符真正拷贝给 Slave 端。同时，线路规程还会把接收到的字符原样写回给 Master 端（回显，Echoing）。<div class="note default flat"><p>记住，终端模拟器本身是很“笨”的，它只在屏幕上显示从 PTY Master 传来的东西。因此，是线路规程把字符“回显”了回来，终端模拟器才会在屏幕上把它画出来，让你看到自己刚敲了什么字。</p></div></li><li>提交数据：当你按下回车键时，TTY 驱动程序（一个内核模块）负责将缓冲区里的数据正式拷贝给 PTY Slave。</li><li>Shell 读取与解析：一直在标准输入上等待的 bash，终于读取到了这些字符（比如 <code>ls -la</code>）。再说一遍，此时 bash 的标准输入就是 PTY Slave。bash 解析这些字符，明白它需要去执行 <code>ls</code> 命令。</li><li>进程分叉（Fork）与继承：bash <code>fork</code> 出一个新进程并在其中运行 <code>ls</code>。这个被 <code>fork</code> 出来的新进程会完美继承 bash 的输入/输出通道——没错，依然是那个 PTY Slave。</li><li>命令执行与输出：<code>ls</code> 程序运行，并将结果打印到它的标准输出（再一次，数据被写进了 PTY Slave）。</li><li>数据回传：TTY 驱动将这些字符拷贝回 PTY Master。</li><li>重新渲染：Ghostty 在一个无限循环中，从 PTY Master 不断读取这些返回的字节，并重新在屏幕上画出 UI，于是你就看到了命令的输出结果。</li></ol><pre><code class="highlight mermaid">graph TD    %% --- 定义主要节点 (实体方块) ---    TE[TERMINAL EMULATOR&lt;br&gt;GUI LIKE Ghostty]    PM[PTY MASTER]    TD[TTY DRIVER]    PS[PTY SLAVE]    BASH[BASH]    CAT[CAT FILE]    %% --- 定义注释节点 (模拟手写批注) ---    NoteTE(/IT LISTENS FOR&lt;br&gt;KEYS AND DRAWS&lt;br&gt;THE GUI/)    NoteTD(/KERNEL MODULE.&lt;br&gt;LINE DISCIPLINE IS&lt;br&gt;APPLIED HERE/)    NoteCAT(/THE PROGRAM YOU&lt;br&gt;START BY TYPING&lt;br&gt;IN THE TERMINAL UI/)    %% --- 定义主要连接 (实线) ---    TE --- PM    PM --- TD    TD --- PS    PS --- BASH    PS --- CAT    %% --- 定义注释连接 (虚线箭头) ---    NoteTE -.-&gt; TE    NoteTD -.-&gt; TD    NoteCAT -.-&gt; CAT    %% --- 样式调整 (透明背景，自适应深浅模式) ---    %% fill:transparent 确保无背景色；stroke:#888 提供中性灰边框    classDef mainNode fill:transparent,stroke:#888,stroke-width:2px;    class TE,PM,TD,PS,BASH,CAT mainNode;    %% 注释节点同样透明，改用细虚线边框    classDef noteNode fill:transparent,stroke:#888,stroke-width:1px,stroke-dasharray: 5 5,font-size:smaller,font-style:italic;    class NoteTE,NoteTD,NoteCAT noteNode;    %% 强制 BASH 和 CAT 在同一层级    subgraph Bottom [ ]        direction LR        BASH        CAT    end    style Bottom fill:transparent,stroke:none</code></pre><p>作为一个实验，在一个新开的终端实例中运行 <code>ps</code> 命令。如果你还没有运行任何其他程序，你会看到与该终端关联的程序只有 <code>ps</code> 和 <code>bash</code>。<br>ps 是我们刚刚启动的程序，而 <code>bash</code> 是由终端自动启动的。你在结果中看到的 <code>pts/0</code> 就是我们之前讨论过的 PTY Slave（从设备端）。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ps</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">  PID TTY          TIME CMD</span><br><span class="line">26113 pts/0    00:00:00 ps</span><br><span class="line">30985 pts/0    00:00:00 bash</span><br></pre></td></tr></table></figure><p>让我们看看，当你启动一个从终端模拟器的标准输入（Standard Input）读取数据的程序时会发生什么。<br>这个程序可以很简单，仅仅是从标准输入读取内容并将其写到标准输出，如下面的 Go 代码所示：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;bufio&quot;</span></span><br><span class="line">    <span class="string">&quot;fmt&quot;</span></span><br><span class="line">    <span class="string">&quot;os&quot;</span></span><br><span class="line">)</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">  reader := bufio.NewReader(os.Stdin)</span><br><span class="line">  fmt.Print(<span class="string">&quot;Enter text: &quot;</span>)</span><br><span class="line">  text, _ := reader.ReadString(<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">  fmt.Println(text)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">go build -o simple-program</span><br><span class="line">./simple-program</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Enter text:</span><br></pre></td></tr></table></figure><p>在这个程序等待输入时，尝试随便敲几个字母，但不要按回车，而是按下 <code>Ctrl + W</code>。你会发现你刚刚打出的单词被删除了。为什么？</p><p>因为 <code>Ctrl + W</code> 这个字符被分配给了一个名为 <code>werase</code>（word erase，删除单词）的线路规程规则。回想一下之前那幅架构图：你敲击的字符被发送到 PTY Master，接着传给实现了线路规程的 TTY Driver（内核）。当线路规程读到 <code>Ctrl + W</code> 这个字符时，它会从自己的内部缓冲区中删掉最后一个单词，并向 PTY Master 发送反向指令：将光标向后退 N 个位置（N 是单词的长度），并沿途擦除屏幕上的字符。</p><p>现在做同样的实验，但这次不要输入字母，而是按下方向键。这些弹出来的奇怪字符 <code>^[A^[B^[C^[D</code> 是什么鬼东西？</p><p>正如我们前面所说，终端只是单纯地将你的按键发送给 Master 端。但是，对于像方向键这样在 ASCII 码表里没有对应字符的按键该怎么办呢？当遇到这种情况时，终端会使用多个字符来对它们进行编码；例如，向上方向键会被编码为 <code>^[A</code>，这里的 <code>^[</code> 被称为转义序列（Escape sequence）。</p><p>因此，当你按下方向键时，底层发生的物理流程和按其他键是一模一样的，只不过它在发送给 PTY Master 时被这样编码了，所以线路规程把它“回显”在屏幕上的结果看起来就非常怪异。</p><div class="note default flat"><p>这里的核心结论是：内核的线路规程并不负责处理上、下、左、右等方向键。</p></div><p>我听到你在问……等等！那为什么当我在没有任何程序运行的纯终端里按下方向键时，我调出的是 Bash 的历史命令，而不是乱码？</p><p>这是因为 <code>^[A</code> 最终被传递给了 Bash 程序，Bash 将其解析为一个请求：“获取命令历史记录中的当前条目”，然后打印出 Bash 历史记录中的那行命令。</p><div class="note info flat"><p>这里需要引入程序接管终端的两种模式：</p><ul><li>状态 A：规范模式（Canonical Mode / Cooked Mode）<ul><li>谁在用：那个简单的 Go 程序、cat、read 命令。</li><li>怎么工作：在这个模式下，内核的 TTY 线路规程是“开启”状态的。内核会把键盘输入缓冲起来，遇到 <code>Ctrl+W</code> 时，内核自己就把内部缓冲区的字删了。直到你按下回车，内核才把剩下的那句话交给 Go 程序。Go 程序根本不知道你按过 <code>Ctrl+W</code>。</li></ul></li><li>状态 B：非规范模式（Non-Canonical Mode / Raw Mode）<ul><li>谁在用：Bash（通过 Readline 库）、Vim、Tmux。</li><li>怎么工作：Bash 在启动时，会调用底层的 C 语言 API（<code>tcsetattr</code>），直接把内核的“规范模式”给关了！ 此时，内核的线路规程变成了“瞎子”，它不再缓冲，也不再处理 <code>Ctrl+W</code>。你按下的每一个键（包括 <code>Ctrl+W</code> 和方向键 <code>^[[A</code>），内核都原封不动地立刻塞给 Bash。是 Bash 自己内部的逻辑判断出这是 <code>Ctrl+W</code>，然后执行了删词操作。</li></ul></li></ul><p>这两个模式在 Linux 系统底层终端 I/O 的 API 文档 <a href="https://man7.org/linux/man-pages/man3/termios.3.html">termios(3) - Linux manual page</a> 中有说明。</p></div>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/6b3e2f30.html</id>
    <link href="https://zhuxz0299.github.io/posts/6b3e2f30.html"/>
    <published>2026-03-21T07:11:49.000Z</published>
    <summary>
      <![CDATA[<div class="note info flat"><p>内容翻译自 <a href="https://dev.to/napicella/linux-terminals-tty-pty-and-shell-192e">Linux terminals, tty, pty and]]>
    </summary>
    <title>terminal, tty, pty 与 shell</title>
    <updated>2026-04-27T13:58:00.775Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="Dev Tools" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/"/>
    <category term="Software" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/Software/"/>
    <category term="systemd" scheme="https://zhuxz0299.github.io/tags/systemd/"/>
    <category term="rclone" scheme="https://zhuxz0299.github.io/tags/rclone/"/>
    <category term="webdav" scheme="https://zhuxz0299.github.io/tags/webdav/"/>
    <category term="sync" scheme="https://zhuxz0299.github.io/tags/sync/"/>
    <content>
      <![CDATA[<h2 id="云存储配置">云存储配置</h2><p>如果希望配置云存储，可以先运行 <code>rclone config</code>，得到输出为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">Current remotes:</span><br><span class="line"></span><br><span class="line">Name                 Type</span><br><span class="line">====                 ====</span><br><span class="line">Tbox                 webdav</span><br><span class="line">cxftp                ftp</span><br><span class="line"></span><br><span class="line">e) Edit existing remote</span><br><span class="line">n) New remote</span><br><span class="line">d) Delete remote</span><br><span class="line">r) Rename remote</span><br><span class="line">c) Copy remote</span><br><span class="line">s) Set configuration password</span><br><span class="line">q) Quit config</span><br><span class="line">e/n/d/r/c/s/q&gt;</span><br></pre></td></tr></table></figure><p><code>Current remotes</code> 即为当前已经创建过的云存储配置；在下方的 prompt 输入相应的字母即可进入下一步。</p><h3 id="创建新配置">创建新配置</h3><ul><li>选择 <code>n</code> （新建 remote）。</li><li>输入名称（如 <code>Tbox</code>）。</li><li>然后会跳出来超级多存储类型选项，从中选择需要配置的云存储类型，（例如选择 WebDAV，则在 <code>Storage&gt;</code> prompt 后面输入 <code>62</code>）</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"> 1 / 1Fichier</span><br><span class="line">   \ (fichier)</span><br><span class="line"> 2 / Akamai NetStorage</span><br><span class="line">   \ (netstorage)</span><br><span class="line"> 3 / Alias for an existing remote</span><br><span class="line">   \ (alias)</span><br><span class="line"> 4 / Amazon S3 Compliant Storage Providers including AWS, Alibaba, ArvanCloud, BizflyCloud, Ceph, ChinaMobile, Cloudflare, Cubbit, DigitalOcean, Dreamhost, Exaba, FileLu, FlashBlade, GCS, Hetzner, HuaweiOBS, IBMCOS, IDrive, Intercolo, IONOS, Leviia, Liara, Linode, LyveCloud, Magalu, Mega, Minio, Netease, Outscale, OVHcloud, Petabox, Qiniu, Rabata, RackCorp, Rclone, Scaleway, SeaweedFS, Selectel, Servercore, SpectraLogic, StackPath, Storj, Synology, TencentCOS, Wasabi, Zata, Other</span><br><span class="line">   \ (s3)</span><br><span class="line"> 5 / Backblaze B2</span><br><span class="line">   \ (b2)</span><br><span class="line"> 6 / Better checksums for other remotes</span><br><span class="line">   \ (hasher)</span><br><span class="line"> 7 / Box</span><br><span class="line">   \ (box)</span><br><span class="line">... (中间省略一些)</span><br><span class="line">61 / Union merges the contents of several upstream fs</span><br><span class="line">   \ (union)</span><br><span class="line">62 / WebDAV</span><br><span class="line">   \ (webdav)</span><br><span class="line">63 / Yandex Disk</span><br><span class="line">   \ (yandex)</span><br><span class="line">64 / Zoho</span><br><span class="line">   \ (zoho)</span><br><span class="line">65 / iCloud Drive</span><br><span class="line">   \ (iclouddrive)</span><br><span class="line">66 / premiumize.me</span><br><span class="line">   \ (premiumizeme)</span><br><span class="line">67 / seafile</span><br><span class="line">   \ (seafile)</span><br><span class="line">Storage&gt; </span><br></pre></td></tr></table></figure><ul><li>根据提示输入 WebDAV 服务器的地址 (<code>url</code>)、用户名 (<code>user</code>)、密码 (<code>pass</code>)。</li><li>对于其他高级设置，如果不确定，通常可以直接按回车使用默认值。</li><li>配置完成后，确认保存 (<code>y</code>)。</li></ul><p>此时就可以对云存储的内容进行操作了。例如运行 <code>rclone lsd Tbox:/</code> 查看云存储中的内容。</p><h3 id="已有配置查看">已有配置查看</h3><p>直接运行 <code>rclone config</code> 看到的信息比较简陋，因此可以运行 <code>rclone config show</code>，输出更加详细的信息：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Tbox]</span></span><br><span class="line"><span class="attr">type</span> = webdav</span><br><span class="line"><span class="attr">url</span> = http://<span class="number">127.0</span>.<span class="number">0.1</span>:<span class="number">65472</span>/</span><br><span class="line"><span class="attr">vendor</span> = other</span><br><span class="line"><span class="attr">user</span> = zxz</span><br><span class="line"><span class="attr">pass</span> = <span class="number">4</span>tJdukYexWERg8oexoZ4iiv4r_2aDZMepH1Bqbi4b2BqLo</span><br><span class="line"></span><br><span class="line"><span class="section">[cxftp]</span></span><br><span class="line"><span class="attr">type</span> = ftp</span><br><span class="line"><span class="attr">port</span> = <span class="number">1484</span></span><br><span class="line"><span class="attr">user</span> = pc</span><br><span class="line"><span class="attr">pass</span> = <span class="number">31</span>mvT_D6sImcYOnHThoDe_tJA</span><br><span class="line"><span class="attr">host</span> = <span class="number">10.85</span>.<span class="number">33.68</span></span><br></pre></td></tr></table></figure><p>该信息和 <code>cat ~/.config/rclone/rclone.conf</code> 得到的结果完全一致。</p><div class="note warning flat"><p><code>~/.config/rclone/rclone.conf</code> 里面记录的密码 <code>pass</code> 都是加密之后的结果，因此不要通过直接编辑该配置文件的方式添加或修改云存储配置，否则密码不可用。</p></div><h2 id="WebDAV-挂载">WebDAV 挂载</h2><div class="note info flat"><p><a href="https://rclone.org/commands/rclone_mount/">官方文档 rclone mount</a></p></div><p>在 Linux 中，如果希望将云存储挂载到本地，可以运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rclone mount remote:path/to/files /path/to/local/mount</span><br></pre></td></tr></table></figure><p>其中 <code>/path/to/local/mount</code> 是一个空现有目录。例如</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rclone mount Tbox:/ /mnt/Tbox/</span><br></pre></td></tr></table></figure><p>就是将整个 <code>Tbox</code> 云存储的内容挂载到挂载点 <code>/mnt/Tbox/</code>。</p><p>如果需要从本地卸载该云存储，则需使用 <code>fusermount</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fusermount -u /path/to/local/mount</span><br></pre></td></tr></table></figure><h3 id="挂载参数">挂载参数</h3><p>为了实现比较好的挂载效果，需要进行一些设置，尤其是 VFS 文件缓存的设置：</p><blockquote><p>File systems expect things to be 100% reliable, whereas cloud storage systems are a long way from 100% reliable. The rclone sync/copy commands cope with this with lots of retries. However rclone mount can’t use retries in the same way without making local copies of the uploads. Look at the VFS File Caching for solutions to make mount more reliable.</p></blockquote><table><thead><tr><th style="text-align:left">参数名称</th><th style="text-align:left">说明与用途</th><th style="text-align:left">常见值/示例</th></tr></thead><tbody><tr><td style="text-align:left"><strong>基本挂载参数</strong></td><td style="text-align:left"></td><td style="text-align:left"></td></tr><tr><td style="text-align:left"><code>&lt;remote&gt;:&lt;path&gt;</code></td><td style="text-align:left">远程存储名称和路径</td><td style="text-align:left"><code>dav:/</code></td></tr><tr><td style="text-align:left"><code>&lt;local_path&gt;</code></td><td style="text-align:left">本地挂载点路径</td><td style="text-align:left"><code>/webdav</code></td></tr><tr><td style="text-align:left"><code>--daemon</code></td><td style="text-align:left"><strong>后台守护进程</strong>模式，允许命令在后台运行，关闭终端也不中断。</td><td style="text-align:left"></td></tr><tr><td style="text-align:left"><strong>权限与访问控制</strong></td><td style="text-align:left"></td><td style="text-align:left"></td></tr><tr><td style="text-align:left"><code>--allow-non-empty</code></td><td style="text-align:left">允许挂载到<strong>非空目录</strong>上。</td><td style="text-align:left"></td></tr><tr><td style="text-align:left"><code>--log-level &lt;LEVEL&gt;</code></td><td style="text-align:left">设置日志详细程度，调试时有用。</td><td style="text-align:left"><code>INFO</code>, <code>DEBUG</code></td></tr><tr><td style="text-align:left"><code>--log-file &lt;FILE&gt;</code></td><td style="text-align:left">将日志输出到指定文件。</td><td style="text-align:left"><code>--log-file /var/log/rclone.log</code></td></tr><tr><td style="text-align:left"><code>--read-only</code></td><td style="text-align:left"><strong>以只读模式挂载</strong>，防止意外修改或删除远程文件。</td><td style="text-align:left"></td></tr></tbody></table><p>VFS 文件缓存设置：</p><table><thead><tr><th style="text-align:left">参数 (Flags)</th><th style="text-align:left">默认值</th><th style="text-align:left">作用描述 (Description)</th></tr></thead><tbody><tr><td style="text-align:left"><code>--vfs-cache-mode</code></td><td style="text-align:left"><code>off</code></td><td style="text-align:left"><strong>VFS 缓存模式（WebDAV 必备参数）</strong>。可选值：<code>off</code>、<code>minimal</code>、<code>writes</code>、<code>full</code>。为了使 VFS 层表现得像普通文件系统（尤其是处理需要同时读写、随机写入的应用程序），<strong>强烈建议将 WebDAV 挂载设置为 <code>writes</code> 或 <code>full</code></strong>，否则许多常规文件操作会报错。</td></tr><tr><td style="text-align:left"><code>--vfs-cache-max-age</code></td><td style="text-align:left"><code>1h0m0s</code></td><td style="text-align:left"><strong>缓存最大存活时间</strong>。指定缓存中的文件对象在最后一次被访问后，能在本地磁盘保留的最长时间。</td></tr><tr><td style="text-align:left"><code>--vfs-cache-max-size</code></td><td style="text-align:left"><code>off</code></td><td style="text-align:left"><strong>缓存最大容量</strong>。限制本地 VFS 缓存所能占用的最大磁盘空间总大小（例如设置为 <code>10G</code>）。当达到上限时，rclone 会自动清理最旧的缓存文件。</td></tr><tr><td style="text-align:left"><code>--dir-cache-time</code></td><td style="text-align:left"><code>5m0s</code></td><td style="text-align:left"><strong>目录缓存时间</strong>。将目录条目（文件列表）在本地缓存的时间。在这段时间内，rclone 不会向 WebDAV 服务器发起重复请求，这能显著提高 <code>ls</code> 等目录列出命令的响应速度。</td></tr><tr><td style="text-align:left"><code>--buffer-size</code></td><td style="text-align:left">系统限制</td><td style="text-align:left"><strong>VFS 文件缓冲大小</strong>。决定在内存中预先缓冲数据的大小。每个打开的文件会尝试在内存中保留这么多数据以加速读取。注意总内存使用量最高可达 <code>--buffer-size * 打开的文件数</code>。</td></tr><tr><td style="text-align:left"><code>--vfs-read-chunk-size</code></td><td style="text-align:left"><code>128M</code></td><td style="text-align:left"><strong>分块读取大小</strong>。从远程存储读取文件时按指定的块大小进行请求，而不是直接请求整个文件。这能减少不必要的数据下载和带宽消耗。</td></tr><tr><td style="text-align:left"><code>--vfs-read-ahead</code></td><td style="text-align:left">(未启用)</td><td style="text-align:left"><strong>VFS 预读大小</strong>。在使用 <code>full</code> 缓存模式时，rclone 读取文件时除了在内存中缓冲外，还会在磁盘上提前预读指定大小的数据，以提高大文件顺序读取的性能。</td></tr></tbody></table><p>运行示例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rclone mount Tbox:/ /mnt/Tbox/ --cache-dir /mnt/Tbox_cache --allow-non-empty --vfs-cache-mode full --vfs-cache-max-size 10G --vfs-read-chunk-size 32M --dir-cache-time 30m</span><br></pre></td></tr></table></figure><h2 id="双向同步">双向同步</h2><div class="note info flat"><p><a href="https://rclone.org/commands/rclone_bisync/">官方文档 rclone bisync</a></p></div><p>如果希望执行远程存储和本地的双向同步，可以运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rclone bisync remote1:path1 remote2:path2 [flags]</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rclone bisync /home/zxz/Data/laboratory Tbox:laboratory</span><br></pre></td></tr></table></figure><h3 id="参数说明">参数说明</h3><p>经常用到的重要参数：</p><table><thead><tr><th style="text-align:left">参数 / 指令</th><th style="text-align:left">可选值 / 示例</th><th style="text-align:left">默认值</th><th style="text-align:left">官方文档说明与实际作用</th></tr></thead><tbody><tr><td style="text-align:left"><code>--dry-run</code> 或 <code>-n</code></td><td style="text-align:left">(作为开关参数，无值)</td><td style="text-align:left">(不启用)</td><td style="text-align:left"><strong>测试运行（演练）。</strong> 只在终端输出计划要执行的操作列表，绝对不会对本地或远端的文件进行任何实际的增删改。强烈建议在执行 <code>--resync</code> 前使用。</td></tr><tr><td style="text-align:left"><code>--verbose</code> 或 <code>-v</code></td><td style="text-align:left">(作为开关参数，无值)</td><td style="text-align:left">(不启用)</td><td style="text-align:left"><strong>输出详细日志。</strong> rclone 默认非常安静。加上此参数会输出 INFO 级别的日志，让你清楚地看到文件的扫描进度、比对逻辑和传输统计。</td></tr></tbody></table><p>其他用到过的参数：</p><table><thead><tr><th style="text-align:left">参数 / 指令</th><th style="text-align:left">可选值 / 示例</th><th style="text-align:left">默认值</th><th style="text-align:left">官方文档说明与实际作用</th></tr></thead><tbody><tr><td style="text-align:left"><code>--resync</code></td><td style="text-align:left">(作为开关参数，无值)</td><td style="text-align:left">(不启用)</td><td style="text-align:left"><strong>执行强制重对齐。</strong> 通常用于首次运行、更改了过滤规则或发生致命错误后修复数据库。如果不额外指定模式，它的行为等同于 <code>--resync-mode path1</code>。</td></tr><tr><td style="text-align:left"><code>--resync-mode</code></td><td style="text-align:left"><code>none</code>, <code>path1</code>, <code>path2</code>, <code>newer</code>, <code>older</code>, <code>larger</code>, <code>smaller</code></td><td style="text-align:left"><code>none</code> (若带有 <code>--resync</code> 则默认为 <code>path1</code>)</td><td style="text-align:left"><strong>指定重对齐 (Resync) 期间的覆盖策略。</strong> 当两端不一致时，决定以哪一边为基准。例如 <code>path2</code> 表示无条件让远端（Path2）覆盖本地（Path1）。<em>注：只要指定了非 none 的值，就会隐式触发 <code>--resync</code>。</em></td></tr><tr><td style="text-align:left"><code>--conflict-resolve</code></td><td style="text-align:left"><code>none</code>, <code>path1</code>, <code>path2</code>, <code>newer</code>, <code>older</code>, <code>larger</code>, <code>smaller</code></td><td style="text-align:left"><code>none</code></td><td style="text-align:left"><strong>自动处理日常同步中的冲突。</strong> 当一个文件在两端同时被修改产生冲突时，决定保留哪个版本。默认 <code>none</code> 会保留冲突文件（加后缀）并报错退出；设置为 <code>newer</code> 会自动保留最后修改时间更近的版本。</td></tr><tr><td style="text-align:left"><code>--create-empty-src-dirs</code></td><td style="text-align:left">(作为开关参数，无值)</td><td style="text-align:left">(不启用)</td><td style="text-align:left"><strong>同步空目录。</strong> rclone 默认为了效率会忽略空文件夹。加上此参数后，一方创建或删除空文件夹的行为也会同步到另一方。</td></tr></tbody></table><h3 id="实际同步流程">实际同步流程</h3><p>在第一次运行的时候，需要进行一次强制对齐：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 测试运行（不会真正修改文件）</span></span><br><span class="line"><span class="comment"># 第一个是本地同步到云端；第二个是云端同步到本地</span></span><br><span class="line">rclone bisync /home/zxz/Data/laboratory Tbox:laboratory --resync --dry-run -v </span><br><span class="line">rclone bisync /home/zxz/Data/laboratory Tbox:laboratory --resync --resync-mode path2 --dry-run -v</span><br><span class="line"></span><br><span class="line"><span class="comment"># 实际运行（建立初始同步）</span></span><br><span class="line">rclone bisync /home/zxz/Data/laboratory Tbox:laboratory --resync -v</span><br><span class="line">rclone bisync /home/zxz/Data/laboratory Tbox:laboratory --resync --resync-mode path2 -v</span><br></pre></td></tr></table></figure><p>后续同步正常运行即可</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rclone bisync /home/zxz/Data/laboratory Tbox:laboratory -v</span><br></pre></td></tr></table></figure><p>如果某次同步遇到冲突，在不设置 <code>--conflict-resolve</code> 的情况下，rclone 会将产生冲突的文件重命名，通常会加上 <code>..path1</code> 和 <code>..path2</code> 的后缀保留在文件夹里。用户需要手动解决冲突，然后运行f</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rclone bisync /home/zxz/Data/laboratory Tbox:laboratory --resync -v</span><br></pre></td></tr></table></figure><p>重新对齐数据库，才能恢复正常的日常自动同步。</p><div class="note warning flat"><p>如果不加 <code>--resync-mode</code> 参数，<code>--resync</code> 的默认行为是以 Path1（即本地）为准去覆盖 Path2（即远端）。<br>不过考虑到用户通常在本地电脑上处理冲突，所以直接运行上面的命令是合理的。</p></div><h3 id="创建-systemd-服务以实现自动同步">创建 systemd 服务以实现自动同步</h3><p>在 Arch Linux 上，个人用户目录下的文件同步推荐使用 Systemd User Service (用户级服务)。这样不需要 root 权限，且在用户登录后才会运行，较为安全。</p><h4 id="新建与使用-Service">新建与使用 Service</h4><p>首先创建 Service 文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim ~/.config/systemd/user/rclone-laboratory-sync.service</span><br></pre></td></tr></table></figure><p>写入</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Unit]</span></span><br><span class="line"><span class="attr">Description</span>=Rclone Bisync for Laboratory Folder</span><br><span class="line"><span class="comment"># 确保在网络连接建立后才运行</span></span><br><span class="line"><span class="attr">After</span>=network-<span class="literal">on</span>line.target</span><br><span class="line"><span class="attr">Wants</span>=network-<span class="literal">on</span>line.target</span><br><span class="line"></span><br><span class="line"><span class="section">[Service]</span></span><br><span class="line"><span class="attr">Type</span>=<span class="literal">on</span>eshot</span><br><span class="line"><span class="comment"># 这里是你日常双向同步的命令，加入了 --conflict-resolve newer 自动处理冲突</span></span><br><span class="line"><span class="attr">ExecStart</span>=/usr/bin/rclone bisync /home/zxz/Data/laboratory Tbox:laboratory -v</span><br><span class="line"><span class="comment"># 限制重试次数，避免网络不好时无限卡住</span></span><br><span class="line"><span class="attr">ExecStartPost</span>=/bin/sleep <span class="number">1</span></span><br></pre></td></tr></table></figure><p>再设置定时器文件以支持定时自动同步：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nano ~/.config/systemd/user/rclone-laboratory-sync.timer</span><br></pre></td></tr></table></figure><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Unit]</span></span><br><span class="line"><span class="attr">Description</span>=Timer for Rclone Laboratory Bisync</span><br><span class="line"></span><br><span class="line"><span class="section">[Timer]</span></span><br><span class="line"><span class="comment"># 开机/用户登录后等待 3 分钟执行第一次（留出时间给 WebDAV 服务启动和网络连接）</span></span><br><span class="line"><span class="attr">OnBootSec</span>=<span class="number">3</span>min</span><br><span class="line"><span class="comment"># 之后每隔 15 分钟执行一次（你可以根据需要把 15min 改成 5min 或 30min）</span></span><br><span class="line"><span class="attr">OnUnitActiveSec</span>=<span class="number">15</span>min</span><br><span class="line"><span class="comment"># 即使错过了任务（比如电脑休眠了），唤醒后也会补全执行</span></span><br><span class="line"><span class="attr">Persistent</span>=<span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="section">[Install]</span></span><br><span class="line"><span class="attr">WantedBy</span>=timers.target</span><br></pre></td></tr></table></figure><p>最后加载配置并使用定时器：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">systemctl --user daemon-reload <span class="comment"># 重新加载 systemd 用户配置</span></span><br><span class="line">systemctl --user <span class="built_in">enable</span> --now rclone-laboratory-sync.timer <span class="comment"># 启用并立即启动 Timer</span></span><br></pre></td></tr></table></figure><h4 id="常用维护命令">常用维护命令</h4><p>用于查看 systemd 模块的运行情况：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看定时器下次运行的时间</span></span><br><span class="line">systemctl --user list-timers --all | grep rclone</span><br><span class="line"><span class="comment"># 查看服务最近一次的运行日志，如果同步出错这里能看到 rclone 的报错</span></span><br><span class="line">journalctl --user -u rclone-laboratory-sync.service -e</span><br><span class="line"><span class="comment"># 立刻手动触发一次同步</span></span><br><span class="line">systemctl --user start rclone-laboratory-sync.service</span><br></pre></td></tr></table></figure><h3 id="同步时忽略特定文件夹">同步时忽略特定文件夹</h3><p>像 <code>.git</code>, <code>.venv</code> 这种文件夹里面包含大量小文件，不方便同步；同时里面的这些小文件在不同的设备中也不兼容。因此，使用 Rclone 做同步的时候推荐将这些文件夹忽略。同时为了方便后续维护，可以设置一个专门的规则文件用于过滤（类似 <code>.gitignore</code>）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mkdir -p ~/.config/rclone</span><br><span class="line">nano ~/.config/rclone/filter-lab.txt</span><br></pre></td></tr></table></figure><p>加入需要忽略的文件：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"># === 版本控制 ===</span><br><span class="line">- .git/**</span><br><span class="line">- .svn/**</span><br><span class="line"></span><br><span class="line"># === 依赖与虚拟环境 ===</span><br><span class="line">- .venv/**</span><br><span class="line">- node_modules/**</span><br><span class="line"></span><br><span class="line"># === 缓存与构建产物 ===</span><br><span class="line">- __pycache__/**</span><br><span class="line">- *.py[cod]</span><br><span class="line">- .pytest_cache/**</span><br><span class="line">- *.class</span><br><span class="line">- *.o</span><br><span class="line">- *.out</span><br><span class="line"></span><br><span class="line"># === 编辑器与系统垃圾 ===</span><br><span class="line">- .vscode/**</span><br><span class="line">- .idea/**</span><br><span class="line">- *.swp</span><br><span class="line">- *~</span><br><span class="line">- .DS_Store</span><br><span class="line">- Thumbs.db</span><br><span class="line"></span><br><span class="line"># === 日志 ===</span><br><span class="line">- *.log</span><br></pre></td></tr></table></figure><p>用新规则重新建立连接：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rclone bisync /home/zxz/Data/laboratory Tbox:laboratory --filter-from /home/zxz/.config/rclone/filter-lab.txt --resync --resync-mode path1 -v</span><br></pre></td></tr></table></figure><p>修改 <code>~/.config/systemd/user/rclone-laboratory-sync.service</code> 文件，将 <code>ExecStart</code> 行改为：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">ExecStart</span>=/usr/bin/rclone bisync /home/zxz/Data/laboratory Tbox:laboratory --filter-from /home/zxz/.config/rclone/filter-lab.txt -v</span><br></pre></td></tr></table></figure><p>重新加载 systemd 配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl --user daemon-reload</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/811636f.html</id>
    <link href="https://zhuxz0299.github.io/posts/811636f.html"/>
    <published>2026-03-03T12:02:55.000Z</published>
    <summary>记录一下 Rclone 的配置方式与用法</summary>
    <title>Rclone 使用基础</title>
    <updated>2026-04-27T13:54:50.599Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="System Knowledge" scheme="https://zhuxz0299.github.io/categories/System-Hardware/System-Knowledge/"/>
    <category term="wps" scheme="https://zhuxz0299.github.io/tags/wps/"/>
    <category term="font" scheme="https://zhuxz0299.github.io/tags/font/"/>
    <category term="strace" scheme="https://zhuxz0299.github.io/tags/strace/"/>
    <content>
      <![CDATA[<h2 id="问题描述">问题描述</h2><p>如果使用 WPS 打开 <code>.pptx</code> 文件，在打开第一个页面时会卡住将近一分钟：页面框架成功加载，但是 PPT 内容不显示，且无法关闭 WPS 页面。但是如果仅仅是打开 WPS 软件本身，加载速度却很快。除此之外，在打开某个 <code>.pptx</code> 文件之后再打开另一个 <code>.pptx</code> 文件，加载同样很快。</p><h2 id="问题分析">问题分析</h2><h3 id="strace-底层监视">strace 底层监视</h3><p>想要找到哪里出了问题，可以使用 <code>strace</code> 监视底层调用情况。首先运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">strace -T -e trace=connect,poll,openat,recvmsg wpp 1.22-进展汇报.pptx</span><br></pre></td></tr></table></figure><p>结果输出以下结果之后戛然而止：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">openat(AT_FDCWD, &quot;/etc/ld.so.cache&quot;, O_RDONLY|O_CLOEXEC) = 3 &lt;0.000014&gt;</span><br><span class="line"></span><br><span class="line">... (省略中间无关内容)</span><br><span class="line"></span><br><span class="line">openat(AT_FDCWD, &quot;/usr/bin/wpp&quot;, O_RDONLY) = 3 &lt;0.000007&gt;</span><br><span class="line"></span><br><span class="line">--- SIGCHLD &#123;si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=18891, si_uid=1000, si_status=0, si_utime=0, si_stime=0&#125; ---</span><br><span class="line"></span><br><span class="line">--- SIGCHLD &#123;si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=18892, si_uid=1000, si_status=0, si_utime=0, si_stime=0&#125; ---</span><br><span class="line"></span><br><span class="line">openat(AT_FDCWD, &quot;/dev/null&quot;, O_WRONLY|O_CREAT|O_TRUNC, 0666) = 3 &lt;0.000027&gt;</span><br></pre></td></tr></table></figure><p>根据 <code>openat(AT_FDCWD, &quot;/usr/bin/wpp&quot;, O_RDONLY) = 3</code> 加上后面的 <code>SIGCHLD</code>（子进程退出信号），说明 <code>/usr/bin/wpp</code> 不是 WPS 的核心主程序，它只是一个用 bash 写的启动外壳（Wrapper Script）。这就导致当这个 bash 脚本在底层启动真正的 WPS 二进制文件时，<code>strace</code> 直接跟丢了。</p><p>因此加上 <code>-f</code> (follow forks) 参数，跟踪子进程：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">strace -f -T -e trace=connect,poll,openat,recvmsg wpp 1.22-进展汇报.pptx</span><br></pre></td></tr></table></figure><p>在 WPS 卡住的时候观察输出内容，发现基本形如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line">[pid 20129] openat(AT_FDCWD, &quot;/usr/local/texlive/2025/texmf-dist/fonts/type1/uhc/umj/umj16.pfb&quot;, O_RDONLY) = 28 &lt;0.000101&gt;</span><br><span class="line">[pid 20129] openat(AT_FDCWD, &quot;/usr/local/texlive/2025/texmf-dist/fonts/type1/uhc/umj/umj17.pfb&quot;, O_RDONLY) = 28&lt;0.000028&gt;</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>得知 WPS 在打开 PPT 文件时大量的时间都在从系统中加载字体，且字体大部分来自于 TeX Live。</p><h3 id="WPS-字体引擎逻辑">WPS 字体引擎逻辑</h3><p>WPS 的字体引擎在首次打开文件时，它不完全信任系统 fontconfig 的缓存，而是会去物理遍历并逐个读取系统字体目录下的每一个文件来构建自己的内部内存树，而 TeX Live 自带了上万个极为零碎的专业排版字体文件，因此花了大量时间进行加载。</p><h2 id="解决方法">解决方法</h2><p>可以把 TeX Live 的字体从系统的全局字体搜索路径中踢出去，LaTeX 引擎（如 <code>xelatex</code>, <code>pdflatex</code>）有自己独立的路径搜索机制（Kpathsea），不需要把字体注册进系统也能正常编译文档。</p><p>因此首先找出 TeX Live 的系统字体配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep -ir <span class="string">&quot;texlive&quot;</span> /etc/fonts/</span><br></pre></td></tr></table></figure><p>发现为 <code>/etc/fonts/conf.d/09-texlive.conf</code>，然后将其删除或重命名为 <code>09-texlive.conf_backup</code>，再执行 <code>fc-cache -fv</code> 把 TeX 字体从缓存中清除。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/cae0e3e1.html</id>
    <link href="https://zhuxz0299.github.io/posts/cae0e3e1.html"/>
    <published>2026-03-02T09:37:09.000Z</published>
    <summary>解决用 WPS 打开 PPT 时加载时间过长的问题</summary>
    <title>WPS 打开时卡住</title>
    <updated>2026-04-27T13:58:09.007Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="Dev Tools" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/"/>
    <category term="Software" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/Software/"/>
    <category term="ai" scheme="https://zhuxz0299.github.io/tags/ai/"/>
    <category term="comfyui" scheme="https://zhuxz0299.github.io/tags/comfyui/"/>
    <category term="stable-diffusion" scheme="https://zhuxz0299.github.io/tags/stable-diffusion/"/>
    <category term="image-generation" scheme="https://zhuxz0299.github.io/tags/image-generation/"/>
    <content>
      <![CDATA[<h2 id="AI-绘图的底层逻辑">AI 绘图的底层逻辑</h2><p>要理解 ComfyUI 中的每一个节点，首先需要了解 Stable Diffusion 的工作原理。</p><h3 id="扩散模型：从噪声到图像">扩散模型：从噪声到图像</h3><p>Stable Diffusion 属于<strong>潜空间扩散模型（Latent Diffusion Model，LDM）</strong>。它的核心思想来自扩散过程：</p><ol><li><strong>正向扩散（加噪）</strong>：训练阶段，对一张真实图像反复添加高斯噪声，直到图像变成纯噪声。模型通过学习这个过程的逆操作来掌握&quot;如何去噪&quot;。</li><li><strong>反向扩散（去噪/推理）</strong>：推理阶段，从一张随机噪声图出发，模型通过若干步骤逐步预测并去除噪声，最终还原出一张清晰图像。</li></ol><p>之所以称为&quot;潜空间&quot;扩散，是因为整个去噪过程并不在像素空间进行，而是在一个维度更低的<strong>潜变量空间（Latent Space）</strong> 中完成，这大幅降低了计算量。最终再通过 VAE 解码器将潜变量映射回像素图像。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">随机噪声 (Latent)</span><br><span class="line">     │</span><br><span class="line">  [U-Net 去噪，共 N 步]  ← 由 CLIP 文本编码的条件引导</span><br><span class="line">     │</span><br><span class="line"> 干净的潜变量 (Latent)</span><br><span class="line">     │</span><br><span class="line">  [VAE 解码]</span><br><span class="line">     │</span><br><span class="line">  最终图像 (像素空间)</span><br></pre></td></tr></table></figure><h3 id="文本如何引导图像生成？">文本如何引导图像生成？</h3><p>模型在去噪时并不是&quot;盲目&quot;操作的，它需要一个<strong>条件信号</strong>来决定生成什么内容。这个条件信号就来自 <strong>CLIP 文本编码器</strong>。</p><p>CLIP（Contrastive Language-Image Pre-Training）是 OpenAI 提出的多模态模型，它能将文字和图像映射到同一个语义空间中。文本编码器将提示词（Prompt）转换为一个高维向量序列，U-Net 在每一步去噪时通过 <strong>Cross-Attention</strong> 机制参考这个向量，从而让去噪的方向朝着&quot;符合描述&quot;的图像收拢。</p><h3 id="CFG：无分类器引导">CFG：无分类器引导</h3><p>在推理时，模型实际上会同时进行两次预测：</p><ul><li>一次依据提示词（有条件）</li><li>一次不依据提示词（无条件，即负面提示词方向）</li></ul><p>最终的去噪方向由以下公式加权合并：</p><p><span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mover accent="true"><mi>ϵ</mi><mo>~</mo></mover><mo>=</mo><msub><mi>ϵ</mi><mtext>uncond</mtext></msub><mo>+</mo><mi>w</mi><mo>⋅</mo><mo stretchy="false">(</mo><msub><mi>ϵ</mi><mtext>cond</mtext></msub><mo>−</mo><msub><mi>ϵ</mi><mtext>uncond</mtext></msub><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex">\tilde{\epsilon} = \epsilon_{\text{uncond}} + w \cdot (\epsilon_{\text{cond}} - \epsilon_{\text{uncond}})</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6679em;"></span><span class="mord accent"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.6679em;"><span style="top:-3em;"><span class="pstrut" style="height:3em;"></span><span class="mord mathnormal">ϵ</span></span><span style="top:-3.35em;"><span class="pstrut" style="height:3em;"></span><span class="accent-body" style="left:-0.1944em;"><span class="mord">~</span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.7333em;vertical-align:-0.15em;"></span><span class="mord"><span class="mord mathnormal">ϵ</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.3361em;"><span style="top:-2.55em;margin-left:0em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord text mtight"><span class="mord mtight">uncond</span></span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">+</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.4445em;"></span><span class="mord mathnormal" style="margin-right:0.0269em;">w</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⋅</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mopen">(</span><span class="mord"><span class="mord mathnormal">ϵ</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.3361em;"><span style="top:-2.55em;margin-left:0em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord text mtight"><span class="mord mtight">cond</span></span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord"><span class="mord mathnormal">ϵ</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.3361em;"><span style="top:-2.55em;margin-left:0em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord text mtight"><span class="mord mtight">uncond</span></span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.15em;"><span></span></span></span></span></span></span><span class="mclose">)</span></span></span></span></span></p><p>其中 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>w</mi></mrow><annotation encoding="application/x-tex">w</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.0269em;">w</span></span></span></span> 就是 <strong>CFG Scale</strong>（即 K 采样器中的 <code>cfg</code> 参数）。<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>w</mi></mrow><annotation encoding="application/x-tex">w</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.0269em;">w</span></span></span></span> 越大，生成结果越贴近提示词，但也可能出现过饱和或失真；<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>w</mi></mrow><annotation encoding="application/x-tex">w</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.0269em;">w</span></span></span></span> 越小，图像越自由、发散。</p><h2 id="最简单的工作流解析">最简单的工作流解析</h2><p>下图展示了 ComfyUI 中最基础的文生图（txt2img）工作流：</p><pre><code class="highlight mermaid">graph LR    %% 节点定义    Ckpt[&quot;Checkpoint加载器 (简易)&lt;br/&gt;v1-5-pruned-emaonly...&quot;]    ClipPos[&quot;CLIP文本编码 (正面)&lt;br/&gt;beautiful scenery nature glass bottle...&quot;]    ClipNeg[&quot;CLIP文本编码 (负面)&lt;br/&gt;text, watermark&quot;]    EmptyLatent[&quot;空Latent图像&lt;br/&gt;512x512&quot;]    KSampler[&quot;K采样器&lt;br/&gt;euler / normal / 20步&quot;]    VAEDecode[&quot;VAE解码&quot;]    SaveImage[&quot;保存图像&lt;br/&gt;ComfyUI&quot;]    %% 连线关系 (大模型引出)    Ckpt -- &quot;模型&quot; --&gt; KSampler    Ckpt -- &quot;CLIP&quot; --&gt; ClipPos    Ckpt -- &quot;CLIP&quot; --&gt; ClipNeg    Ckpt -- &quot;VAE&quot; --&gt; VAEDecode    %% 连线关系 (提示词引出)    ClipPos -- &quot;条件 (正面)&quot; --&gt; KSampler    ClipNeg -- &quot;条件 (负面)&quot; --&gt; KSampler    %% 连线关系 (潜空间图像引出)    EmptyLatent -- &quot;Latent&quot; --&gt; KSampler    %% 连线关系 (采样后处理)    KSampler -- &quot;Latent&quot; --&gt; VAEDecode    VAEDecode -- &quot;图像&quot; --&gt; SaveImage</code></pre><p>整个流程由 6 个节点构成，数据从左到右流动。下面逐一拆解每个节点的职责。</p><h3 id="Checkpoint-加载器">Checkpoint 加载器</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">模型文件：v1-5-pruned-emaonly-fp16.safetensors</span><br><span class="line">输出：模型 / CLIP / VAE</span><br></pre></td></tr></table></figure><p><strong>Checkpoint</strong> 是整个生成系统的核心，它是一个&quot;大模型包&quot;，内部集成了三个子模块：</p><table><thead><tr><th>子模块</th><th>作用</th></tr></thead><tbody><tr><td><strong>U-Net</strong></td><td>去噪网络，负责在潜空间中执行每一步去噪</td></tr><tr><td><strong>CLIP 文本编码器</strong></td><td>将提示词转换为语义向量</td></tr><tr><td><strong>VAE</strong></td><td>在像素空间与潜空间之间进行编解码</td></tr></tbody></table><p>Checkpoint 加载后会将这三者分别输出，供后续节点使用。文件名中的 <code>fp16</code> 表示使用半精度浮点数存储，节省显存；<code>pruned</code> 表示裁剪了训练相关权重，仅保留推理权重；<code>ema</code> 是 Exponential Moving Average 权重，推理质量更好。</p><blockquote><p>Checkpoint 文件通常较大（2 GB～7 GB），存放于 <code>models/checkpoints/</code> 目录。</p></blockquote><h3 id="CLIP-文本编码（正向-负向）">CLIP 文本编码（正向 &amp; 负向）</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">正向提示词：beautiful scenery nature glass bottle landscape, purple galaxy bottle,</span><br><span class="line">负向提示词：text, watermark</span><br><span class="line">输出：条件向量（Conditioning）</span><br></pre></td></tr></table></figure><p>工作流中有两个 CLIP 文本编码节点，分别对应<strong>正向提示词</strong>和<strong>负向提示词</strong>，两者都从 Checkpoint 的 CLIP 端口获取编码器。</p><ul><li><strong>正向提示词（Positive Prompt）</strong>：描述你希望图像中出现的内容。</li><li><strong>负向提示词（Negative Prompt）</strong>：描述你<strong>不希望</strong>出现的内容。借助前面提到的 CFG 机制，负向提示词将作为无条件方向，引导生成结果远离这些描述。</li></ul><p>两个节点的输出（条件向量）分别接入 K 采样器的&quot;正面条件&quot;与&quot;负面条件&quot;端口。</p><h3 id="空-Latent-图像">空 Latent 图像</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">宽度：512   高度：512   批量大小：1</span><br><span class="line">输出：Latent 张量</span><br></pre></td></tr></table></figure><p>这个节点创建一块<strong>随机初始化的噪声潜变量</strong>，作为去噪的起点。尺寸设置的是潜空间中的尺寸，实际对应的像素分辨率需要乘以 VAE 的压缩倍率（SD 1.x 中为 8 倍），因此 <code>512×512</code> 的 Latent 对应最终 <code>512×512</code> 的像素图像（潜空间为 <code>64×64</code>）。</p><p>批量大小（Batch Size）控制一次生成几张图。</p><h3 id="K-采样器（KSampler）">K 采样器（KSampler）</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">种子：156680208700286    步数：20</span><br><span class="line">cfg：8.0                采样器：euler</span><br><span class="line">调度器：normal          降噪：1.00</span><br></pre></td></tr></table></figure><p>K 采样器是整个工作流的<strong>核心执行引擎</strong>，它将所有输入汇聚在一起，驱动去噪过程：</p><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><strong>种子（Seed）</strong></td><td>随机数种子，相同种子在其他条件不变时可复现结果</td></tr><tr><td><strong>步数（Steps）</strong></td><td>去噪迭代次数。步数越多细节越丰富，但耗时增加；通常 20～30 步足够</td></tr><tr><td><strong>CFG Scale</strong></td><td>文本引导强度，见上文公式。常用范围 6～12</td></tr><tr><td><strong>采样器（Sampler）</strong></td><td>去噪算法，如 <code>euler</code>、<code>dpm++2m</code>、<code>ddim</code> 等，影响图像风格与收敛速度</td></tr><tr><td><strong>调度器（Scheduler）</strong></td><td>控制每步噪声衰减的曲线，如 <code>normal</code>、<code>karras</code>，与采样器配合使用</td></tr><tr><td><strong>降噪（Denoise）</strong></td><td>去噪强度，<code>1.0</code> 表示从纯噪声开始（txt2img）；图像精修（img2img）时可设为 0.5～0.8</td></tr></tbody></table><p>K 采样器完成后输出一个&quot;干净&quot;的潜变量，传递给 VAE 解码器。</p><h3 id="VAE-解码">VAE 解码</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：Latent + VAE</span><br><span class="line">输出：像素图像</span><br></pre></td></tr></table></figure><p><strong>VAE（Variational Autoencoder，变分自编码器）</strong> 承担空间转换的职责：</p><ul><li><strong>VAE 编码器（Encoder）</strong>：将像素图像压缩为潜变量（用于 img2img 场景）。</li><li><strong>VAE 解码器（Decoder）</strong>：将潜变量还原为像素图像（本工作流使用此步骤）。</li></ul><p>SD 1.x 中 VAE 将图像压缩为原来的 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mfrac><mn>1</mn><mn>8</mn></mfrac></mrow><annotation encoding="application/x-tex">\frac{1}{8}</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1.1901em;vertical-align:-0.345em;"></span><span class="mord"><span class="mopen nulldelimiter"></span><span class="mfrac"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.8451em;"><span style="top:-2.655em;"><span class="pstrut" style="height:3em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">8</span></span></span></span><span style="top:-3.23em;"><span class="pstrut" style="height:3em;"></span><span class="frac-line" style="border-bottom-width:0.04em;"></span></span><span style="top:-3.394em;"><span class="pstrut" style="height:3em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">1</span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.345em;"><span></span></span></span></span></span><span class="mclose nulldelimiter"></span></span></span></span></span>，即 <code>512×512</code> 像素 → <code>64×64</code> 潜变量。解码时，VAE 会补全潜变量中没有表达的高频细节，因此 VAE 的质量直接影响图像的色彩准确性和细节锐度。</p><blockquote><p>Checkpoint 内置了 VAE，但也可以单独加载外部 VAE 文件（存放于 <code>models/vae/</code>）来替换，以改善某些模型默认 VAE 色彩偏灰的问题。</p></blockquote><h3 id="保存图像">保存图像</h3><p>将解码后的像素图像写入磁盘，文件名前缀默认为 <code>ComfyUI</code>。</p><h2 id="ComfyUI-模型文件目录结构">ComfyUI 模型文件目录结构</h2><p>ComfyUI 将不同类型的模型文件按功能分类存放在 <code>models/</code> 目录的各个子文件夹中。</p><table><thead><tr><th style="text-align:left">文件夹名称</th><th style="text-align:left">中文简称 / 对应模型</th><th style="text-align:left">详细作用与存放的文件说明</th></tr></thead><tbody><tr><td style="text-align:left"><strong>audio_encoders</strong></td><td style="text-align:left">音频编码器</td><td style="text-align:left">存放处理音频输入的模型（如 Whisper 等），常用于音频驱动动画或嘴型同步等特殊工作流。</td></tr><tr><td style="text-align:left"><strong>checkpoints</strong></td><td style="text-align:left">主模型 / 大模型</td><td style="text-align:left">存放完整的生成模型（通常打包了 UNet、VAE 和 CLIP）。常见后缀为 <code>.safetensors</code> 或 <code>.ckpt</code>（如 SD 1.5, SDXL 的主模型）。</td></tr><tr><td style="text-align:left"><strong>clip</strong></td><td style="text-align:left">CLIP 文本编码器</td><td style="text-align:left">存放独立的 CLIP 文本编码模型。用于将你的提示词（Prompt）翻译成 AI 能理解的特征向量。</td></tr><tr><td style="text-align:left"><strong>clip_vision</strong></td><td style="text-align:left">CLIP 视觉编码器</td><td style="text-align:left">存放图像特征提取模型。常用于 IP-Adapter 工作流，让 AI 能够“看懂”你提供的参考图并提取其风格或特征。</td></tr><tr><td style="text-align:left"><strong>configs</strong></td><td style="text-align:left">配置文件</td><td style="text-align:left">存放 YAML 格式的模型结构配置文件。加载某些特殊架构或老版本模型时需要用到。</td></tr><tr><td style="text-align:left"><strong>controlnet</strong></td><td style="text-align:left">控制网模型</td><td style="text-align:left">存放 ControlNet 模型（如深度图、Canny 线稿、OpenPose 骨架控制等），用于对生成的构图和姿势进行精确引导。</td></tr><tr><td style="text-align:left"><strong>diffusers</strong></td><td style="text-align:left">Diffusers 格式模型</td><td style="text-align:left">存放以 Hugging Face Diffusers 文件夹结构（包含多个子文件夹和 <code>model_index.json</code>）保存的模型，而不是单文件模型。</td></tr><tr><td style="text-align:left"><strong>diffusion_models</strong></td><td style="text-align:left">独立扩散模型</td><td style="text-align:left">存放纯粹的扩散核心网络（如纯 UNet 或 DiT），不包含 VAE 和文本编码器。在最新的 Flux 或 SD3 工作流中经常用到。</td></tr><tr><td style="text-align:left"><strong>embeddings</strong></td><td style="text-align:left">文本反转 / 嵌入模型</td><td style="text-align:left">存放 Textual Inversion 模型（通常称为 TI 或 embeddings）。体积很小（KB 级别），常用于特定画风、人物特征或负面提示词（如 <code>EasyNegative</code>）。</td></tr><tr><td style="text-align:left"><strong>gligen</strong></td><td style="text-align:left">GLIGEN 模型</td><td style="text-align:left">存放 GLIGEN 权重文件，用于基于边界框（Bounding Box）进行高度精准的局部物体位置控制生成。</td></tr><tr><td style="text-align:left"><strong>hypernetworks</strong></td><td style="text-align:left">超网络模型</td><td style="text-align:left">早期的一种微调模型格式，类似于 LoRA 的前身。目前已被 LoRA 大量取代，较少使用。</td></tr><tr><td style="text-align:left"><strong>latent_upscale_models</strong></td><td style="text-align:left">潜空间放大模型</td><td style="text-align:left">存放专门在 Latent（潜空间）层面进行分辨率放大的算法模型。</td></tr><tr><td style="text-align:left"><strong>loras</strong></td><td style="text-align:left">LoRA 微调模型</td><td style="text-align:left">存放低秩适应模型。体积通常在几百 MB，用于在主模型基础上叠加特定的人物脸型、画风、服装或概念。</td></tr><tr><td style="text-align:left"><strong>model_patches</strong></td><td style="text-align:left">模型补丁</td><td style="text-align:left">存放用于修补或动态修改底层模型网络结构的文件（较为少见的高级用法）。</td></tr><tr><td style="text-align:left"><strong>onnx</strong></td><td style="text-align:left">ONNX 格式模型</td><td style="text-align:left">存放转换为 ONNX 格式的模型，通常配合特定节点用于 TensorRT 硬件加速计算。</td></tr><tr><td style="text-align:left"><strong>photomaker</strong></td><td style="text-align:left">PhotoMaker 模型</td><td style="text-align:left">存放专用于 PhotoMaker 节点的模型，常用于高质量的人物 ID 保持和换脸/面部定制工作流。</td></tr><tr><td style="text-align:left"><strong>sams</strong></td><td style="text-align:left">SAM 分割模型</td><td style="text-align:left">存放 Segment Anything Model 模型，用于在图像中进行极为精确的自动抠图和蒙版（Mask）生成。</td></tr><tr><td style="text-align:left"><strong>style_models</strong></td><td style="text-align:left">风格模型</td><td style="text-align:left">存放专门的风格迁移模型（如特定的 Style Adapter 等），用于提取和应用参考图的艺术风格。</td></tr><tr><td style="text-align:left"><strong>text_encoders</strong></td><td style="text-align:left">文本编码器</td><td style="text-align:left">类似于 <code>clip</code> 文件夹，但通常用于存放体积更庞大的独立文本模型（例如 SD3 或 Flux 使用的 <code>t5xxl</code> 模型文件）。</td></tr><tr><td style="text-align:left"><strong>ultralytics</strong></td><td style="text-align:left">目标检测模型</td><td style="text-align:left">存放基于 YOLO 等架构的检测模型，常用于自动检测人脸、手部（配合类似于 ADetailer 的局部重绘工作流使用）。</td></tr><tr><td style="text-align:left"><strong>unet</strong></td><td style="text-align:left">UNet 模型</td><td style="text-align:left">作用与 <code>diffusion_models</code> 类似，专门用于存放提取出来的单文件 UNet 结构模型。</td></tr><tr><td style="text-align:left"><strong>upscale_models</strong></td><td style="text-align:left">像素级放大模型</td><td style="text-align:left">存放常规的高清放大算法模型（如 ESRGAN, SwinIR, RealESRGAN 等）。它们直接作用于最终生成的像素图像。</td></tr><tr><td style="text-align:left"><strong>vae</strong></td><td style="text-align:left">变分自编码器</td><td style="text-align:left">存放单独的 VAE 模型。它的作用像翻译官，负责在 AI 计算的“潜空间（Latent）”和你肉眼能看到的“像素图像”之间进行转换（解码/编码）。</td></tr><tr><td style="text-align:left"><strong>vae_approx</strong></td><td style="text-align:left">近似 VAE 模型</td><td style="text-align:left">极轻量级的 VAE 模型。主要配合某些节点，让你在生图的过程中能快速预览潜空间中正在生成的模糊图像。</td></tr></tbody></table><h2 id="小结">小结</h2><p>通过最简单的 txt2img 工作流，可以梳理出 Stable Diffusion 的完整推理链路：</p><blockquote><p>文本提示词 → CLIP → 条件向量 → K 采样器（U-Net × N 步）→ 干净 Latent → VAE 解码 → 像素图像</p></blockquote><p>ComfyUI 以节点图的形式将这条链路完全暴露出来，每个节点对应一个明确的计算模块。理解了各模块的职责，就能在此基础上灵活组合更复杂的工作流，例如加入 ControlNet 控制构图、使用 LoRA 调整风格、或通过 img2img 对已有图像进行精修。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/25bc50da.html</id>
    <link href="https://zhuxz0299.github.io/posts/25bc50da.html"/>
    <published>2026-02-28T15:28:38.000Z</published>
    <summary>从底层原理出发，结合最基础的工作流，逐步拆解 AI 绘图的各个组件，并介绍 ComfyUI 的模型文件管理方式。</summary>
    <title>ComfyUI 入门与基础概念学习</title>
    <updated>2026-04-28T06:26:41.613Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="Dev Tools" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/"/>
    <category term="Git" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/Git/"/>
    <category term="github" scheme="https://zhuxz0299.github.io/tags/github/"/>
    <category term="git" scheme="https://zhuxz0299.github.io/tags/git/"/>
    <content>
      <![CDATA[<div class="note info flat"><p>内容摘取自 <a href="https://git-scm.com/book/zh/v2">Pro Git</a> 这本书。</p></div><h2 id="Git-运行前的配置">Git 运行前的配置</h2><p><code>git</code> 的几个配置文件：</p><ul><li><code>/etc/gitconfig</code>，系统级配置，感觉平时用不到</li><li><code>~/.gitconfig</code>，用户级配置<ul><li><code>git config --global user.name xxx</code> 以及 <code>git config --global user.name xxx</code> 的设置会自动存到这里</li></ul></li><li><code>.git/config</code>，仓库级配置</li></ul><p>运行 <code>git config --list --show-origin</code> 可以查看所有的配置以及它们所在的文件。</p><h2 id="Git-基础">Git 基础</h2><h3 id="当前文件状态">当前文件状态</h3><p>工作目录下的文件可以分为<strong>已跟踪</strong> (tracked) 和<strong>未跟踪</strong> (untracked) 两种状态，已跟踪的文件是指那些被纳入了版本控制的文件，在上一次快照中有它们的记录，在工作一段时间后，它们的状态可能是未修改，已修改或已放入暂存区。简而言之，已跟踪的文件就是 Git 已经知道的文件。</p><p>下面这张图展示了并未被 commit 到仓库的文件的不同状态。</p><div align=center><img src='../../figure/git_/Git-用法学习/image.webp' width=600></div><p>通过 <code>git status</code> 可以查询当前工作目录下的文件状态。</p><h3 id="移除文件与停止-track">移除文件与停止 track</h3><p>假如需要在从工作目录中移除某个文件，可以使用 <code>rm &lt;file_name&gt;</code> + <code>git add &lt;file_name&gt;</code> 的操作，不过也可以直接使用 <code>git rm &lt;file_name&gt;</code>。相比前者，<code>git rm &lt;file_name&gt;</code> 会在删除前检查该文件是否处于 Modified 或者 Staged 的状态，用于防止误删尚未保存的数据。如果依然希望强制删除，则需要使用参数 <code>-f</code>。</p><p>但是如果希望文件继续留在磁盘，但仅仅是不希望 Git 继续 track，那么可以加上 <code>--cached</code> 参数，即：<code>git rm --cached &lt;file_name&gt;</code>；然后再将文件名写入 <code>.gitignore</code>。</p><div class="note warning flat"><p>使用了 <code>git rm --cached</code> 之后，之前被 Git 快照记录的内容依然存在，仅仅是之后不会再 track。</p></div><h3 id="撤销操作">撤销操作</h3><p>有时候 <code>commit</code> 完了才发现漏掉了几个文件没有添加，或者 <code>commit</code> 信息写错了。此时，可以运行带有 <code>--amend</code> 选项的提交命令来重新提交：<code>git commit --amend</code>，并且可以修改<code>commit</code> 的信息。</p><p>例如，提交后发现忘记了暂存某些需要的修改，可以像下面这样操作：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git commit -m <span class="string">&#x27;initial commit&#x27;</span></span><br><span class="line">git add forgotten_file</span><br><span class="line">git commit --amend</span><br></pre></td></tr></table></figure><p>如果往 <code>commit</code> 增加文件的同时不需要修改信息，可以使用 <code>git commit --amend --no-edit</code>。</p><h3 id="远程仓库的使用">远程仓库的使用</h3><h4 id="查看远程仓库">查看远程仓库</h4><p>如果想查看你已经配置的远程仓库服务器，可以运行 <code>git remote</code> 命令。如果你已经克隆了自己的仓库，那么至少应该能看到 <code>origin</code> —— 这是 Git 给你克隆的仓库服务器的默认名字。</p><p>也可以指定选项 <code>-v</code>，会显示需要读写远程仓库使用的 Git 保存的简写与其对应的 URL。例如 <code>git remote -v</code> 结果如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">origin https://github.com/schacon/ticgit (fetch)</span><br><span class="line">origin https://github.com/schacon/ticgit (push)</span><br></pre></td></tr></table></figure><h4 id="添加远程仓库">添加远程仓库</h4><p>运行 <code>git remote add &lt;shortname&gt; &lt;url&gt;</code> 添加一个新的远程 Git 仓库，同时指定一个方便使用的简写：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">$ git remote</span><br><span class="line">origin</span><br><span class="line">$ git remote add pb https://github.com/paulboone/ticgit</span><br><span class="line">$ git remote -v</span><br><span class="line">origin https://github.com/schacon/ticgit (fetch)</span><br><span class="line">origin https://github.com/schacon/ticgit (push)</span><br><span class="line">pb https://github.com/paulboone/ticgit (fetch)</span><br><span class="line">pb https://github.com/paulboone/ticgit (push)</span><br></pre></td></tr></table></figure><p>现在你可以在命令行中使用字符串 <code>pb</code> 来代替整个 URL。 例如，如果你想拉取 Paul 的仓库中有但你没有的信息，可以运行 <code>git fetch pb</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">$ git fetch pb</span><br><span class="line">remote: Counting objects: 43, done.</span><br><span class="line">remote: Compressing objects: 100% (36/36), done.</span><br><span class="line">remote: Total 43 (delta 10), reused 31 (delta 5)</span><br><span class="line">Unpacking objects: 100% (43/43), done.</span><br><span class="line">From https://github.com/paulboone/ticgit</span><br><span class="line">* [new branch] master -&gt; pb/master</span><br><span class="line">* [new branch] ticgit -&gt; pb/ticgit</span><br></pre></td></tr></table></figure><h4 id="从远程仓库中-pull-与-fetch">从远程仓库中 pull 与 fetch</h4><p>在 <code>clone</code> 一个仓库后，Git 会自动添加该远程仓库并命名为 <code>origin</code>，因此可以使用 <code>git fetch origin</code> 将仓库中新增信息下载到本地仓库——不过它不会自动合并或修改你当前的工作，而是需要手动 <code>merge</code>。</p><p>如果当前分支设置了 track 远程分支，那么可以直接 <code>git pull</code> 来自动抓取并合并远程分支到当前分支。默认情况下，<code>git clone</code> 命令会自动设置本地 <code>master</code> 分支 track 克隆的远程仓库的 <code>master</code> 分支（或其它名字的默认分支）。</p><h4 id="查看某个远程仓库">查看某个远程仓库</h4><p>可以使用 <code>git remote show &lt;remote&gt;</code> 命令，例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">$ git remote show origin</span><br><span class="line">* remote origin</span><br><span class="line">  Fetch URL: https://github.com/schacon/ticgit</span><br><span class="line">  Push URL: https://github.com/schacon/ticgit</span><br><span class="line">  HEAD branch: master</span><br><span class="line">  Remote branches:</span><br><span class="line">    master tracked</span><br><span class="line">    dev-branch tracked</span><br><span class="line">  Local branch configured for &#x27;git pull&#x27;:</span><br><span class="line">    master merges with remote master</span><br><span class="line">  Local ref configured for &#x27;git push&#x27;:</span><br><span class="line">    master pushes to master (up to date)</span><br></pre></td></tr></table></figure><p>上述信息说明当前处于 <code>master</code> 分支，并且如果运行 <code>git pull</code>，就会抓取所有的远程引用，然后将远程 <code>master</code> 分支合并到本地 <code>master</code> 分支。</p><h3 id="打标签">打标签</h3><h4 id="附注标签">附注标签</h4><p>例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git tag -a v1.4 -m <span class="string">&quot;my version 1.4&quot;</span></span><br></pre></td></tr></table></figure><p>由于没有指定 Commit ID，因此 Git 默认是给 HEAD 所在的 commit 打上标签。</p><ul><li><code>-a</code> 参数表示 Annotated Tag，即这是个附注标签</li><li><code>-m</code> 参数和 commit message 的类似，表示后面的是对这个 tag 的说明信息。</li></ul><h4 id="轻量标签">轻量标签</h4><p>不需要各种参数，直接 <code>git tag &lt;tag_name&gt;</code> 就行。</p><h4 id="给特定的-commit-打标签">给特定的 commit 打标签</h4><p>假如提交历史如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">$ git log --pretty=oneline</span><br><span class="line">4682c3261057305bdd616e23b64b0857d832627b added a todo file</span><br><span class="line">166ae0c4d3f420721acbb115cc33848dfcc2121a started write support</span><br><span class="line">9fceb02d0ae598e95dc970b74767f19372d61af8 updated rakefile</span><br><span class="line">964f16d36dfccde844893cac5b347e7b3d44abbc commit the todo</span><br><span class="line">8a5cbc430f1a9c3d00faaeffd07798508422908a updated readme</span><br></pre></td></tr></table></figure><p>希望给 <code>updated rakefile</code> 对应的 commit 加上 <code>v1.2</code> 的标签，可以在 <code>git tag</code> 的时候指定 commit 的校验和（或部分校验和）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git tag -a v1.2 9fceb02</span><br></pre></td></tr></table></figure><h2 id="Git-分支">Git 分支</h2><p>Git 保存的不是文件的变化或者差异，而是一系列不同时刻的<strong>快照</strong>；而在进行提交操作时，Git 会保存一个 commit 对象 (commit object)。commit 对象会包含一个指向暂存内容快照的指针，除此之外还包含了作者的姓名和邮箱、commit message 以及指向它的父对象的指针。</p><p>举个例子：<br>假设现在有一个工作目录，里面包含了三个将要被暂存和提交的文件。暂存操作会为每一个文件计算校验和（给出哈希值），然后会把当前版本的文件快照保存到 Git 仓库中 （Git 使用 blob 对象来保存它们），最终将校验和加入到暂存区域等待提交：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git add README test.rb LICENSE</span><br><span class="line">git commit -m <span class="string">&#x27;The initial commit of my project&#x27;</span></span><br></pre></td></tr></table></figure><p>当使用 <code>git commit</code> 进行提交操作时，Git 会先计算每一个子目录（本例中只有项目根目录）的校验和， 然后在 Git 仓库中将这些校验和保存为 tree 对象。随后，Git 创建一个 commit 对象，它包含指向这个 tree 对象（项目根目录）的指针。</p><div align=center><img src='../../figure/git_/Git-用法学习/image-1.webp' width=600></div><p>做些修改后再次提交，那么这次产生的提交对象会包含一个指向上次提交对象（父对象）的指针。</p><div align=center><img src='../../figure/git_/Git-用法学习/image-2.webp' width=600></div><p>Git 的分支，其实本质上仅仅是指向提交对象的可变指针。Git 的默认分支名字是 master，master 分支会在每次提交时自动向前移动。</p><div align=center><img src='../../figure/git_/Git-用法学习/image-3.webp' width=450></div><h3 id="分支的创建、切换与合并">分支的创建、切换与合并</h3><p>使用 <code>git branch</code> 可以看到当前本地的所有分支；使用 <code>git branch -v</code> 能够看到更详细的信息：包含每个分支的最后一个 commit。</p><h4 id="创建分支">创建分支</h4><p>例如创建一个 testing 分支：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git branch testing</span><br></pre></td></tr></table></figure><p>该操作实际上是在当前所在的 commit 对象上创建一个指针。那么，Git 又是怎么知道当前在哪一个分支上呢？这依靠一个名为 HEAD 的特殊指针，它指向当前所在的本地分支。</p><div align=center><img src='../../figure/git_/Git-用法学习/image-4.webp' width=450></div><p>同时从图上可以看出，<code>git branch</code> 命令仅仅创建一个新分支，HEAD 并不会自动切换到新分支中去。</p><h4 id="切换分支">切换分支</h4><p>要切换到一个已存在的分支，需要使用 <code>git checkout</code> 命令。运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git checkout testing</span><br></pre></td></tr></table></figure><p>那么有</p><div align=center><img src='../../figure/git_/Git-用法学习/image-5.webp' width=450></div><p>如果需要在创建分支的时候就进行切换，运行 <code>git checkout -b &lt;newbranchname&gt;</code> 即可。</p><h4 id="举个例子">举个例子</h4><p>来看一个简单的分支新建与分支合并的例子，实际工作中你可能会用到类似的工作流。 你将经历如下步骤：</p><ol><li>开发某个网站。</li><li>为实现某个新的用户需求，创建一个分支。</li><li>在这个分支上开展工作。</li></ol><p>工作的情况可能是这样，你需要解决网站的 #53 问题，所以创建了一个分支：</p><div align=center><img src='../../figure/git_/Git-用法学习/image-6.webp' width=450></div><p>然后执行了一些工作，使得分支向前推进：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">vim index.html</span><br><span class="line">git commit -a -m <span class="string">&#x27;added a new footer [issue 53]&#x27;</span></span><br></pre></td></tr></table></figure><div align=center><img src='../../figure/git_/Git-用法学习/image-7.webp' width=500></div><p>正在此时，你突然接到一个电话说有个很严重的问题需要紧急修补。你将按照如下方式来处理：</p><ol><li>切换到你的线上分支（production branch）。</li><li>为这个紧急任务新建一个分支，并在其中修复它。</li><li>在测试通过之后，切换回线上分支，然后合并这个修补分支，最后将改动推送到线上分支。</li><li>切换回你最初工作的分支上，继续工作。</li></ol><p>不过在执行以上操作之前，要留意工作目录和暂存区里那些还没有被提交的修改，它可能会和你即将 checkout 的分支产生冲突从而阻止 Git 切换到该分支。所以最好切换分支之前保持好一个干净的状态；或者通过贮藏 (stashing) 修补提交 (commit amending) 绕过这个问题。现在假设修改都已经被 commit，可以切回 master 分支并进行 bug 修复：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git checkout master</span><br><span class="line">git checkout -b hotfix</span><br><span class="line">vim index.html</span><br><span class="line">git commit -a -m <span class="string">&#x27;fixed the broken email address&#x27;</span></span><br></pre></td></tr></table></figure><p>然后状况如下：</p><div align=center><img src='../../figure/git_/Git-用法学习/image-8.webp' width=500></div><p>如果经过测试，发现修改是正确的，那么可以将 <code>hotfix</code> 分支合并回 <code>master</code> 分支来部署到线上：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">$ git checkout master</span><br><span class="line">$ git merge hotfix</span><br><span class="line">Updating f42c576..3a0874c</span><br><span class="line">Fast-forward</span><br><span class="line">index.html | 2 ++</span><br><span class="line">1 file changed, 2 insertions(+)</span><br></pre></td></tr></table></figure><p>由于你想要合并的分支 <code>hotfix</code> 所指向的 commit <code>C4</code> 是 <code>HEAD</code> 所在的 commit <code>C2</code> 的直接后继，这种情况下的合并操作没有需要解决的分歧，因此 Git 会直接将指针向前移动。这就叫做“快进 (fast-forward)”。</p><p>现在，最新的修改已经在 <code>master</code> 分支所指向的提交快照中，你可以着手发布该修复了。</p><div align=center><img src='../../figure/git_/Git-用法学习/image-9.webp' width=500></div><p>关于这个紧急问题的解决方案发布之后，你准备回到被打断之前时的工作中解决 #53 问题的那个分支。不过在此之前，你应该先删除 <code>hotfix</code> 分支，因为你已经不再需要它了 —— <code>master</code> 分支已经指向了同一个位置。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git branch -d hotfix <span class="comment"># 删除分支</span></span><br><span class="line">git checkout iss53</span><br><span class="line">vim index.html</span><br><span class="line">git commit -a -m <span class="string">&#x27;finished the new footer [issue 53]&#x27;</span></span><br></pre></td></tr></table></figure><div align=center><img src='../../figure/git_/Git-用法学习/image-10.webp' width=600></div><h4 id="分支合并">分支合并</h4><p>假设你已经修正了 #53 问题，并且打算将你的工作合并入 <code>master</code> 分支。为此，你需要合并 <code>iss53</code> 分支到 <code>master</code> 分支：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">$ git checkout master</span><br><span class="line">Switched to branch &#x27;master&#x27;</span><br><span class="line">$ git merge iss53</span><br><span class="line">Merge made by the &#x27;recursive&#x27; strategy.</span><br><span class="line">index.html | 1 +</span><br><span class="line">1 file changed, 1 insertion(+)</span><br></pre></td></tr></table></figure><p>这和之前合并 <code>hotfix</code> 分支的时候有点不一样。因为开发历史从一个更早的地方分叉开来（diverged），<code>master</code> 分支所在提交并不是 <code>iss53</code> 分支所在提交的直接祖先，Git 不得不做一些额外的工作：Git 会使用两个分支的末端所指的快照（<code>C4</code> 和 <code>C5</code>）以及这两个分支的公共祖先（<code>C2</code>），做一个简单的三方合并。</p><div align=center><img src='../../figure/git_/Git-用法学习/image-11.webp' width=600></div>并且 Git 将此次三方合并的结果做了一个新的快照，再自动创建一个新的 commit 指向它。<div align=center><img src='../../figure/git_/Git-用法学习/image-12.webp' width=600></div><p>如果 merge 发生了冲突，那么 Git 会修改冲突的文件，对冲突区域进行标记，形如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD:index.html</span><br><span class="line">&lt;div id=&quot;footer&quot;&gt;contact : email.support@github.com&lt;/div&gt;</span><br><span class="line">=======</span><br><span class="line">&lt;div id=&quot;footer&quot;&gt;</span><br><span class="line">  please contact us at support@github.com</span><br><span class="line">&lt;/div&gt;</span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; iss53:index.html</span><br></pre></td></tr></table></figure><p>用户需要手动处理这些冲突区域，并且通过 <code>git add</code> 来将其标记为冲突已解决。</p><h3 id="远程分支">远程分支</h3><p>远程引用是对远程仓库的引用（指针），包括分支、标签等等。你可以通过 <code>git ls-remote &lt;remote&gt;</code> 来显式地获得远程引用的完整列表， 或者通过 <code>git remote show &lt;remote&gt;</code> 获得远程分支的更多信息。</p><p>它们以 <code>&lt;remote&gt;/&lt;branch&gt;</code> 的形式命名。例如，如果你想要看你最后一次与远程仓库 <code>origin</code> 通信时 <code>master</code> 分支的状态，你可以查看 <code>origin/master</code> 分支。</p><p>来看一个例子。假设你的网络里有一个在 <a href="http://git.ourcompany.com">git.ourcompany.com</a> 的 Git 服务器。如果你从这里克隆：</p><div align=center><img src='../../figure/git_/Git-用法学习/image-13.webp' width=600></div><p>接下来你在本地的 <code>master</code> 分支做了一些工作，在同一段时间内有其他人推送提交到<br><a href="http://git.ourcompany.com">git.ourcompany.com</a> 并且更新了它的 <code>master</code> 分支，但如果不进行 <code>git fetch</code>，本地的 <code>origin/master</code> 指针就不会移动。</p><div align=center><img src='../../figure/git_/Git-用法学习/image-14.webp' width=600></div><p>只有运行了 <code>git fetch origin</code>，才会 更新本地数据库，移动 <code>origin/master</code> 指针到更新之后的位置。</p><div align=center><img src='../../figure/git_/Git-用法学习/image-15.webp' width=600></div><h4 id="推送">推送</h4><p>如果和别人一起在名为 <code>serverfix</code> 的分支上工作，可以运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push origin serverfix</span><br></pre></td></tr></table></figure><p>Git 自动将 <code>serverfix</code> 展开为 <code>refs/heads/serverfix:refs/heads/serverfix</code>，从而将本地的 <code>serverfix</code> 推送到 <code>origin</code> 的 <code>serverfix</code> 上。如果希望推送到其他分支上，可以运行 <code>git push origin serverfix:awesomebranch</code> 来将本地的 <code>serverfix</code> 分支推送到远程仓库上的 <code>awesomebranch</code> 分支。</p><h4 id="跟踪分支">跟踪分支</h4><p>从一个远程跟踪分支 checkout 一个本地分支会自动创建所谓的“跟踪分支”（它跟踪的分支叫做“上游分支”）。跟踪分支是与远程分支有直接关系的本地分支。 如果在一个跟踪分支上输入 <code>git pull</code>，Git 能自动地识别去哪个服务器上抓取、合并到哪个分支。</p><p><code>git clone</code> 一个仓库的时候，如果远程仓库 <code>origin</code> 的 HEAD 在 <code>master</code> 分支上，那么本地也会自动创建一个跟踪 <code>origin/master</code> 的 <code>master</code> 分支。</p><p>如果希望能在本地跟踪其他的分支，可以运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">git checkout -b &lt;branch&gt; &lt;remote&gt;/&lt;branch&gt;</span><br><span class="line"><span class="comment"># 或者是下面这个简写，本地会默认使用 &lt;branch&gt; 作为本地分支名称</span></span><br><span class="line">git checkout --track &lt;remote&gt;/&lt;branch&gt; </span><br><span class="line"><span class="comment"># 又或者是这个更简写的。这个的前提是 &lt;remote&gt; 有个名为 &lt;branch&gt; 的分支</span></span><br><span class="line">git checkout -b &lt;branch&gt; </span><br></pre></td></tr></table></figure><p>设置已有的本地分支跟踪一个刚刚拉取下来的远程分支，或者想要修改正在跟踪的上游分支，可以使用 <code>-u</code> 或 <code>--set-upstream-to</code> 选项显式地设置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git branch -u origin/serverfix</span><br></pre></td></tr></table></figure><p>就是将当前 <code>HEAD</code> 所在分支 track 到 <code>origin/serverfix</code> 分支上。或者如果希望将其他本地分支 track 到 <code>origin/serverfix</code> 上，可以运行更完整的</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git branch --set-upstream-to=origin/serverfix &lt;branch_name&gt;</span><br></pre></td></tr></table></figure><h2 id="Git-工具">Git 工具</h2><h3 id="子模块">子模块</h3><h4 id="开始使用子模块">开始使用子模块</h4><p>可以通过在 <code>git submodule add</code> 命令后面加上想要跟踪的项目的相对或绝对 URL 来添加新的子模块。 在本例中，我们将会添加一个名为 “DbConnector” 的库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git submodule add https://github.com/chaconinc/DbConnector</span><br></pre></td></tr></table></figure><p>默认情况下，子模块会将子项目放到一个与仓库同名的目录中，本例中是“DbConnector”。 如果你想要放到其他地方，那么可以在命令结尾添加一个不同的路径。</p><p>此时运行 <code>git status</code>，可以看到：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">On branch master</span><br><span class="line">Your branch is up-to-date with &#x27;origin/master&#x27;.</span><br><span class="line">Changes to be committed:</span><br><span class="line">  (use &quot;git reset HEAD &lt;file&gt;...&quot; to unstage)</span><br><span class="line">new file: .gitmodules</span><br><span class="line">new file: DbConnector</span><br></pre></td></tr></table></figure><p>首先是 <code>.gitmodules</code> 文件，该配置文件保存了项目 URL 与本地目录之间的映射：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[submodule &quot;DbConnector&quot;]</span><br><span class="line">  path = DbConnector</span><br><span class="line">  url = https://github.com/chaconinc/DbConnector</span><br></pre></td></tr></table></figure><p>然后虽然 <code>DbConnector</code> 是工作目录中的一个子目录，但 Git 还是会将它视作一个子模块。当你不在那个目录中时，Git 并不会跟踪它的内容，而是将它看作子模块仓库中的某个具体的 commit。</p><h4 id="克隆含有子模块的项目">克隆含有子模块的项目</h4><p>在克隆这样的项目时，默认会包含该子模块目录，但其中还没有任何文件。因此需要运行两个命令：<code>git submodule init</code> 用来初始化本地配置文件 <code>.git/config</code>；而 <code>git submodule update</code> 会先去子模块的远程仓库下载最新的修改，然后查看主项目里记录的 Commit ID，最后把子模块 checkout 到那个特定的 commit。</p><p>不过还有更简单一点的方式。如果给 <code>git clone</code> 命令传递 <code>--recurse-submodules</code> 参数，它就会自动初始化并更新仓库中的每一个子模块，包括可能存在的嵌套子模块。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/e3206b7d.html</id>
    <link href="https://zhuxz0299.github.io/posts/e3206b7d.html"/>
    <published>2026-02-11T14:06:00.000Z</published>
    <summary>系统性地学习一下 git 的常用用法，但是略过 git add 等最基础的东西以及被 IDE 图像化界面替代的记录查询操作。</summary>
    <title>Git 用法学习</title>
    <updated>2026-05-07T12:46:27.800Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="Dev Tools" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/"/>
    <category term="Software" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/Software/"/>
    <category term="yazi" scheme="https://zhuxz0299.github.io/tags/yazi/"/>
    <category term="tui" scheme="https://zhuxz0299.github.io/tags/tui/"/>
    <category term="filechooser" scheme="https://zhuxz0299.github.io/tags/filechooser/"/>
    <category term="file-manager" scheme="https://zhuxz0299.github.io/tags/file-manager/"/>
    <content>
      <![CDATA[<div class="note info flat"><p>基本安装与默认快捷键可直接参考 <a href="https://yazi-rs.github.io/docs/installation">Yazi 官方文档</a>，快捷键记不住时可以按 <code>F1</code> 查询。本文记录当前系统中相对默认配置所做的修改。</p></div><p>yazi 默认配置目录为 <code>~/.config/yazi/</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">~/.config/yazi/</span><br><span class="line">├── init.lua       # Lua 初始化与自定义 linemode</span><br><span class="line">├── keymap.toml    # 自定义快捷键</span><br><span class="line">├── package.toml   # ya 管理的插件依赖</span><br><span class="line">├── theme.toml     # 当前为空，使用默认主题</span><br><span class="line">└── yazi.toml      # 布局、打开规则与预览器</span><br></pre></td></tr></table></figure><p>这些文件只包含需要覆盖或追加的项。Yazi 升级后，未设置选项会自动使用新版默认值。</p><h2 id="在-Shell-中跟随-Yazi-切换目录">在 Shell 中跟随 Yazi 切换目录</h2><p>直接运行 <code>yazi</code> 时，退出后 Shell 仍会留在原目录。当前在 <code>~/.zshrc</code> 中定义了 <code>y</code> 函数，通过 <code>--cwd-file</code> 读取退出时的目录：</p><figure class="highlight zsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="function"><span class="title">y</span></span>() &#123;</span><br><span class="line"><span class="built_in">local</span> tmp=<span class="string">&quot;<span class="subst">$(mktemp -t <span class="string">&quot;yazi-cwd.XXXXXX&quot;</span>)</span>&quot;</span> cwd</span><br><span class="line"><span class="built_in">command</span> yazi <span class="string">&quot;<span class="variable">$@</span>&quot;</span> --cwd-file=<span class="string">&quot;<span class="variable">$tmp</span>&quot;</span></span><br><span class="line">IFS= <span class="built_in">read</span> -r -d <span class="string">&#x27;&#x27;</span> cwd &lt; <span class="string">&quot;<span class="variable">$tmp</span>&quot;</span></span><br><span class="line">[ <span class="string">&quot;<span class="variable">$cwd</span>&quot;</span> != <span class="string">&quot;<span class="variable">$PWD</span>&quot;</span> ] &amp;&amp; [ -d <span class="string">&quot;<span class="variable">$cwd</span>&quot;</span> ] &amp;&amp; <span class="built_in">builtin</span> <span class="built_in">cd</span> -- <span class="string">&quot;<span class="variable">$cwd</span>&quot;</span></span><br><span class="line"><span class="built_in">rm</span> -f -- <span class="string">&quot;<span class="variable">$tmp</span>&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>重新加载 Zsh 配置后，使用 <code>y</code> 进入 Yazi，退出时即可同步切换 Shell 的工作目录。</p><h2 id="界面、排序与打开规则">界面、排序与打开规则</h2><p><code>~/.config/yazi/yazi.toml</code> 中的基础部分如下：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[mgr]</span></span><br><span class="line"><span class="attr">ratio</span>    = [ <span class="number">3</span>, <span class="number">9</span>, <span class="number">8</span> ]</span><br><span class="line"><span class="attr">sort_by</span>  = <span class="string">&quot;natural&quot;</span></span><br><span class="line"><span class="attr">linemode</span> = <span class="string">&quot;size_and_mtime&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[opener]</span></span><br><span class="line"><span class="attr">edit</span> = [</span><br><span class="line">&#123; run = <span class="string">&quot;$&#123;EDITOR:-nvim&#125; %s&quot;</span>, desc = <span class="string">&quot;$EDITOR&quot;</span>, for = <span class="string">&quot;unix&quot;</span>, block = <span class="literal">true</span> &#125;,</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="section">[open]</span></span><br><span class="line"><span class="attr">prepend_rules</span> = [</span><br><span class="line"><span class="comment"># 强制将 .service 文件交给 nvim (edit) 处理</span></span><br><span class="line">&#123; url = <span class="string">&quot;*.service&quot;</span>, use = [ <span class="string">&quot;edit&quot;</span>, <span class="string">&quot;reveal&quot;</span> ] &#125;,</span><br><span class="line">]</span><br></pre></td></tr></table></figure><ul><li><code>ratio = [ 3, 9, 8 ]</code> 调整父目录、当前目录和预览窗格的宽度比例。</li><li><code>sort_by = &quot;natural&quot;</code> 使文件名按自然顺序排列，例如 <code>file2</code> 会排在 <code>file10</code> 前面。</li><li><code>edit</code> 优先使用 <code>$EDITOR</code>，没有设置时回退到 Neovim，并等待编辑器退出。</li><li><code>.service</code> 文件会优先使用 <code>edit</code> 和 <code>reveal</code> 规则。</li></ul><h3 id="同时显示文件大小与修改时间">同时显示文件大小与修改时间</h3><p>在 <code>~/.config/yazi/init.lua</code> 中定义 <code>size_and_mtime</code>，将大小和修改时间放在同一行：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Linemode:size_and_mtime</span><span class="params">()</span></span></span><br><span class="line"><span class="keyword">local</span> <span class="built_in">time</span> = <span class="built_in">math</span>.<span class="built_in">floor</span>(<span class="built_in">self</span>._file.cha.mtime <span class="keyword">or</span> <span class="number">0</span>)</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">time</span> == <span class="number">0</span> <span class="keyword">then</span></span><br><span class="line"><span class="built_in">time</span> = <span class="string">&quot;&quot;</span></span><br><span class="line"><span class="keyword">elseif</span> <span class="built_in">os</span>.<span class="built_in">date</span>(<span class="string">&quot;%Y&quot;</span>, <span class="built_in">time</span>) == <span class="built_in">os</span>.<span class="built_in">date</span>(<span class="string">&quot;%Y&quot;</span>) <span class="keyword">then</span></span><br><span class="line"><span class="built_in">time</span> = <span class="built_in">os</span>.<span class="built_in">date</span>(<span class="string">&quot;%b %d %H:%M&quot;</span>, <span class="built_in">time</span>)</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line"><span class="built_in">time</span> = <span class="built_in">os</span>.<span class="built_in">date</span>(<span class="string">&quot;%b %d  %Y&quot;</span>, <span class="built_in">time</span>)</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">local</span> size = <span class="built_in">self</span>._file:size()</span><br><span class="line"><span class="keyword">return</span> <span class="built_in">string</span>.<span class="built_in">format</span>(<span class="string">&quot;%s %s&quot;</span>, size <span class="keyword">and</span> ya.readable_size(size) <span class="keyword">or</span> <span class="string">&quot;-&quot;</span>, <span class="built_in">time</span>)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>当年文件显示“月日 + 时分”，其他年份的文件显示“月日 + 年份”；文件大小则通过 <code>ya.readable_size</code> 转换成易读格式。<code>yazi.toml</code> 中的 <code>linemode = &quot;size_and_mtime&quot;</code> 会将它设为默认模式。</p><h2 id="增加或覆盖快捷键">增加或覆盖快捷键</h2><p>根据官方文档 <a href="https://yazi-rs.github.io/docs/configuration/keymap">keymap.toml</a> 中的说明，当我们需要在默认快捷键的基础上增加快捷键时，可以使用 <code>prepend_keymap</code> 或者 <code>append_keymap</code> 增加前置或是后置快捷键。由于 Yazi 选择第一个匹配的键来运行，因此前置键始终比默认键具有更高的优先级，而后置键始终比默认键具有较低的优先级。</p><p>当前的 <code>~/.config/yazi/keymap.toml</code> 只包含两个前置快捷键：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[mgr]</span></span><br><span class="line"><span class="attr">prepend_keymap</span> = [</span><br><span class="line">  &#123; <span class="literal">on</span> = <span class="string">&quot;y&quot;</span>, run = [</span><br><span class="line">    <span class="string">&#x27;shell -- for path in %s; do echo &quot;file://$path&quot;; done | wl-copy -t text/uri-list&#x27;</span>,</span><br><span class="line">    <span class="string">&quot;yank&quot;</span></span><br><span class="line">  ], desc = <span class="string">&quot;Yank selected files (copy) and send to system clipboard&quot;</span> &#125;,</span><br><span class="line">  &#123; <span class="literal">on</span> = <span class="string">&quot;&lt;C-n&gt;&quot;</span>, run = <span class="string">&#x27;shell -- dragon-drop -a -x -i -T &quot;$@&quot;&#x27;</span> &#125;,</span><br><span class="line">]</span><br></pre></td></tr></table></figure><h3 id="y：同时复制到-Yazi-和系统剪贴板"><code>y</code>：同时复制到 Yazi 和系统剪贴板</h3><p>覆盖默认 <code>y</code> 后，会先把选中路径按 <code>text/uri-list</code> 格式写入 Wayland 剪贴板，再执行内置 <code>yank</code>。因此既能在 Yazi 中按 <code>p</code> 粘贴，也能粘贴到支持文件剪贴板的图形应用中。</p><p>该功能依赖 <code>wl-copy</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S wl-clipboard</span><br></pre></td></tr></table></figure><div class="note warning flat"><p>这段配置面向 Wayland；在 X11 环境下需要改用 <code>xclip</code> 或 <code>xsel</code>。</p></div><h3 id="Ctrl-n：将选中文件放入拖拽窗口"><code>Ctrl+n</code>：将选中文件放入拖拽窗口</h3><p>安装 <a href="https://github.com/mwh/dragon">dragon-drop</a> 后，在 Yazi 中选中一个或多个文件并按 <code>Ctrl+n</code>，会弹出一个可向其他应用拖拽文件的窗口。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S dragon-drop</span><br></pre></td></tr></table></figure><h2 id="预览压缩包与-Debian-软件包">预览压缩包与 Debian 软件包</h2><p>当前安装了两个预览相关插件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S ouch 7zip</span><br><span class="line">ya pkg add ndtoan96/ouch</span><br><span class="line">ya pkg add yazi-rs/plugins:piper</span><br></pre></td></tr></table></figure><p><code>yazi.toml</code> 中的配置为：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[plugin]</span></span><br><span class="line"><span class="attr">prepend_previewers</span> = [</span><br><span class="line">  &#123; url = <span class="string">&quot;*.deb&quot;</span>, run = <span class="string">&#x27;piper -- 7z l &quot;$1&quot;&#x27;</span> &#125;,</span><br><span class="line">  &#123; mime = <span class="string">&quot;application/&#123;*zip,tar,bzip2,7z*,rar,xz,zstd,java-archive&#125;&quot;</span>, run = <span class="string">&quot;ouch&quot;</span> &#125;,</span><br><span class="line">]</span><br></pre></td></tr></table></figure><ul><li><a href="https://github.com/ndtoan96/ouch.yazi">ouch.yazi</a> 调用 <code>ouch</code> 以树形结构展示常见压缩包的内容，可用 <code>J</code> 和 <code>K</code> 滚动预览区。</li><li><code>piper.yazi</code> 可将任意命令的输出作为预览内容。这里对 <code>.deb</code> 文件执行 <code>7z l</code>，用来查看软件包内的文件列表。</li></ul><h2 id="显示-Git-文件状态">显示 Git 文件状态</h2><p>当前还安装了官方 <code>git.yazi</code> 插件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ya pkg add yazi-rs/plugins:git</span><br></pre></td></tr></table></figure><p>在 <code>init.lua</code> 中初始化插件：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">require</span>(<span class="string">&quot;git&quot;</span>):setup(&#123;</span><br><span class="line"><span class="comment">-- Order of status signs showing in the linemode</span></span><br><span class="line">order = <span class="number">1500</span>,</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>再在 <code>yazi.toml</code> 中分别为文件和目录注册 fetcher：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[[plugin.prepend_fetchers]]</span></span><br><span class="line"><span class="attr">url</span>   = <span class="string">&quot;*&quot;</span></span><br><span class="line"><span class="attr">run</span>   = <span class="string">&quot;git&quot;</span></span><br><span class="line"><span class="attr">group</span> = <span class="string">&quot;git&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[[plugin.prepend_fetchers]]</span></span><br><span class="line"><span class="attr">url</span>   = <span class="string">&quot;*/&quot;</span></span><br><span class="line"><span class="attr">run</span>   = <span class="string">&quot;git&quot;</span></span><br><span class="line"><span class="attr">group</span> = <span class="string">&quot;git&quot;</span></span><br></pre></td></tr></table></figure><p>进入 Git 仓库后，文件列表会显示未跟踪、未暂存、已暂存、已删除等状态标记。当前 <code>theme.toml</code> 为空，因此使用插件的默认样式和字符。</p><h2 id="插件管理">插件管理</h2><p>上述三个插件会被记录到 <code>~/.config/yazi/package.toml</code>。可以用下列命令查看和更新：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ya pkg list</span><br><span class="line">ya pkg upgrade</span><br></pre></td></tr></table></figure><p>当前的插件列表为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ndtoan96/ouch</span><br><span class="line">yazi-rs/plugins:piper</span><br><span class="line">yazi-rs/plugins:git</span><br></pre></td></tr></table></figure><h2 id="将-Yazi-设为系统文件选择器">将 Yazi 设为系统文件选择器</h2><p>这里需要使用 <a href="https://github.com/hunkyburrito/xdg-desktop-portal-termfilechooser?">xdg-desktop-portal-termfilechooser</a>。首先进行安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">yay -S xdg-desktop-portal-termfilechooser-hunkyburrito-git</span><br></pre></td></tr></table></figure><p>安装完成后 <code>/usr/share/xdg-desktop-portal-termfilechooser/</code> 下会有一系列配置所需文件。复制 <code>config</code> 到 <code>~/.config/xdg-desktop-portal-termfilechooser/</code> 并修改为以下内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">[filechooser]</span><br><span class="line">cmd=yazi-wrapper.sh</span><br><span class="line">default_dir=$HOME</span><br><span class="line">; Uncomment to skip creating destination save files with instructions in them</span><br><span class="line">; create_help_file=0</span><br><span class="line">; Uncomment and edit the line below to change the terminal emulator command</span><br><span class="line">env=TERMCMD=ghostty -e</span><br><span class="line"></span><br><span class="line">; Mode must be one of &#x27;suggested&#x27;, &#x27;default&#x27;, or &#x27;last&#x27;.</span><br><span class="line">open_mode=suggested</span><br><span class="line">save_mode=suggested</span><br></pre></td></tr></table></figure><p>其中 <code>yazi-wrapper.sh</code> 也可以复制到 <code>~/.local/bin/</code> 中，这样方便调用。</p><p>再到 <code>~/.config/xdg-desktop-portal/portals.conf</code> 中加入如下内容，将 <code>termfilechooser</code> 设为默认文件选择器：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">[preferred]</span><br><span class="line">org.freedesktop.impl.portal.FileChooser=termfilechooser</span><br></pre></td></tr></table></figure><p>然后重启一下 portal即可使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl --user restart xdg-desktop-portal.service</span><br></pre></td></tr></table></figure>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/c55202f6.html</id>
    <link href="https://zhuxz0299.github.io/posts/c55202f6.html"/>
    <published>2026-02-07T16:45:51.000Z</published>
    <summary>记录 Yazi 的本机配置、插件与快捷键</summary>
    <title>yazi 文件管理器</title>
    <updated>2026-09-04T12:54:25.397Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="Dev Tools" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/"/>
    <category term="Software" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/Software/"/>
    <category term="python" scheme="https://zhuxz0299.github.io/tags/python/"/>
    <category term="uv" scheme="https://zhuxz0299.github.io/tags/uv/"/>
    <content>
      <![CDATA[<h2 id="uv-使用">uv 使用</h2><h3 id="项目环境管理">项目环境管理</h3><p>可以通过 <code>uv init &lt;proj-dir&gt;</code> 初始化项目，或者是进入项目文件夹后再运行 <code>uv init</code>。如果希望指定特定的 python 版本，例如 3.12，在初始化时加上 <code>--python 3.12</code> 即可。<br>uv 将创建以下文件：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">├── .gitignore</span><br><span class="line">├── .python-version</span><br><span class="line">├── README.md</span><br><span class="line">├── main.py</span><br><span class="line">└── pyproject.toml</span><br></pre></td></tr></table></figure><ul><li>假如项目中原先没有使用 git 管理，<code>uv init</code> 会自动调用 <code>git init</code> 操作；<code>.gitignore</code> 文件也仅在原本不存在的情况下自动创建。</li><li><code>README.md</code> 情况类似，一个项目通常总是会有 README 的，所以如果原本没有，uv 会自动创建</li><li><code>main.py</code> 感觉通常没啥用</li><li><code>.python-version</code> 包含项目的默认 Python 版本。此文件告诉 uv 在创建项目的虚拟环境时使用哪个 Python 版本。</li><li><code>pyproject.toml</code> 包含关于项目的元数据，主要是 python 依赖项，还可以包括项目的 description、License 之类的信息。对于 python 依赖的管理，可以手动编辑此文件，效果和 <code>uv add</code> 和 <code>uv remove</code> 等命令相同。</li></ul><p>刚刚完成初始化之后不会自动生成虚拟环境，只有在第一次运行项目命令（即 <code>uv run</code>、<code>uv sync</code> 或 <code>uv lock</code>）时，uv 才会项目根目录中创建一个虚拟环境和 <code>uv.lock</code> 文件。</p><p>但是如果使用 vscode 作为代码编辑器，vscode python 插件的 IntelliSense 总是希望指定一个 python 环境以方便语法检测，那么可以初始化完成之后先运行一次 <code>uv sync</code>，生成 <code>.venv</code> 文件夹及其环境；或者直接运行 <code>uv add &lt;python_package&gt;</code>，这样也可以自动创建环境。</p><h3 id="脚本运行">脚本运行</h3><p>如果并不是为了运行项目中的代码，而仅仅是需要临时运行某个 python 脚本（假设叫做 <code>example.py</code>），可以直接使用 <code>uv run example.py</code> 运行：当 uv 在当前目录下检测不到代表项目环境的 <code>pyproject.toml</code> 文件时，会自动创建一个临时的环境用于运行脚本。</p><h4 id="直接运行带依赖的脚本">直接运行带依赖的脚本</h4><p>如果运行的脚本包含某些依赖，例如依赖于 <code>numpy</code>，那么在运行脚本的时候可以使用</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv run --with numpy example.py</span><br></pre></td></tr></table></figure><p>uv 会在临时的环境中下载并使用 <code>numpy</code> 包。</p><h4 id="python-脚本管理">python 脚本管理</h4><p>uv 也可以像管理 python 项目那样管理一个脚本及其依赖。例如需要创建一个 python 脚本，并将 python 版本指定为 3.12：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv init --script example.py --python 3.12</span><br></pre></td></tr></table></figure><p>此时就会得到一个文件，内容为：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># /// script</span></span><br><span class="line"><span class="comment"># requires-python = &quot;&gt;=3.12&quot;</span></span><br><span class="line"><span class="comment"># dependencies = []</span></span><br><span class="line"><span class="comment"># ///</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">main</span>() -&gt; <span class="literal">None</span>:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Hello from example.py!&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p>如果这个脚本需要依赖于某些包，则可以运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uv add --script example.py <span class="string">&#x27;requests&lt;3&#x27;</span> <span class="string">&#x27;rich&#x27;</span></span><br></pre></td></tr></table></figure><p>将需要的包加上。此时脚本文件的注释区域将会变为：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># /// script</span></span><br><span class="line"><span class="comment"># dependencies = [</span></span><br><span class="line"><span class="comment">#   &quot;requests&lt;3&quot;,</span></span><br><span class="line"><span class="comment">#   &quot;rich&quot;,</span></span><br><span class="line"><span class="comment"># ]</span></span><br><span class="line"><span class="comment"># ///</span></span><br></pre></td></tr></table></figure><p>这部分 toml 格式的注释对于 python 脚本文件而言，就像 <code>pyproject.toml</code> 之于整个项目，都是用来管理 python 版本及其依赖的。有了上面的注释，运行脚本的时候就不需要使用 <code>--with</code> 参数再指定依赖，而是可以直接使用 <code>uv run example.py</code>。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/20b007b1.html</id>
    <link href="https://zhuxz0299.github.io/posts/20b007b1.html"/>
    <published>2026-02-04T06:22:54.000Z</published>
    <summary>记录一些频繁使用到的 uv 的操作</summary>
    <title>uv-python包与项目管理器</title>
    <updated>2026-04-27T06:07:11.835Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Network Practice" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Network-Practice/"/>
    <category term="linux" scheme="https://zhuxz0299.github.io/tags/linux/"/>
    <category term="tailscale" scheme="https://zhuxz0299.github.io/tags/tailscale/"/>
    <category term="vpn" scheme="https://zhuxz0299.github.io/tags/vpn/"/>
    <category term="wireguard" scheme="https://zhuxz0299.github.io/tags/wireguard/"/>
    <category term="strongswan" scheme="https://zhuxz0299.github.io/tags/strongswan/"/>
    <category term="ikev2" scheme="https://zhuxz0299.github.io/tags/ikev2/"/>
    <category term="windows" scheme="https://zhuxz0299.github.io/tags/windows/"/>
    <category term="tun" scheme="https://zhuxz0299.github.io/tags/tun/"/>
    <content>
      <![CDATA[<p>基本的工作原理其实非常简单，就是利用隧道技术实现数据的加密与传输，参见<a href="https://zhuxz0299.github.io/posts/769365ef.html#VPN-Virtual-Private-Network">网络层-VPN(Virtual Private Network)</a>。</p><h2 id="Linux-下-VPN-配置与工作">Linux 下 VPN 配置与工作</h2><h3 id="StrongSwan-配置">StrongSwan 配置</h3><p>交大 VPN 的<a href="https://net.sjtu.edu.cn/info/1200/3296.htm">官方文档</a>推荐使用 StrongSwan 软件配置 VPN 协议 IKEv2。首先安装 StrongSwan：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Arch Linux</span></span><br><span class="line"><span class="built_in">sudo</span> pacman -S strongswan</span><br><span class="line"></span><br><span class="line"><span class="comment"># Fedora</span></span><br><span class="line"><span class="built_in">sudo</span> dnf install strongswan</span><br></pre></td></tr></table></figure><p>下文以 Arch Linux 为例。Fedora 的配置内容相同，但软件包将所有配置统一放在 <code>/etc/strongswan/</code> 下：</p><ul><li>Arch Linux：连接、账号和证书位于 <code>/etc/swanctl/</code>，插件配置位于 <code>/etc/strongswan.d/</code>。</li><li>Fedora：对应目录分别为 <code>/etc/strongswan/swanctl/</code> 和 <code>/etc/strongswan/strongswan.d/</code>。</li></ul><h4 id="VPN-信息配置">VPN 信息配置</h4><p>在 Arch Linux 的 <code>/etc/swanctl/conf.d/</code> 下创建 <code>sjtuvpn.conf</code>；Fedora 对应路径为 <code>/etc/strongswan/swanctl/conf.d/sjtuvpn.conf</code>。文件内容为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line">connections &#123;</span><br><span class="line"> vpn-staff &#123;</span><br><span class="line">  vips = 0.0.0.0,::</span><br><span class="line">  remote_addrs = vpn.sjtu.edu.cn</span><br><span class="line">  send_certreq = no</span><br><span class="line">  local &#123;</span><br><span class="line">   auth = eap-peap</span><br><span class="line">   eap_id = myname</span><br><span class="line">   aaa_id = @radius.net.sjtu.edu.cn</span><br><span class="line">  &#125;</span><br><span class="line">  remote &#123;</span><br><span class="line">   auth = pubkey</span><br><span class="line">   id = %any</span><br><span class="line">  &#125;</span><br><span class="line">  children &#123;</span><br><span class="line">   vpn-staff &#123;</span><br><span class="line">    remote_ts = 202.120.0.0/16, 10.85.33.64/28, 111.186.0.0/16</span><br><span class="line">    esp_proposals = aes128-sha1-modp1024, aes128-sha2_256-modp1024, 3des-sha1-modp1024,default</span><br><span class="line">   &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  version = 2</span><br><span class="line">  mobike = no</span><br><span class="line">  proposals = aes128-sha1-modp1024, aes256-sha1-modp1024,3des-sha1-modp1024,default</span><br><span class="line"> &#125;</span><br><span class="line"> </span><br><span class="line"> vpn-student &#123;</span><br><span class="line">  vips = 0.0.0.0,::</span><br><span class="line">  remote_addrs = stu.vpn.sjtu.edu.cn</span><br><span class="line">  send_certreq = no</span><br><span class="line">  local &#123;</span><br><span class="line">   auth = eap-peap</span><br><span class="line">   eap_id = myname </span><br><span class="line">   aaa_id = @radius.net.sjtu.edu.cn</span><br><span class="line">  &#125;</span><br><span class="line">  remote &#123;</span><br><span class="line">   auth = pubkey</span><br><span class="line">   id = @stu.vpn.sjtu.edu.cn</span><br><span class="line">  &#125;</span><br><span class="line">  children &#123;</span><br><span class="line">   vpn-student &#123;</span><br><span class="line">    remote_ts = 202.120.0.0/16, 10.85.33.64/28, 111.186.0.0/16</span><br><span class="line">   &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  version = 2</span><br><span class="line">  mobike = no</span><br><span class="line"> &#125; </span><br><span class="line">&#125;</span><br><span class="line">secrets &#123;</span><br><span class="line"> eap-jaccount &#123;</span><br><span class="line">  id = myname</span><br><span class="line">  secret = &quot;mypassword&quot;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其中用户 jAccount 账号名假定为 myname，密码假定为 mypassword。</p><div class="note default flat"><p>配置文件里面一开始把整个交大的大内网 <code>10.0.0.0/8</code> 加入了分流，但是这个操作可能会导致某些利用大内网加速的服务（例如游戏下载可能会用到运营商的内网缓存）无法正常工作。所以设置了更加精确的分流策略。</p></div><p>然后是证书配置的问题，StrongSwan 不会像浏览器那样默认信任所有公网 CA，所以这部分权限会交给用户。所以可以手动添加对应证书（交大 VPN 使用的证书可能会随时间更新，所以为了防止过段时间就要手动新增一次证书，这里就把所有证书都添加了）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Arch Linux</span></span><br><span class="line"><span class="built_in">sudo</span> find /etc/ssl/certs -maxdepth 1 -<span class="built_in">type</span> l -name <span class="string">&#x27;*.pem&#x27;</span> -<span class="built_in">exec</span> <span class="built_in">ln</span> -sf &#123;&#125; /etc/swanctl/x509ca/ \;</span><br><span class="line"></span><br><span class="line"><span class="comment"># Fedora</span></span><br><span class="line"><span class="built_in">sudo</span> find /etc/pki/ca-trust/extracted/pem/directory-hash -maxdepth 1 -<span class="built_in">type</span> f -name <span class="string">&#x27;*.pem&#x27;</span> -<span class="built_in">exec</span> <span class="built_in">ln</span> -sfn &#123;&#125; /etc/strongswan/swanctl/x509ca/ \;</span><br></pre></td></tr></table></figure><div class="note warning flat"><p><a href="https://net.sjtu.edu.cn/info/1200/3296.htm">官方文档</a>以 Ubuntu 为例，在配置证书时使用的是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">rm</span> -f /etc/ipsec.d/cacerts/*</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">ln</span> -s /etc/ssl/certs/* /etc/ipsec.d/cacerts/</span><br></pre></td></tr></table></figure><p>现代的 swanctl 模式下，Arch Linux 的证书应当放在 <code>/etc/swanctl/x509ca/</code>，Fedora 则放在 <code>/etc/strongswan/swanctl/x509ca/</code>（虽然不知道为什么 <code>/etc/ipsec.d/cacerts/</code> 也能用）。<br>同时 <code>sudo ln -s /etc/ssl/certs/* /etc/ipsec.d/cacerts/</code> 直接链接整个目录，但是目录里除了证书还有 hash 形式的重复链接，全部塞进去没必要，可能只是增加重复加载和日志噪音。</p></div><p>最后加载 swanctl 配置；命令在两个系统上相同：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> swanctl --load-all</span><br></pre></td></tr></table></figure><h4 id="strongSwan-插件配置与启动">strongSwan 插件配置与启动</h4><p>然后启动 strongSwan 服务并设置开机自启：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start strongswan</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> strongswan  <span class="comment"># 开机自启</span></span><br></pre></td></tr></table></figure><p>如果启动时报插件相关错误，再到 Arch Linux 的 <code>/etc/strongswan.d/charon</code> 或 Fedora 的 <code>/etc/strongswan/strongswan.d/charon</code> 底下找到 <code>revocation.conf</code> 以及几个和 sql 相关的插件配置，在里面设置 <code>load = no</code>。这类修改影响的是 strongSwan 服务本身，因此改完以后需要重启服务，并重新加载 swanctl 配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl restart strongswan</span><br><span class="line"><span class="built_in">sudo</span> swanctl --load-all <span class="comment"># 重启 strongSwan 后，daemon 里的 swanctl 连接配置可能会被清掉</span></span><br></pre></td></tr></table></figure><div class="note warning flat"><p><a href="https://net.sjtu.edu.cn/info/1200/3296.htm">官方文档</a>在这里使用的是 <code>sudo ipsec restart</code>，不过前面既然已经使用 <code>/etc/swanctl/</code> 和 <code>swanctl</code>，这里就继续保持同一套写法：服务启停用 <code>systemctl</code>，配置加载和连接控制用 <code>swanctl</code>。</p></div><h4 id="连接-VPN">连接 VPN</h4><p>最后，如果开关 VPN 的命令记不住的话，可以写一个别名放在 <code>.zshrc</code> 里面：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">alias</span> sjtu-vpnup=<span class="string">&#x27;sudo swanctl -i --child vpn-student&#x27;</span></span><br><span class="line"><span class="built_in">alias</span> sjtu-vpndown=<span class="string">&#x27;sudo swanctl -t --ike vpn-student&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="StrongSwan-工作方式">StrongSwan 工作方式</h3><p>StrongSwan 默认是隐形的——它直接挂在物理网卡上，用内核的 XFRM 框架来截获数据包。</p><p>同时在上面的 <code>/etc/swanctl/conf.d/sjtuvpn.conf</code> 配置中，有一行 <code>remote_ts = 202.120.0.0/16, 10.85.33.64/28, 111.186.0.0/16</code>，这个就是 StrongSwan 设置的分流规则。<a href="https://net.sjtu.edu.cn/info/1200/3296.htm">官方文档</a>为了方便，原本直接设置 <code>remote_ts = 0.0.0.0/0,::/0</code> 让 VPN 代理所有流量，但是这会导致不同 VPN 之间的冲突，以及访问非校园网信息的卡顿。</p><h4 id="无线网卡新-IP">无线网卡新 IP</h4><p>通过 <code>ip addr show</code> 可以看到无线网卡上有两个 IP 地址：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">2: wlp0s20f3: &lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt; mtu 1500 qdisc noqueue state UP group default qlen 1000</span><br><span class="line">    link/ether 34:cf:f6:90:e7:c3 brd ff:ff:ff:ff:ff:ff</span><br><span class="line">    altname wlx34cff690e7c3</span><br><span class="line">    inet 10.131.147.51/24 brd 10.131.147.255 scope global dynamic noprefixroute wlp0s20f3</span><br><span class="line">       valid_lft 1875sec preferred_lft 1875sec</span><br><span class="line">    inet 111.186.54.51/32 scope global wlp0s20f3</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br><span class="line">    inet6 2001:da8:8000:7100::3:31/128 scope global nodad</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br><span class="line">    inet6 2409:891f:6b44:97e8:ba2e:f936:a167:2654/64 scope global dynamic noprefixroute</span><br><span class="line">       valid_lft 6365sec preferred_lft 6365sec</span><br><span class="line">    inet6 fe80::533e:d19:167a:b068/64 scope link noprefixroute</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br></pre></td></tr></table></figure><p>可以看到既有连接了手机点的地址 <code>10.131.147.51/24</code>，又有通过 VPN 获得的虚拟地址 <code>111.186.54.51/32</code>，这两个地址的选择依赖于路由表。</p><h4 id="路由表">路由表</h4><p>运行 <code>ip rule list</code> 可以查看系统中用到的路由表：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">0:      from all lookup local</span><br><span class="line">220:    from all lookup 220</span><br><span class="line">5210:   from all fwmark 0x80000/0xff0000 lookup main</span><br><span class="line">5230:   from all fwmark 0x80000/0xff0000 lookup default</span><br><span class="line">5250:   from all fwmark 0x80000/0xff0000 unreachable</span><br><span class="line">5270:   from all lookup 52</span><br><span class="line">32766:  from all lookup main</span><br><span class="line">32767:  from all lookup default</span><br></pre></td></tr></table></figure><p>左边数字越小，优先级越高。其中路由表 220 即为 StrongSwan 使用的路由表。</p><p>查看路由表：<code>ip route show table 220</code></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">10.85.33.64/28 dev wlp0s20f3 proto static src 111.186.54.51</span><br><span class="line">throw 10.131.147.0/24 proto static</span><br><span class="line">throw 100.90.131.9 proto static</span><br><span class="line">throw 100.94.155.95 proto static</span><br><span class="line">throw 100.100.100.100 proto static</span><br><span class="line">throw 100.108.53.75 proto static</span><br><span class="line">throw 100.118.249.39 proto static</span><br><span class="line">111.186.0.0/16 dev wlp0s20f3 proto static src 111.186.54.51</span><br><span class="line">throw 172.17.0.0/16 proto static</span><br><span class="line">throw 172.18.0.0/16 proto static</span><br><span class="line">202.120.0.0/16 dev wlp0s20f3 proto static src 111.186.54.51</span><br></pre></td></tr></table></figure><p>可以看出发往几个校内地址的流量都走了 <code>111.186.54.51</code>，而几个 <code>throw</code> 则是 StrongSwan 先查询了本机的其他网卡占用的网段，将其扔到后续的路由表中处理。</p><h4 id="XFRM-策略">XFRM 策略</h4><p>运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ip xfrm policy</span><br></pre></td></tr></table></figure><p>然后在输出结果中可以找到：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">src 111.186.54.51/32 dst 202.120.0.0/16</span><br><span class="line">        dir out priority 375423 ptype main</span><br><span class="line">        tmpl src 2409:891f:6b44:97e8:ba2e:f936:a167:2654 dst 2001:da8:8000:7100::7</span><br><span class="line">                proto esp spi 0xceb844a4 reqid 1 mode tunnel</span><br><span class="line">src 202.120.0.0/16 dst 111.186.54.51/32</span><br><span class="line">        dir fwd priority 375423 ptype main</span><br><span class="line">        tmpl src 2001:da8:8000:7100::7 dst 2409:891f:6b44:97e8:ba2e:f936:a167:2654</span><br><span class="line">                proto esp reqid 1 mode tunnel</span><br><span class="line">src 202.120.0.0/16 dst 111.186.54.51/32</span><br><span class="line">        dir in priority 375423 ptype main</span><br><span class="line">        tmpl src 2001:da8:8000:7100::7 dst 2409:891f:6b44:97e8:ba2e:f936:a167:2654</span><br><span class="line">                proto esp reqid 1 mode tunnel</span><br><span class="line">src 111.186.54.51/32 dst 111.186.0.0/16</span><br><span class="line">        dir out priority 375423 ptype main</span><br><span class="line">        tmpl src 2409:891f:6b44:97e8:ba2e:f936:a167:2654 dst 2001:da8:8000:7100::7</span><br><span class="line">                proto esp spi 0xceb844a4 reqid 1 mode tunnel</span><br><span class="line">src 111.186.0.0/16 dst 111.186.54.51/32</span><br><span class="line">        dir fwd priority 375423 ptype main</span><br><span class="line">        tmpl src 2001:da8:8000:7100::7 dst 2409:891f:6b44:97e8:ba2e:f936:a167:2654</span><br><span class="line">                proto esp reqid 1 mode tunnel</span><br><span class="line">src 111.186.0.0/16 dst 111.186.54.51/32</span><br><span class="line">        dir in priority 375423 ptype main</span><br><span class="line">        tmpl src 2001:da8:8000:7100::7 dst 2409:891f:6b44:97e8:ba2e:f936:a167:2654</span><br><span class="line">                proto esp reqid 1 mode tunnel</span><br><span class="line">src 111.186.54.51/32 dst 10.85.33.64/28</span><br><span class="line">        dir out priority 379519 ptype main</span><br><span class="line">        tmpl src 2409:891f:6b44:97e8:ba2e:f936:a167:2654 dst 2001:da8:8000:7100::7</span><br><span class="line">                proto esp spi 0xceb844a4 reqid 1 mode tunnel</span><br><span class="line">src 10.85.33.64/28 dst 111.186.54.51/32</span><br><span class="line">        dir fwd priority 379519 ptype main</span><br><span class="line">        tmpl src 2001:da8:8000:7100::7 dst 2409:891f:6b44:97e8:ba2e:f936:a167:2654</span><br><span class="line">                proto esp reqid 1 mode tunnel</span><br><span class="line">src 10.85.33.64/28 dst 111.186.54.51/32</span><br><span class="line">        dir in priority 379519 ptype main</span><br><span class="line">        tmpl src 2001:da8:8000:7100::7 dst 2409:891f:6b44:97e8:ba2e:f936:a167:2654</span><br><span class="line">                proto esp reqid 1 mode tunnel</span><br></pre></td></tr></table></figure><p>等内容，<code>tmpl</code> 表示对数据进行加密/解密操作。例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">src 111.186.54.51/32 dst 202.120.0.0/16</span><br><span class="line">        dir out priority 375423 ptype main</span><br><span class="line">        tmpl src 2409:891f:6b44:97e8:ba2e:f936:a167:2654 dst 2001:da8:8000:7100::7</span><br><span class="line">                proto esp spi 0xceb844a4 reqid 1 mode tunnel</span><br></pre></td></tr></table></figure><p>指的就是从本机 (111.186.54.51/32) 发往校园网公网 (202.120.0.0/16) 的内容，需要进行加密，并且通过 ipv6 传输。</p><h4 id="整体流程概述">整体流程概述</h4><div class="note info flat"><p>以下内容由 Gemini 3 Pro 整理生成</p></div><p>第一幕：诞生与寻路 (The Application &amp; Routing I)</p><ol><li><p>应用程序发起请求：你的浏览器创建一个 TCP 连接请求（SYN 包）。</p><ul><li>内容：“你好，我想连 <code>202.120.1.1</code> 的 <code>80</code> 端口。”</li><li>此时的状态：这是一个裸数据包，还没有确定源 IP。</li></ul></li><li><p>询问路由管理员 (<code>ip rule</code>)：内核网络栈接手这个包，开始查路由。</p><ul><li>内核问：“这个包该怎么走？”</li><li>IP Rule（管理员）看了一眼规则表：“优先级 220 说，先去查 Table 220。”</li></ul></li><li><p>查阅 Table 220 (<code>ip route show table 220</code>) 内核打开 220 号路由表：</p><ul><li>第一步（避让）：先看有没有 <code>throw</code> 规则（Tailscale, 本地 Wi-Fi）。<ul><li>目标是 <code>202.120.1.1</code>，不属于 <code>10.131.x.x</code>，也不属于 <code>100.x.x.x</code>。没有被扔出去。</li></ul></li><li>第二步（匹配）：找到了规则 <code>202.120.0.0/16 ... src 111.186.54.51</code>。</li><li>结论：“要去学校，得用虚拟身份（源 IP <code>111.186.54.51</code>），并准备从物理网卡 <code>wlp0s20f3</code> 出去。”</li></ul><p>此时数据包头部定型：</p><ul><li>Source: <code>111.186.54.51</code> (你的校内虚拟 IP)</li><li>Dest: <code>202.120.1.1</code> (教务处)</li><li>Payload: HTTP 请求 (明文)</li></ul></li></ol><p>第二幕：拦截与易容 (XFRM Interception &amp; Encapsulation)</p><ol start="4"><li><p>撞上安检口 (XFRM Policy Check)：就在数据包准备交给网卡驱动发送的前一微秒，它经过了 XFRM 检查点。</p><ul><li>内核拿着数据包的头部（源 <code>111.x</code>, 目的 <code>202.x</code>）去比对 XFRM 策略库。</li><li>命中！发现一条策略：<code>src 111.186.54.51 dst 202.120.0.0/16 dir out tmpl ...</code></li><li>动作：拦截！策略要求必须使用 ESP 隧道模式封装。</li></ul></li><li><p>封装与加密 (Transformation) 内核把这个明文包扣下，开始“打包”：</p><ul><li>加密：使用协商好的密钥（<code>AES-256-GCM</code>）把整个原始包（连头带尾）加密成一堆乱码。</li><li>加头：在乱码前面加上 ESP 头（IPsec 协议号 50）。</li><li>换皮（关键）：StrongSwan 根据连接配置，给这堆加密数据套上了一个全新的 IP 头部。</li></ul><p>此时数据包变成了“新包”：</p><ul><li>New Source: <code>2409:891f...</code> (你的物理 IPv6 地址，来自 Wi-Fi)</li><li>New Dest: <code>2001:da8...</code> (学校 VPN 服务器的 IPv6 地址)</li><li>Protocol: IPv6 (Next Header: ESP)</li><li>Payload: [ 加密后的乱码 (里面藏着刚才那个去教务处的包) ]</li></ul></li></ol><p>第三幕：二次寻路与发射 (Routing II &amp; Transmission)</p><ol start="6"><li><p>第二次询问路由 (<code>ip rule &amp; Main Table</code>) 现在内核手里拿的是一个要去“学校 VPN 服务器（公网 IPv6）”的包。它必须重新查一次路由。</p><ul><li>Table 220：里查不到去 <code>2001:da8...</code> 的路。</li><li>Table Main：查到了！这是公网流量，走默认网关（IPv6网关）。</li><li>结论：“从 <code>wlp0s20f3</code> 发给下一跳路由器。”</li></ul></li><li><p>物理发射</p><ul><li>数据包再次经过网卡出口。</li><li>XFRM 再次检查：源是物理 IP，目的是 VPN 服务器 IP。没有策略要求拦截这个公网包。</li><li>放行：网卡驱动将电信号（或无线电波）发射出去。</li></ul></li></ol><p>第四幕：彼岸 (The Other Side)</p><ol start="8"><li><p>在学校服务器端</p><ul><li>学校服务器收到这个 IPv6 包。</li><li>拆开 ESP 信封，解密，拿出了里面的 原始包（源 <code>111.x</code>，去 <code>202.x</code>）。</li><li>学校服务器发现你是自己人（源 IP 是合法的校内虚拟 IP），于是把它转发给教务处。</li></ul></li><li><p>回程（Reply）</p><ul><li>教务处回复数据给 <code>111.186.54.51</code>。</li><li>学校 VPN 服务器截获回复，加密，封包，发回给你的物理 IPv6 地址。</li><li>你的电脑收到包，XFRM 解密 (<code>dir in</code>)，露出原始数据。</li><li>浏览器收到 HTTP 响应，页面加载成功。</li></ul></li></ol><h3 id="Tailscale-工作方式">Tailscale 工作方式</h3><p>Tailscale 基本不需要手动配置，用户登录之后 Tailscale 会自动把 Wireguard 配置好。因此直接研究其工作方式。</p><h4 id="虚拟网卡-TUN">虚拟网卡 TUN</h4><p>相比 StrongSwan 在内核层工作，Tailscale 会创建一个虚拟网卡 <code>tailscale0</code>，凡是发给这个虚拟网卡的流量后续都会交给 Tailscale 软件处理。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">3: tailscale0: &lt;POINTOPOINT,MULTICAST,NOARP,UP,LOWER_UP&gt; mtu 1280 qdisc fq_codel state UNKNOWN group default qlen 500</span><br><span class="line">    link/none</span><br><span class="line">    inet 100.91.189.19/32 scope global tailscale0</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br><span class="line">    inet6 fd7a:115c:a1e0::2132:bd13/128 scope global</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br><span class="line">    inet6 fe80::5147:5beb:2ebc:ab9a/64 scope link stable-privacy proto kernel_ll</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br></pre></td></tr></table></figure><h4 id="路由表-2">路由表</h4><p>运行 <code>ip rule list</code> 得到的路由表中，优先级为 <code>5210</code>, <code>5230</code>, <code>5250</code>, <code>5270</code> 的几个都属于 Tailscale。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">0:      from all lookup local</span><br><span class="line">220:    from all lookup 220</span><br><span class="line">5210:   from all fwmark 0x80000/0xff0000 lookup main</span><br><span class="line">5230:   from all fwmark 0x80000/0xff0000 lookup default</span><br><span class="line">5250:   from all fwmark 0x80000/0xff0000 unreachable</span><br><span class="line">5270:   from all lookup 52</span><br><span class="line">32766:  from all lookup main</span><br><span class="line">32767:  from all lookup default</span><br></pre></td></tr></table></figure><div class="note primary flat"><p>Tailscale 的 rule 逻辑比较复杂，目的是为了配合 MagicDNS 和 Exit Node，不过我没有到，这里暂时先不深究。</p></div><p>运行 <code>ip route show table 52</code> 得到</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">100.90.131.9 dev tailscale0</span><br><span class="line">100.94.155.95 dev tailscale0</span><br><span class="line">100.100.100.100 dev tailscale0</span><br><span class="line">100.108.53.75 dev tailscale0</span><br><span class="line">100.118.249.39 dev tailscale0</span><br></pre></td></tr></table></figure><p>这里的几个 IP 就是用户拥有的设备的 Tailscale 的虚拟地址。（除了 <code>100.100.100.100</code> 是 MagicDNS）</p><h4 id="整体流程概述-2">整体流程概述</h4><div class="note info flat"><p>以下内容由 Gemini 3 Pro 整理生成</p></div><p>第一幕：被踢皮球的寻路</p><ol><li><p>应用程序发起<br>ping 程序请求连接 <code>100.90.131.9</code>。此时这是一个裸的 ICMP 数据包。</p></li><li><p>第一次过安检 (ip rule)<br>内核网络栈接手，开始查路由策略：</p><ul><li>优先级 <code>220</code> (StrongSwan)：先查 Table 220。由于 StrongSwan 的 bypass-lan 机制，发现针对该 IP 的 <code>throw</code> 规则，将包扔回，不予处理。</li><li>优先级 <code>5270</code> (Tailscale)：继续向下查，命中 Table 52。</li><li>查 Table 52：发现规则指向 <code>tailscale0</code> 设备。结论是将包发送至虚拟网卡。</li></ul></li></ol><p>第二幕：穿越传送门</p><ol start="3"><li><p>进入虚拟网卡 (TUN Interface)<br>数据包进入 tailscale0 接口。StrongSwan 是直接在物理网卡操作，而 Tailscale 使用 TUN 设备。</p></li><li><p>惊险一跃 (Context Switch)<br>数据包从内核空间转移到用户空间。后台运行的 <code>tailscaled</code> 进程接手该数据包。</p></li></ol><p>第三幕：暗房操作</p><ol start="5"><li><p>查表与加密<br><code>tailscaled</code> 进程在内存中处理数据：</p><ul><li>查通讯录：确认目标 IP 对应的物理地址和端口。</li><li>加密：使用 ChaCha20-Poly1305 算法加密 ICMP 包。</li><li>封装：将密文装进一个新的 UDP 数据包中。</li></ul></li><li><p>关键动作：打标记 (Fwmark)<br>在发送 UDP 包回内核前，<code>tailscaled</code> 给 socket 打上 <code>0x80000</code> 标记。这意味着这是已处理的合法包，要求系统直接放行。</p></li></ol><p>第四幕：持证通关</p><ol start="7"><li><p>重新注入内核<br><code>tailscaled</code> 将带有标记的 UDP 包交给操作系统发送。内核将其视为新包，重新进行路由查询。</p></li><li><p>触发 VIP 规则 (Rule 5210)<br>内核再次遍历 <code>ip rule</code>：</p><ul><li>优先级 5210 抢答：规则匹配到 <code>0x80000</code> 标记。</li><li>动作：指示直接查询 Main 表，跳过 StrongSwan 的 Table 220 和 Tailscale 的 Table 52。</li></ul></li><li><p>物理发送</p><ul><li>查 Main 表：找到默认网关。</li><li>防火墙：Netfilter/IPTables 识别标记，放行。</li><li>离去：UDP 包通过物理网卡发送至互联网。</li></ul></li></ol><p>第五幕：彼岸的镜像</p><ol start="10"><li>对方接收与还原<ul><li>接收：对方物理网卡收到 UDP 包，转交 <code>tailscaled</code>。</li><li>解密：还原出原始 ICMP 请求，写入对方的 <code>tailscale0</code> 接口。</li><li>交付：操作系统从虚拟网卡读取数据，交给应用层处理。</li></ul></li></ol><h2 id="Windows-下-VPN-配置与工作">Windows 下 VPN 配置与工作</h2><h3 id="Windows-系统内置-VPN-配置">Windows 系统内置 VPN 配置</h3><p>这里的配置相对简单：“连接名称”自己随便写一个；“服务器名称或地址”填写 <code>stu.vpn.sjtu.edu.cn</code>；“VPN 类型”选择 <code>IKEv2</code>；“登陆信息的类型”选择“用户名和密码”；最后的用户名和密码填写自己的 jAccount。</p><p>这里官方同样默认让所有流量走 VPN，因此需要额外的操作手动设置分流。首先运行：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 开启拆分隧道 (Split Tunneling)</span></span><br><span class="line"><span class="built_in">Set-VpnConnection</span> <span class="literal">-Name</span> <span class="string">&quot;SJTU VPN&quot;</span> <span class="literal">-SplitTunneling</span> <span class="variable">$true</span></span><br></pre></td></tr></table></figure><p>让 VPN 支持路由。然后添加学校的路由网段：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 添加静态路由 (重启后依然有效)</span></span><br><span class="line"><span class="comment"># -DestinationPrefix: 学校网段</span></span><br><span class="line"><span class="comment"># -PassThru: 显示结果</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Add-VpnConnectionRoute</span> <span class="literal">-ConnectionName</span> <span class="string">&quot;SJTU VPN&quot;</span> <span class="literal">-DestinationPrefix</span> <span class="number">202.120</span>.<span class="number">0.0</span>/<span class="number">16</span> <span class="literal">-PassThru</span></span><br><span class="line"><span class="built_in">Add-VpnConnectionRoute</span> <span class="literal">-ConnectionName</span> <span class="string">&quot;SJTU VPN&quot;</span> <span class="literal">-DestinationPrefix</span> <span class="number">10.85</span>.<span class="number">33.64</span>/<span class="number">28</span> <span class="literal">-PassThru</span></span><br><span class="line"><span class="built_in">Add-VpnConnectionRoute</span> <span class="literal">-ConnectionName</span> <span class="string">&quot;SJTU VPN&quot;</span> <span class="literal">-DestinationPrefix</span> <span class="number">111.186</span>.<span class="number">0.0</span>/<span class="number">16</span> <span class="literal">-PassThru</span></span><br></pre></td></tr></table></figure><h3 id="Windows-内置-VPN-工作方式">Windows 内置 VPN 工作方式</h3><h4 id="PPP-适配器">PPP 适配器</h4><p>运行 <code>ipconfig</code>，会看到多出来一个叫 <code>PPP 适配器</code> 的东西：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">PPP 适配器 SJTU VPN:</span><br><span class="line"></span><br><span class="line">   连接特定的 DNS 后缀 . . . . . . . :</span><br><span class="line">   IPv6 地址 . . . . . . . . . . . . : 2001:da8:8000:7100::5:204</span><br><span class="line">   本地链接 IPv6 地址. . . . . . . . : fe80::5:204%62</span><br><span class="line">   IPv4 地址 . . . . . . . . . . . . : 111.186.44.11</span><br><span class="line">   子网掩码  . . . . . . . . . . . . : 255.255.255.255</span><br><span class="line">   默认网关. . . . . . . . . . . . . : 0.0.0.0</span><br></pre></td></tr></table></figure><p>这里可以看到获得的学校内网的 IP。</p><div class="note info flat"><p>为什么叫 “PPP”？ 这是一个历史遗留问题。早期的拨号上网（Modem）和老式 VPN（PPTP/L2TP）都使用 PPP 协议。虽然现在的 IKEv2 协议底层其实不一定跑 PPP 封装，但因为它们都归 Windows 的 RAS (Remote Access Service) 子系统管理，所以 ipconfig 为了省事，经常统称它们为“PPP 适配器”。这只是个名字，不代表它真的在用古老的 PPP 协议传输。</p></div><p>从 <code>ipconfig</code> 的信息可以看出，与 StrongSwan 使用 XFRM 策略在物理网卡处直接拦截流量不同，Windows 创建了一个新的虚拟网卡。但是这个策略与 TUN 模式存在不同：</p><ul><li>Tailscale 的 TUN 模式：当内核把数据包扔进 TUN 时，运行在用户态的应用程序 Tailscale 会从 TUN 里面把数据读出来，然后进行加密处理，再发送出去。</li><li>Windows 内置 VPN 的 PPP 适配器：内核把数据包扔给 PPP 适配器时，Windows 的 TCP/IP 协议栈和 IPsec 驱动可以直接在内核里截获这个包，再利用内核驱动进行加密封装，节省了“内核态”-“用户态”的切换过程。</li></ul><h4 id="路由表-3">路由表</h4><p>运行 <code>route print</code>，可以得到一张很大的路由表。与 Linux 分出很多张路由表不同，Windows 的这张路由表基本包含了所有路由。为简洁与方便理解，这里只放出 IPV4 的路由表：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line">IPv4 路由表</span><br><span class="line">===========================================================================</span><br><span class="line">活动路由:</span><br><span class="line">          网络目标         网络掩码                网关             接口   跃点数</span><br><span class="line">          0.0.0.0          0.0.0.0      10.131.147.110    10.131.147.78     35</span><br><span class="line">         10.0.0.0        255.0.0.0            在链路上     111.186.44.11     26</span><br><span class="line">     10.131.147.0    255.255.255.0            在链路上     10.131.147.78    291</span><br><span class="line">    10.131.147.78  255.255.255.255            在链路上     10.131.147.78    291</span><br><span class="line">   10.131.147.255  255.255.255.255            在链路上     10.131.147.78    291</span><br><span class="line">   10.255.255.255  255.255.255.255            在链路上     111.186.44.11    281</span><br><span class="line">     100.90.131.9  255.255.255.255            在链路上      100.90.131.9    261</span><br><span class="line">    100.91.189.19  255.255.255.255            在链路上      100.90.131.9      5</span><br><span class="line">    100.94.155.95  255.255.255.255            在链路上      100.90.131.9      5</span><br><span class="line">  100.100.100.100  255.255.255.255            在链路上      100.90.131.9      5</span><br><span class="line">    100.108.53.75  255.255.255.255            在链路上      100.90.131.9      5</span><br><span class="line">   100.118.249.39  255.255.255.255            在链路上      100.90.131.9      5</span><br><span class="line">        111.0.0.0        255.0.0.0            在链路上     111.186.44.11     26</span><br><span class="line">      111.186.0.0      255.255.0.0            在链路上     111.186.44.11     26</span><br><span class="line">     111.186.44.0  255.255.255.255      10.131.147.110    10.131.147.78     36</span><br><span class="line">    111.186.44.11  255.255.255.255            在链路上     111.186.44.11    281</span><br><span class="line">  111.186.255.255  255.255.255.255            在链路上     111.186.44.11    281</span><br><span class="line">  111.255.255.255  255.255.255.255            在链路上     111.186.44.11    281</span><br><span class="line">        127.0.0.0        255.0.0.0            在链路上         127.0.0.1    331</span><br><span class="line">        127.0.0.1  255.255.255.255            在链路上         127.0.0.1    331</span><br><span class="line">  127.255.255.255  255.255.255.255            在链路上         127.0.0.1    331</span><br><span class="line">      172.30.32.0    255.255.240.0            在链路上       172.30.32.1    271</span><br><span class="line">      172.30.32.1  255.255.255.255            在链路上       172.30.32.1    271</span><br><span class="line">    172.30.47.255  255.255.255.255            在链路上       172.30.32.1    271</span><br><span class="line">      202.120.0.0      255.255.0.0            在链路上     111.186.44.11     26</span><br><span class="line">  202.120.255.255  255.255.255.255            在链路上     111.186.44.11    281</span><br><span class="line">        224.0.0.0        240.0.0.0            在链路上         127.0.0.1    331</span><br><span class="line">        224.0.0.0        240.0.0.0            在链路上     10.131.147.78    291</span><br><span class="line">        224.0.0.0        240.0.0.0            在链路上       172.30.32.1    271</span><br><span class="line">        224.0.0.0        240.0.0.0            在链路上     111.186.44.11    281</span><br><span class="line">  255.255.255.255  255.255.255.255            在链路上         127.0.0.1    331</span><br><span class="line">  255.255.255.255  255.255.255.255            在链路上     10.131.147.78    291</span><br><span class="line">  255.255.255.255  255.255.255.255            在链路上       172.30.32.1    271</span><br><span class="line">  255.255.255.255  255.255.255.255            在链路上     111.186.44.11    281</span><br><span class="line">===========================================================================</span><br><span class="line">永久路由:</span><br><span class="line">  无</span><br></pre></td></tr></table></figure><p>信息解释：</p><ul><li><strong>跃点数</strong>：跃点数代表“成本”或“距离”，越小越优先。<ul><li>虽然默认路由 <code>0.0.0.0</code> 的跃点数很低，但是路由匹配优先使用最长前缀匹配原则，因此不用担心那些跃点数特别大的路由无法生效。</li></ul></li><li><strong>接口</strong>：与 Linux 直接告知网卡名称不同，Windows 用这张网卡此时此刻拥有的 IP 地址代指网卡。例如接口 <code>111.186.44.11</code> 指代了 SJTU VPN 的虚拟适配器。</li><li><strong>在链路上</strong>：表示直连，不需要经过路由器（网关）。例如 <code>10.131.147.0    255.255.255.0    在链路上    10.131.147.78</code>，表示凡是发给 <code>10.131.147.xxx</code> 的数据，因为就在局域网里（这个 IP 应该是 5G 基站发的，和常规的局域网或许也有点区别），直接发，不用找路由器转发。</li></ul><p>Windows 中特殊 IP 说明：<br>下面是 Windows 系统中常见的特殊 IP 地址及其用途总结。</p><table><thead><tr><th style="text-align:left">IP 网段 / 地址</th><th style="text-align:left">常见名称 / 归属</th><th style="text-align:left">详细解释</th></tr></thead><tbody><tr><td style="text-align:left"><code>0.0.0.0</code></td><td style="text-align:left">未指定 / 默认路由</td><td style="text-align:left">在路由表中代表“所有其他不匹配的流量”（默认网关）。在监听端口时，代表“监听本机所有网卡”。</td></tr><tr><td style="text-align:left"><code>127.0.0.0/8</code></td><td style="text-align:left">回环地址 (Loopback)</td><td style="text-align:left">最常见的是 <code>127.0.0.1</code> (<code>localhost</code>)。代表“本机自己”。流量不会经过物理网卡，直接在内核被接收。即便断网也能 ping 通。</td></tr><tr><td style="text-align:left"><code>172.17.x.x - 172.31.x.x</code></td><td style="text-align:left">Hyper-V / WSL2 / Docker</td><td style="text-align:left">Windows 的虚拟化组件（Hyper-V）默认会从 B 类私有地址中随机抽取一个 <code>/20</code> 或 <code>/24</code> 的网段作为虚拟交换机的地址。每次重启或重置网络，这个段可能会变。</td></tr><tr><td style="text-align:left"><code>224.0.0.0 - 239.255.255.255</code></td><td style="text-align:left">组播地址 (Multicast)</td><td style="text-align:left">用于局域网内的一对多通信。比如 SSDP (智能家居发现)、LLMNR (主机名解析) 等后台服务都会用到。</td></tr><tr><td style="text-align:left"><code>255.255.255.255</code></td><td style="text-align:left">受限广播地址</td><td style="text-align:left">代表“当前局域网内的所有主机”。发往这里的包会被广播给同一网段的所有设备。</td></tr></tbody></table>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/2415e5a2.html</id>
    <link href="https://zhuxz0299.github.io/posts/2415e5a2.html"/>
    <published>2026-01-24T17:22:10.000Z</published>
    <summary>由于同时有使用交大 VPN 连接校园网以及 Tailscale 实现异地组网的需求，因此研究一下 VPN 的配置以及工作方式。</summary>
    <title>VPN 应用与工作方式</title>
    <updated>2026-09-04T12:58:26.803Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="Dev Tools" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/"/>
    <category term="Software" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/Software/"/>
    <category term="linux" scheme="https://zhuxz0299.github.io/tags/linux/"/>
    <category term="wayland" scheme="https://zhuxz0299.github.io/tags/wayland/"/>
    <category term="vim" scheme="https://zhuxz0299.github.io/tags/vim/"/>
    <category term="keyd" scheme="https://zhuxz0299.github.io/tags/keyd/"/>
    <category term="capslock" scheme="https://zhuxz0299.github.io/tags/capslock/"/>
    <category term="esc" scheme="https://zhuxz0299.github.io/tags/esc/"/>
    <content>
      <![CDATA[<h2 id="如何优雅地设置-Esc">如何优雅地设置 Esc</h2><p>使用 Vim 的一个非常直接的阻碍是：频繁使用的 Esc 键实在有些远离手指可以方便够到的区域，因此目前的一个主流方法是：通过系统设置把 Esc 键以及 Caps 键进行互换。通常来说这个策略确实可行，但是我本身又是 Capslock+ 的用户。虽然这个软件并没有提供 Linux 支持，但是由于我已经习惯了使用 Capslock + 其他按键实现光标的移动，所以我又通过 keyd 在 Linux 中也实现了类似的功能。这就导致了一个问题：假如直接把 Esc 和 Caps 互换，那我在除了 vim 之外的其他地方就没法方便地移动光标了。</p><p>上述问题的最终解决方案是：在 keyd 中设置点按 Caps 键执行 Esc；Caps + 空格键实现大小写转换。配置按键使用的软件与文章<a href="https://zhuxz0299.github.io/posts/aae19956.html">快捷使用方向键</a>中相同。</p><h3 id="Windows-CapsLock-配置方式">Windows CapsLock+ 配置方式</h3><p>在 <code>CapsLock+settings.ini</code> 的 <code>[key]</code> 底下加入</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">;短按 Caps Lock -&gt; 发送 Esc</span></span><br><span class="line"><span class="attr">press_caps</span>=keyFunc_esc</span><br><span class="line"><span class="comment">;Capslock+Space -&gt; enter</span></span><br><span class="line"><span class="attr">caps_space</span>=keyFunc_toggleCapsLock</span><br></pre></td></tr></table></figure><h3 id="Linux-keyd-配置方式">Linux keyd 配置方式</h3><p>在 <code>/etc/keyd/default.conf</code> 中加入，这个的完整配置在<a href="https://zhuxz0299.github.io/posts/aae19956.html">快捷使用方向键</a>里面已经有了。</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Capslock + Enter: 在行末换行 (End -&gt; Enter)</span></span><br><span class="line"><span class="attr">enter</span> = macro(end enter)</span><br><span class="line"><span class="comment"># Capslock + Backspace: 删除当前行 (Home -&gt; Shift+End -&gt; Backspace)</span></span><br><span class="line"><span class="attr">backspace</span> = macro(home S-end backspace)</span><br></pre></td></tr></table></figure><h2 id="Vim-常用操作">Vim 常用操作</h2><h3 id="简单操作">简单操作</h3><table><thead><tr><th style="text-align:left">分类</th><th style="text-align:left">按键 / 命令</th><th style="text-align:left">功能说明</th></tr></thead><tbody><tr><td style="text-align:left"><strong>移动</strong></td><td style="text-align:left"><strong><code>h</code> <code>j</code> <code>k</code> <code>l</code></strong></td><td style="text-align:left"><strong>左 / 下 / 上 / 右</strong> (建议强迫自己只用这四个键)</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>w</code> / <code>b</code></td><td style="text-align:left">下一个单词开头 / 上一个单词开头</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>0</code> / <code>$</code></td><td style="text-align:left">跳转到 <strong>行首</strong> / <strong>行尾</strong></td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>gg</code> / <code>G</code></td><td style="text-align:left">跳转到 <strong>文件第一行</strong> / <strong>文件最后一行</strong></td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>Ctrl</code> + <code>u</code> / <code>d</code></td><td style="text-align:left">向上 / 向下翻半页 (Up/Down)</td></tr><tr><td style="text-align:left"><strong>插入</strong></td><td style="text-align:left"><strong><code>i</code> / <code>a</code></strong></td><td style="text-align:left">光标 <strong>前</strong> 插入 / 光标 <strong>后</strong> 插入</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>I</code> / <code>A</code></td><td style="text-align:left">跳转到 <strong>行首</strong> 插入 / 跳转到 <strong>行尾</strong> 插入</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>o</code> / <code>O</code></td><td style="text-align:left">在 <strong>下方</strong> 新建一行 / 在 <strong>上方</strong> 新建一行</td></tr><tr><td style="text-align:left"><strong>编辑</strong></td><td style="text-align:left"><strong><code>yy</code> / <code>p</code></strong></td><td style="text-align:left"><strong>复制</strong> 当前行 / <strong>粘贴</strong> (在光标后)</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>dd</code></td><td style="text-align:left"><strong>剪切 (删除)</strong> 当前行 (配合 <code>p</code> 粘贴即为移动行)</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>x</code> / <code>r</code></td><td style="text-align:left">删除当前字符 / 替换当前字符 (无需进入插入模式)</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>u</code> / <code>Ctrl</code>+<code>r</code></td><td style="text-align:left"><strong>撤销</strong> (Undo) / <strong>重做</strong> (Redo)</td></tr><tr><td style="text-align:left"><strong>组合</strong></td><td style="text-align:left"><code>Num</code> + <code>Cmd</code></td><td style="text-align:left"><strong>重复命令</strong> (例: <code>3yy</code> 复制3行, <code>5dd</code> 删5行, <code>10j</code> 下移10行)</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>ciw</code></td><td style="text-align:left"><strong>修改单词</strong> (Change Inner Word, 删除单词并立刻进入插入模式)</td></tr><tr><td style="text-align:left"><strong>文件</strong></td><td style="text-align:left"><code>:w</code> / <code>:q</code></td><td style="text-align:left">保存 / 退出</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>:wq</code> / <code>:q!</code></td><td style="text-align:left">保存并退出 / 强制退出 (不保存)</td></tr><tr><td style="text-align:left"><strong>搜索</strong></td><td style="text-align:left"><code>/text</code></td><td style="text-align:left">向下搜索 “text” (按 <code>n</code> 下一个, <code>N</code> 上一个)</td></tr><tr><td style="text-align:left"></td><td style="text-align:left"><code>:%s/old/new/g</code></td><td style="text-align:left">全局替换 (将 old 替换为 new)</td></tr></tbody></table><h3 id="组合操作详解">组合操作详解</h3><blockquote><p><strong>命令 (Verb) + 范围 (Scope) + 对象 (Object)</strong></p></blockquote><p>例如：<code>ci&quot;</code> = <strong>C</strong>hange <strong>I</strong>nner <strong>&quot;</strong> (修改双引号内部的内容)</p><h4 id="A-动词-Verb-：你想做什么？">A. 动词 (Verb)：你想做什么？</h4><table><thead><tr><th style="text-align:left">按键</th><th style="text-align:left">英文</th><th style="text-align:left">含义</th></tr></thead><tbody><tr><td style="text-align:left"><code>c</code></td><td style="text-align:left">Change</td><td style="text-align:left">修改 (删除并进入插入模式)</td></tr><tr><td style="text-align:left"><code>d</code></td><td style="text-align:left">Delete</td><td style="text-align:left">删除 (剪切)</td></tr><tr><td style="text-align:left"><code>y</code></td><td style="text-align:left">Yank</td><td style="text-align:left">复制</td></tr><tr><td style="text-align:left"><code>v</code></td><td style="text-align:left">Visual</td><td style="text-align:left">选中 (进入可视模式)</td></tr></tbody></table><h4 id="B-范围-Scope-：包含边界吗？">B. 范围 (Scope)：包含边界吗？</h4><p>这是最关键的区别：</p><table><thead><tr><th style="text-align:left">按键</th><th style="text-align:left">英文</th><th style="text-align:left">含义</th></tr></thead><tbody><tr><td style="text-align:left"><code>i</code></td><td style="text-align:left">Inner</td><td style="text-align:left">内部 (不包含括号/引号本身)</td></tr><tr><td style="text-align:left"><code>a</code></td><td style="text-align:left">Around</td><td style="text-align:left">周围 (包含括号/引号本身)</td></tr></tbody></table><h4 id="C-对象-Object-：操作什么东西？">C. 对象 (Object)：操作什么东西？</h4><table><thead><tr><th style="text-align:left">按键</th><th style="text-align:left">对象</th><th style="text-align:left">示例</th></tr></thead><tbody><tr><td style="text-align:left"><code>w</code></td><td style="text-align:left">Word</td><td style="text-align:left">单词</td></tr><tr><td style="text-align:left"><code>&quot;</code> / <code>'</code></td><td style="text-align:left">Quote</td><td style="text-align:left">引号内的字符串</td></tr><tr><td style="text-align:left"><code>(</code> / <code>)</code></td><td style="text-align:left">Parentheses</td><td style="text-align:left">圆括号 <code>(...)</code></td></tr><tr><td style="text-align:left"><code>[</code> / <code>]</code></td><td style="text-align:left">Brackets</td><td style="text-align:left">方括号 <code>[...]</code></td></tr><tr><td style="text-align:left"><code>&#123;</code> / <code>&#125;</code></td><td style="text-align:left">Braces</td><td style="text-align:left">大括号 <code>&#123;...&#125;</code></td></tr><tr><td style="text-align:left"><code>t</code></td><td style="text-align:left">Tag</td><td style="text-align:left">HTML/XML 标签 <code>&lt;div&gt;...&lt;/div&gt;</code></td></tr><tr><td style="text-align:left"><code>p</code></td><td style="text-align:left">Paragraph</td><td style="text-align:left">段落</td></tr></tbody></table><h3 id="查找并替换详解">查找并替换详解</h3><p><code>:s</code> 命令的基本格式为：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:[范围]s[分隔符]查找内容[分隔符]替换内容[分隔符][选项]</span><br></pre></td></tr></table></figure><h4 id="范围">范围</h4><p>“范围”决定命令对哪些行生效。如果省略范围，则只处理光标所在的当前行。</p><table><thead><tr><th style="text-align:left">范围</th><th style="text-align:left">含义</th></tr></thead><tbody><tr><td style="text-align:left">省略</td><td style="text-align:left">当前行</td></tr><tr><td style="text-align:left"><code>%</code></td><td style="text-align:left">整个文件</td></tr><tr><td style="text-align:left"><code>10,20</code></td><td style="text-align:left">第 10 至 20 行，包含起止行</td></tr><tr><td style="text-align:left"><code>.,$</code></td><td style="text-align:left">从当前行到文件末尾，<code>.</code> 表示当前行，<code>$</code> 表示最后一行</td></tr><tr><td style="text-align:left"><code>'&lt;,'&gt;</code></td><td style="text-align:left">Visual 模式选中的行范围；选中内容后按 <code>:</code> 会自动填入</td></tr></tbody></table><h4 id="分隔符">分隔符</h4><p>分隔符用来分开“查找内容”、“替换内容”和“选项”。<code>/</code> 并不是固定分隔符，可以换成大多数单字节的非字母数字字符。例如：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">:s/foo/bar/</span><br><span class="line">:s#foo#bar#</span><br><span class="line">:s@foo@bar@</span><br><span class="line">:s!foo!bar!</span><br><span class="line">:s;foo;bar;</span><br></pre></td></tr></table></figure><p>当查找或替换的内容本身包含大量 <code>/</code> 时，用别的分隔符可以避免频繁输入转义符 <code>\</code>。例如，下面两条命令效果相同，但第二条更容易阅读：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">:%s/\/home\/user/\/<span class="keyword">opt</span>\/app/g</span><br><span class="line">:%s#/home/user#/<span class="keyword">opt</span>/app#g</span><br></pre></td></tr></table></figure><h4 id="选项">选项</h4><p>“选项”放在最后一个分隔符之后，用来控制替换方式。多个选项可以组合使用，例如 <code>gc</code> 表示“替换每行中的所有匹配，并在每次替换前确认”。</p><table><thead><tr><th style="text-align:left">选项</th><th style="text-align:left">含义</th></tr></thead><tbody><tr><td style="text-align:left">省略</td><td style="text-align:left">每行只替换第一个匹配项</td></tr><tr><td style="text-align:left"><code>g</code></td><td style="text-align:left">替换每行中的所有匹配项</td></tr><tr><td style="text-align:left"><code>c</code></td><td style="text-align:left">每次替换前进行确认</td></tr><tr><td style="text-align:left"><code>i</code></td><td style="text-align:left">匹配时忽略大小写</td></tr><tr><td style="text-align:left"><code>I</code></td><td style="text-align:left">匹配时强制区分大小写</td></tr></tbody></table><p>使用 <code>c</code> 选项时，常用的确认按键有 <code>y</code>（替换当前项）、<code>n</code>（跳过当前项）、<code>a</code>（替换后面的所有匹配项）和 <code>q</code>（退出）。</p><h4 id="完整示例">完整示例</h4><table><thead><tr><th style="text-align:left">命令</th><th style="text-align:left">功能说明</th></tr></thead><tbody><tr><td style="text-align:left"><code>:s/old/new/</code></td><td style="text-align:left">将当前行中第一个 <code>old</code> 替换为 <code>new</code></td></tr><tr><td style="text-align:left"><code>:s/old/new/g</code></td><td style="text-align:left">将当前行中所有 <code>old</code> 替换为 <code>new</code></td></tr><tr><td style="text-align:left"><code>:%s/old/new/gc</code></td><td style="text-align:left">在整个文件中替换所有 <code>old</code>，每次替换前都进行确认</td></tr><tr><td style="text-align:left"><code>:10,20s/old/new/gi</code></td><td style="text-align:left">在第 10 至 20 行中替换所有 <code>old</code>，并忽略大小写</td></tr><tr><td style="text-align:left"><code>:'&lt;,'&gt;s/^/# /g</code></td><td style="text-align:left">在 Visual 模式选中的每行行首添加 <code># </code></td></tr></tbody></table><h2 id="Vim-的剪切板机制">Vim 的剪切板机制</h2><p>在 Vim 中使用 <code>y</code> 复制的内容，默认无法直接在其他软件（如浏览器）中通过 <code>Ctrl+v</code> 粘贴。这是因为 Vim 有自己独立的<strong>寄存器 (Register)</strong> 系统，默认的复制操作只是把内容放进了 Vim 内部的“无名寄存器”，并没有进入系统的剪切板。</p><h3 id="系统剪切板寄存器">系统剪切板寄存器</h3><p>Vim 专门预留了两个寄存器用于与系统剪切板交互：</p><ul><li><strong><code>+</code> 寄存器</strong>：对应系统的剪切板 (Clipboard)，也就是常用的 <code>Ctrl+c</code> / <code>Ctrl+v</code> 所在的区域。</li><li><strong><code>*</code> 寄存器</strong>：对应 Linux 下的“主选择区” (Primary Selection)，即选中即复制，中键粘贴的区域。</li></ul><p>通常我们关注的是 <code>+</code> 寄存器。</p><h3 id="常用操作">常用操作</h3><p>想要与系统剪切板交互，只需要在常用的 <code>y</code> (复制)、<code>d</code> (剪切)、<code>p</code> (粘贴) 命令前加上 <code>&quot;+</code> 即可。</p><table><thead><tr><th style="text-align:left">命令</th><th style="text-align:left">功能说明</th></tr></thead><tbody><tr><td style="text-align:left"><code>&quot;+y</code></td><td style="text-align:left">复制选中内容到系统剪切板</td></tr><tr><td style="text-align:left"><code>&quot;+yy</code></td><td style="text-align:left">复制当前行到系统剪切板</td></tr><tr><td style="text-align:left"><code>&quot;+p</code></td><td style="text-align:left">将系统剪切板的内容粘贴到光标后</td></tr><tr><td style="text-align:left"><code>&quot;+P</code></td><td style="text-align:left">将系统剪切板的内容粘贴到光标前</td></tr></tbody></table><h3 id="一劳永逸的配置">一劳永逸的配置</h3><p>若是觉得每次都要按 <code>&quot;+</code> 三个键过于繁琐，可以通过修改配置文件（<code>.vimrc</code> 或 <code>init.lua</code>），让 Vim 的默认操作直接使用系统剪切板。</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&quot; 将 unnamedplus (也就是 + 寄存器) 添加到 clipboard 选项</span></span><br><span class="line"><span class="keyword">set</span> clipboard=unnamedplus</span><br></pre></td></tr></table></figure><p>这样设置之后，直接使用 <code>y</code>、<code>d</code>、<code>p</code> 就会自动同步到系统剪切板了，非常方便。</p><div class="note warning flat"><ul><li>要使用此功能，Vim 版本必须支持 <code>clipboard</code> 特性。可以通过命令行输入 <code>vim --version</code> 查看，如果显示 <code>+clipboard</code> 则表示支持；如果是 <code>-clipboard</code>，则可能需要安装 <code>vim-gtk</code> 或 <code>vim-gui-common</code> 等包含图形界面支持的版本（即使只在终端使用）。</li><li>即使 Vim 版本支持 <code>clipboard</code> 特性，有时候做了上述设置依然不会成功。这个时候可以考虑一下下载 xclip (X11) 或者 wl-clipboard (Wayland) 来看看能否成功。</li><li>如果还是不行，那就只能设置<figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&quot; 支持在Visual模式下，通过C-y复制到系统剪切板</span></span><br><span class="line"><span class="keyword">vnoremap</span> <span class="symbol">&lt;C-y&gt;</span> <span class="comment">&quot;+y</span></span><br><span class="line"><span class="comment">&quot; 支持在normal模式下，通过C-p粘贴系统剪切板</span></span><br><span class="line"><span class="keyword">nnoremap</span> <span class="symbol">&lt;C-p&gt;</span> <span class="comment">&quot;*p </span></span><br></pre></td></tr></table></figure>凑合着用吧。</li></ul></div><h3 id="如何只删除不剪切-黑洞寄存器">如何只删除不剪切 (黑洞寄存器)</h3><p>在 Vim 中，<code>d</code> (Delete) 之类的操作实际上是<strong>剪切</strong>，这经常导致覆盖掉粘贴板中原本想粘贴的内容。Vim 提供了一个<strong>黑洞寄存器</strong> <code>&quot;_</code>，放入其中的内容会被直接丢弃。</p><p>如果希望 <code>d</code> 操作永远<strong>只删除不剪切</strong>，可以将 <code>d</code> 直接绑定到黑洞寄存器上。</p><h4 id="Vim-Neovim-配置">Vim / Neovim 配置</h4><p>在配置文件 <code>~/.vimrc</code> 中加入：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">nnoremap</span> d <span class="comment">&quot;_d</span></span><br><span class="line"><span class="keyword">vnoremap</span> d <span class="comment">&quot;_d</span></span><br></pre></td></tr></table></figure><h4 id="Neovim-配置">Neovim 配置</h4><p>在配置文件 <code>~/.config/nvim/lua/config/keymaps.lua</code> 中加入：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 对应 nnoremap d &quot;_d 和 vnoremap d &quot;_d</span></span><br><span class="line">vim.keymap.set(&#123; <span class="string">&quot;n&quot;</span>, <span class="string">&quot;v&quot;</span> &#125;, <span class="string">&quot;d&quot;</span>, <span class="string">&#x27;&quot;_d&#x27;</span>, &#123; desc = <span class="string">&quot;Delete without yanking&quot;</span> &#125;)</span><br><span class="line"><span class="comment">-- 为了保险起见，通常建议把 dd (删除整行) 也专门加上</span></span><br><span class="line">vim.keymap.set(<span class="string">&quot;n&quot;</span>, <span class="string">&quot;dd&quot;</span>, <span class="string">&#x27;&quot;_dd&#x27;</span>, &#123; desc = <span class="string">&quot;Delete line without yanking&quot;</span> &#125;)</span><br></pre></td></tr></table></figure><h4 id="VS-Code-Vim-插件-配置">VS Code (Vim 插件) 配置</h4><p>在 <code>settings.json</code> 中加入以下配置：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">&quot;vim.normalModeKeyBindingsNonRecursive&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;before&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;d&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;after&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;\&quot;&quot;</span><span class="punctuation">,</span> <span class="string">&quot;_&quot;</span><span class="punctuation">,</span> <span class="string">&quot;d&quot;</span><span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;vim.visualModeKeyBindingsNonRecursive&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;before&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;d&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;after&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;\&quot;&quot;</span><span class="punctuation">,</span> <span class="string">&quot;_&quot;</span><span class="punctuation">,</span> <span class="string">&quot;d&quot;</span><span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure><p>这样修改后，<code>d</code> 相关的操作就不再污染剪切板了。如果偶尔需要剪切，可以使用 <code>x</code> (剪切字符) 或 <code>c</code> (修改)。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/efca9c95.html</id>
    <link href="https://zhuxz0299.github.io/posts/efca9c95.html"/>
    <published>2026-01-22T13:08:49.000Z</published>
    <summary>介绍解决 Vim 中按键映射的方式，以及 Vim 的一些简单操作，最后是 Vim 的剪切板的奇怪机制。</summary>
    <title>Vim 使用入门</title>
    <updated>2026-09-04T12:14:18.243Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Windows" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Windows/"/>
    <category term="windows" scheme="https://zhuxz0299.github.io/tags/windows/"/>
    <category term="software" scheme="https://zhuxz0299.github.io/tags/software/"/>
    <content>
      <![CDATA[<ul><li><p><a href="https://capslox.com/capslock-plus/">Capslock+</a></p><ul><li>博客<a href="https://zhuxz0299.github.io/posts/aae19956.html">快捷使用方向键</a>中有记录。</li></ul></li><li><p><a href="https://github.com/daipeihust/im-select">im-select</a></p><ul><li>为了方便在使用 VSCodeVim 插件时，进入 NORMAL 模式时能够自动切换英文输入法的辅助工具。</li></ul></li><li><p><a href="https://iperf.fr/">iperf</a></p><ul><li>一个用于测量两台机器之间带宽与延迟的工具。</li></ul></li><li><p><a href="https://github.com/OpenListTeam/OpenList">Openlist</a></p><ul><li>博客 <a href="https://zhuxz0299.github.io/posts/85c3534f.html">WebDAV 协议与网盘的本地挂载</a>中有记录</li></ul></li><li><p><a href="https://www.sumatrapdfreader.org/free-pdf-reader">SumatraPDF</a></p><ul><li>一个 PDF 阅读器，足够轻量且支持对文档进行简单的反色。</li></ul></li><li><p><a href="https://github.com/1357310795/TboxWebdav">TboxWebdav</a></p><ul><li>让用户可以通过 WebDAV 协议访问新交大云盘。</li></ul></li><li><p><a href="https://github.com/2dust/v2rayN">v2rayN</a></p><ul><li>一个图形化的代理工具，支持 Xray，sing-box 等内核。</li></ul></li><li><p><a href="https://www.ventoy.net/cn/index.html">ventory</a></p><ul><li>制作可启动U盘的开源工具，支持各种操作系统。</li></ul></li><li><p><a href="https://www.7-zip.org/">7-zip</a></p><ul><li>非常好用的压缩/解压缩工具。</li></ul></li><li><p><a href="https://www.adobe.com/products/premiere.html">Adobe Premiere Pro</a></p><ul><li>专业的视频剪辑软件。</li></ul></li><li><p><a href="https://www.amd.com/">AMD Software</a></p><ul><li>AMD 显卡驱动与管理软件。</li></ul></li><li><p><a href="https://github.com/ClassicOldSong/Apollo">Apollo</a></p><ul><li>远程串流软件，是 Sunshine 的 Fork，增加了更多的高级功能。</li></ul></li><li><p>Application Verifier</p><ul><li>微软官方的应用 BUG 监测工具。不知道哪来的，可能是下载 Visual Studio 附带的。</li></ul></li><li><p><a href="https://www.autohotkey.com/">AutoHotkey</a></p><ul><li>在 Windows 上用来给各种组合键自定义脚本，是非常好用的工具。</li></ul></li><li><p><a href="https://www.chemtable.com/autorun-organizer.htm">Autorun Organizer 下载地址（找不到官网）</a></p><ul><li>用于设置 Windows 的开机自启动项，可以禁用某些自启动，也可以设置开机之后一定时间再自启动，缓解刚开机是大量应用同时启动导致的卡顿。</li></ul></li><li><p><a href="https://bettergi.com/">BetterGI</a></p><ul><li>原神脚本软件。</li></ul></li><li><p><a href="https://www.cherry-ai.com/">Cherry Studio</a></p><ul><li>提供和 AI 交互的 GUI。</li></ul></li><li><p><a href="https://github.com/clash-verge-rev/clash-verge-rev">Clash Verge</a></p><ul><li>非常强大的代理软件。</li></ul></li><li><p><a href="https://potplayer.tv/">DAUM PotPlayer</a></p><ul><li>开箱即用且功能丰富的视频播放器。</li></ul></li><li><p><a href="https://www.docker.com/products/docker-desktop/">Docker-desktop</a></p><ul><li>目前想在 Windows 上用 Docker 不得不品的一环。</li></ul></li><li><p>dotnet</p><ul><li>.NET 开发环境</li></ul></li><li><p><a href="https://www.eudic.net/v4/en/app/eudic">eudic</a></p><ul><li>欧陆词典，目前用下来比较好用且美观的本地词典。</li></ul></li><li><p><a href="https://git-scm.com/">Git</a></p><ul><li>git 工具</li></ul></li><li><p><a href="https://www.google.com/chrome/">Google Chrome</a></p><ul><li>Chrome 浏览器</li></ul></li><li><p><a href="https://handbrake.fr/">HandBrake</a></p><ul><li>开源视频格式转换工具</li></ul></li><li><p><a href="https://support.hp.com/us-en/help/printscandoctor">HP Print and Scan Doctor</a></p><ul><li>惠普打印机诊断工具。</li></ul></li><li><p><a href="https://www.jetbrains.com/">JetBrains</a></p><ul><li>JetBrains 全家桶，强大的 IDE 集合。</li></ul></li><li><p><a href="https://localsend.org/">LocalSend</a></p><ul><li>开源跨平台局域网文件传输工具。</li></ul></li><li><p><a href="https://www.logitechg.com/zh-cn/innovation/g-hub.html">Logitech G Hub</a></p><ul><li>罗技游戏外设管理驱动。</li></ul></li><li><p><a href="https://www.logitech.com/zh-cn/software/logi-options-plus.html">Logi Options+</a></p><ul><li>罗技办公外设管理驱动。</li></ul></li><li><p><a href="https://www.mechrevo.com/">Mechrevo Control Center</a></p><ul><li>机械革命电脑控制中心。</li></ul></li><li><p><a href="https://www.office.com/">Microsoft Office</a></p><ul><li>微软办公套件。</li></ul></li><li><p><a href="https://www.officeplus.cn/">Microsoft OfficePLUS</a></p><ul><li>微软官方的 Office 插件，提供模板和素材。</li></ul></li><li><p><a href="https://onedrive.live.com/about/zh-cn/">Microsoft OneDrive</a></p><ul><li>微软推出的云存储服务。</li></ul></li><li><p><a href="https://www.microsoft.com/zh-cn/sql-server">Microsoft SQL Server</a></p><ul><li>微软推出的关系型数据库管理系统。</li></ul></li><li><p><a href="https://visualstudio.microsoft.com/">Microsoft Visual Studio</a></p><ul><li>微软推出的集成开发环境（IDE）。</li></ul></li><li><p><a href="https://www.mihoyo.com/">MiHoYo Launcher</a></p><ul><li>米哈游启动器，用于启动原神、崩坏：星穹铁道等游戏。</li></ul></li><li><p><a href="https://moonlight-stream.org/">Moonlight</a></p><ul><li>基于 NVIDIA GameStream 的开源游戏串流客户端。</li></ul></li><li><p><a href="https://www.mozilla.org/zh-CN/firefox/new/">Mozilla Firefox</a></p><ul><li>优秀的开源浏览器。</li></ul></li><li><p><a href="https://www.thunderbird.net/">Mozilla Thunderbird</a></p><ul><li>Mozilla 出品的开源邮件客户端。</li></ul></li><li><p><a href="https://nodejs.org/">Node.js</a></p><ul><li>JavaScript 运行时环境。</li></ul></li><li><p><a href="https://obsproject.com/">OBS Studio</a></p><ul><li>强大的开源录屏与直播软件。</li></ul></li><li><p><a href="https://connect.oppo.com/zh-CN">OPPO Connect</a></p><ul><li>OPPO 跨屏互联，手机与电脑协同工具。</li></ul></li><li><p><a href="https://github.com/microsoft/PowerToys">PowerToys</a></p><ul><li>微软官方出品的系统增强工具集，非常实用。</li></ul></li><li><p><a href="https://www.qbittorrent.org/">qBittorrent</a></p><ul><li>开源 BitTorrent 客户端。</li></ul></li><li><p><a href="https://www.raidrive.com/">RaiDrive</a></p><ul><li>将网盘挂载为本地磁盘的工具。</li></ul></li><li><p><a href="https://www.sangfor.com.cn/">Sangfor EasyConnect</a></p><ul><li>深信服 EasyConnect，用于远程接入办公网络。在电脑上实际上大概是交大 VPN 用到的 64 位的库。</li></ul></li><li><p><a href="https://simpletex.cn/">SimpleTex</a></p><ul><li>强大的数学公式识别截图 OCR 工具，不过目前来看不如直接用 AI 识别了。</li></ul></li><li><p><a href="https://tailscale.com/">Tailscale</a></p><ul><li>基于 WireGuard 的零配置虚拟局域网组网工具。</li></ul></li><li><p><a href="https://im.qq.com/">Tencent QQ</a></p><ul><li>腾讯即时通讯软件。</li></ul></li><li><p><a href="https://weixin.qq.com/">WeChat</a></p><ul><li>微信电脑版。</li></ul></li><li><p><a href="https://meeting.tencent.com/">Tencent Meeting</a></p><ul><li>腾讯会议。</li></ul></li><li><p><a href="https://www.todesk.com/">ToDesk</a></p><ul><li>远程桌面控制软件。</li></ul></li><li><p><a href="https://typora.io/">Typora</a></p><ul><li>优雅的 Markdown 编辑器（所见即所得）。</li></ul></li><li><p><a href="https://github.com/nefarius/ViGEmBus">ViGEmBus</a></p><ul><li>虚拟游戏手柄驱动，常配合手柄映射工具使用。不知道哪来的，可能跟着 Apollo 一起装的？</li></ul></li><li><p><a href="https://www.zotero.org/">Zotero</a></p><ul><li>开源文献管理工具。</li></ul></li></ul>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/48e5d01.html</id>
    <link href="https://zhuxz0299.github.io/posts/48e5d01.html"/>
    <published>2026-01-22T11:07:34.000Z</published>
    <summary>记录一下 Windows 中用到的一些软件</summary>
    <title>Windows 软件记录</title>
    <updated>2026-04-27T06:07:11.834Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="Dev Tools" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/"/>
    <category term="Software" scheme="https://zhuxz0299.github.io/categories/Dev-Tools/Software/"/>
    <category term="proxy" scheme="https://zhuxz0299.github.io/tags/proxy/"/>
    <category term="systemd" scheme="https://zhuxz0299.github.io/tags/systemd/"/>
    <category term="ssh" scheme="https://zhuxz0299.github.io/tags/ssh/"/>
    <category term="tun" scheme="https://zhuxz0299.github.io/tags/tun/"/>
    <category term="software" scheme="https://zhuxz0299.github.io/tags/software/"/>
    <category term="clash" scheme="https://zhuxz0299.github.io/tags/clash/"/>
    <category term="configuration" scheme="https://zhuxz0299.github.io/tags/configuration/"/>
    <category term="mihomo" scheme="https://zhuxz0299.github.io/tags/mihomo/"/>
    <category term="yaml" scheme="https://zhuxz0299.github.io/tags/yaml/"/>
    <category term="rule" scheme="https://zhuxz0299.github.io/tags/rule/"/>
    <category term="proxy-groups" scheme="https://zhuxz0299.github.io/tags/proxy-groups/"/>
    <content>
      <![CDATA[<h2 id="名词解释">名词解释</h2><div class="note info flat"><p>来自 <a href="https://www.clashverge.dev/guide/term.html">Clash Verge Rev Docs</a></p></div><h3 id="系统代理-Tun-模式">系统代理 / Tun 模式</h3><p>系统代理：</p><ul><li>说明<ol><li>代理程序会在系统“约定”的特定位置（如注册表、系统变量等）<strong>设置好代理程序监听请求的端口信息</strong>，进行网络请求的应用会自发性地尝试读取这部分信息，并将请求发送至代理程序。不同操作系统的“约定”方式各异。</li><li>系统代理更像是一种行业内的“约定”，并非所有程序都遵守这种非强制性的“约定”，最终采取哪种方式发生请求往往取决于开发人员的意愿。（就比如说 Chrome, Firefox 等浏览器可能默认会走系统代理，但是命令行的一些工具则需要单独配置代理。）</li></ol></li><li>特点<ol><li>具有自发性，网络请求程序尝试使用“约定”配置或使用网络请求程序里额外指定的配置。</li><li>不能代理UDP流量（如游戏数据包）。</li></ol></li></ul><p>Tun 模式（实现透明代理的一种重要方式）</p><ul><li>说明<ul><li>代理程序会创建一张虚拟网卡，通过<strong>配置操作系统的路由</strong>将网络请求重定向到这张虚拟网卡，代理程序从虚拟网卡中读取并处理这些网络请求。</li></ul></li><li>特点<ol><li>拦截和处理所有流量(TCP/UDP)重定向到本地的代理程序。</li><li>网络请求程序无需额外配置。</li></ol></li></ul><h3 id="Meta-内核">Meta 内核</h3><p>一般指 Clash Meta，也称 <code>Meta</code>、 <code>Mihomo</code> 内核。区别于 <code>Clash Premium</code> 为闭源内核，<code>Mihomo</code> 为开源内核。</p><h3 id="CFW">CFW</h3><p>一般指 Clash For Windows，是一款的基于 <code>Clash Premium</code> 内核的全平台代理软件（虽然叫做 For Windows）。2023 年 11 月 2 日宣布停止更新，并删除发布仓库。</p><h2 id="使用注意事项">使用注意事项</h2><div class="note primary flat"><p>以下讨论基于的 Clash Verge rev 版本为 v2.4.0。</p></div><h3 id="配置文件修改">配置文件修改</h3><p>虽然通过“设置”-“Verge 高级设置”-“配置目录”可以打开一个有很多配置文件的目录，但是这里面的文件是会在订阅更新的时候自动更新的，所以不要在这里面修改配置。</p><p>如果需要修改配置，比较直接的方式是在“订阅”中找到目前在用的订阅，然后右键并点击“编辑文件”，在这个配置文件里进行修改。需要修改的内容比较多的话，也可以重新写一个 <code>.yaml</code> 文件上传。</p><h3 id="Gemini-代理">Gemini 代理</h3><p>尝试过 blackmatrix7 的 <a href="https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/rule/Clash/Gemini/Gemini.yaml">Gemini 代理规则</a>，会遇到 <code>ip A≠B</code> 这种问题。已经有用户提出了 <a href="https://github.com/blackmatrix7/ios_rule_script/issues/1600">issue</a>，但是依照 issue 里的方法依然会遇到这个问题。</p><p>最后没招了，只能把 <code>www.google.com</code> 加到 Gemini 代理组里面。</p><h2 id="配置文件写法">配置文件写法</h2><p>目前参考的是 <a href="https://github.com/Rabbit-Spec/Clash">Rabbit-Spec</a> 的配置文件，除此之外还有一个 <a href="https://gist.github.com/dahaha-365/0b8beb613f8d1ee656fe1f21e1a07959">lazy_script.js</a> 挺全面的，以后可以试试。</p><h3 id="yaml-语法基础">yaml 语法基础</h3><p>首先 yaml 是 json 的超集，所以配置文件中会出现一些 json 语法</p><p>其次 yaml 中有一个语法会在配置文件中经常使用：“锚点”（Anchor <code>&amp;</code>）和“引用”（Alias <code>*</code>），加上经常配合使用的“合并”（Merge <code>&lt;&lt;</code>）。</p><p>举例：</p><ul><li><code>&amp;</code> 锚点 (Anchor) —— 定义变量<figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 就像编程里的：var my_setting = &#123; interval: 300, url: &#x27;...&#x27; &#125;</span></span><br><span class="line"><span class="string">基本设置:</span> <span class="meta">&amp;my_setting</span></span><br><span class="line">  <span class="attr">interval:</span> <span class="number">300</span></span><br><span class="line">  <span class="attr">url:</span> <span class="string">&quot;http://google.com&quot;</span></span><br></pre></td></tr></table></figure></li><li><code>*</code> 引用 (Alias) —— 使用变量<figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 就像编程里的：use(my_setting)</span></span><br><span class="line"><span class="string">下载任务:</span></span><br><span class="line">  <span class="string">*my_setting</span>  <span class="comment"># &lt;--- 这里等同于把上面那两行直接写在这里</span></span><br></pre></td></tr></table></figure></li><li><code>&lt;&lt;</code> 合并 (Merge Key) —— 继承并修改<figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">下载任务2:</span></span><br><span class="line">  <span class="string">&lt;&lt;:</span> <span class="string">*my_setting</span>  <span class="comment"># 先继承上面的 interval: 300 和 url</span></span><br><span class="line">  <span class="attr">interval:</span> <span class="number">600</span>    <span class="comment"># 然后把 interval 改成 600 (覆盖)</span></span><br><span class="line">  <span class="attr">new_item:</span> <span class="literal">true</span>   <span class="comment"># 再加个新属性</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="多订阅链接">多订阅链接</h3><p>在 <code>proxy-providers</code> 底下加上多个 provider，就能把多个机场的节点放到一起来用。参数 <code>proxy: DIRECT</code> 能够让代理软件在更新订阅的时候直连访问订阅链接，这个参数有时候不能落掉，否则会无法正常更新订阅。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">proxies:</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 锚点 - 节点订阅的参数</span></span><br><span class="line"><span class="attr">NodeParam:</span> <span class="string">&amp;NodeParam</span> &#123;<span class="attr">type:</span> <span class="string">http</span>, <span class="attr">interval:</span> <span class="number">86400</span>, <span class="attr">health-check:</span> &#123;<span class="attr">enable:</span> <span class="literal">true</span>, <span class="attr">url:</span> <span class="string">&#x27;http://connectivitycheck.gstatic.com/generate_204&#x27;</span>, <span class="attr">interval:</span> <span class="number">60</span>&#125;, <span class="attr">proxy:</span> <span class="string">DIRECT</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 锚点 - 节点订阅 (在这里填你的机场链接)</span></span><br><span class="line"><span class="attr">proxy-providers:</span> </span><br><span class="line">  <span class="attr">provider1:</span></span><br><span class="line">    <span class="attr">url:</span>  <span class="comment"># 订阅链接</span></span><br><span class="line">    <span class="string">&lt;&lt;:</span> <span class="meta">*NodeParam</span></span><br><span class="line">    <span class="attr">path:</span> <span class="string">&#x27;./proxy_provider/Provider1.yaml&#x27;</span></span><br><span class="line">  <span class="attr">provider2:</span></span><br><span class="line">    <span class="attr">url:</span>  <span class="comment"># 订阅链接2</span></span><br><span class="line">    <span class="string">&lt;&lt;:</span> <span class="meta">*NodeParam</span></span><br><span class="line">    <span class="attr">path:</span> <span class="string">&#x27;./proxy_provider/Provider2.yaml&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="节点筛选">节点筛选</h3><p>如果机场管理员给各个节点提供了比较规范的名字，那么可以使用一些筛选逻辑进行筛选。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 锚点 - 节点筛选组</span></span><br><span class="line"><span class="attr">FilterHK:</span> <span class="string">&amp;FilterHK</span> <span class="string">&#x27;^(?=.*((?i)🇭🇰|香港|(\b(HK|Hong)\b))).*$&#x27;</span></span><br><span class="line"><span class="attr">FilterJP:</span> <span class="string">&amp;FilterJP</span> <span class="string">&#x27;^(?=.*((?i)🇯🇵|日本|川日|东京|大阪|泉日|埼玉|(\b(JP|Japan)\b))).*$&#x27;</span></span><br><span class="line"><span class="comment"># ... 太长了，中间省略一部分</span></span><br><span class="line"><span class="attr">FilterAll:</span> <span class="string">&amp;FilterAll</span> <span class="string">&#x27;^(?=.*(.))(?!.*((?i)群|邀请|返利|循环|官网|客服|网站|网址|获取|订阅|流量|到期|机场|下次|版本|官址|备用|过期|已用|联系|邮箱|工单|贩卖|通知|倒卖|防止|国内|地址|频道|无法|说明|使用|提示|特别|访问|支持|教程|关注|更新|作者|加入|(\b(USE|USED|TOTAL|EXPIRE|EMAIL|Panel|Channel|Author)\b|(\d&#123;4&#125;-\d&#123;2&#125;-\d&#123;2&#125;|\d+G)))).*$&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="策略组">策略组</h3><p>下面的部分决定了软件的“代理”栏最后呈现出来的是怎样的。</p><p>首先最底下的部分就利用了节点筛选规则，把名字为不同国家的节点筛选到不同的策略组里面，例如 <code>&#123;name: 🇭🇰 香港节点, &lt;&lt;: *FallBack, filter: *FilterHK&#125;</code>，这个策略组由于使用了预定义 <code>*FallBack</code>，其中包含 <code>type: fallback</code>，因此会自动选择延时最短的节点，无法手动选择。</p><p>如果希望在软件的“代理”栏中可以手动选择，那应当设置 <code>type: select</code>。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 锚点 - 故障转移参数</span></span><br><span class="line"><span class="attr">FallBack:</span> <span class="string">&amp;FallBack</span> &#123;<span class="attr">type:</span> <span class="string">fallback</span>, <span class="attr">interval:</span> <span class="number">10</span>, <span class="attr">lazy:</span> <span class="literal">true</span>, <span class="attr">url:</span> <span class="string">&#x27;http://connectivitycheck.gstatic.com/generate_204&#x27;</span>, <span class="attr">disable-udp:</span> <span class="literal">false</span>, <span class="attr">timeout:</span> <span class="number">3000</span>, <span class="attr">max-failed-times:</span> <span class="number">3</span>, <span class="attr">hidden:</span> <span class="literal">true</span>, <span class="attr">include-all-providers:</span> <span class="literal">true</span>&#125;</span><br><span class="line"><span class="attr">Manual:</span> <span class="string">&amp;Manual</span> &#123;<span class="attr">type:</span> <span class="string">select</span>, <span class="attr">include-all-providers:</span> <span class="literal">true</span>&#125;</span><br><span class="line"><span class="comment"># 锚点 - 规则参数</span></span><br><span class="line"><span class="attr">RuleSet:</span> <span class="string">&amp;RuleSet</span> &#123;<span class="attr">type:</span> <span class="string">http</span>, <span class="attr">behavior:</span> <span class="string">classical</span>, <span class="attr">interval:</span> <span class="number">86400</span>, <span class="attr">format:</span> <span class="string">yaml</span>, <span class="attr">proxy:</span> <span class="string">Proxy</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 策略组</span></span><br><span class="line"><span class="attr">proxy-groups:</span> </span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🦈</span> <span class="string">JMS</span> <span class="string">专用</span>, <span class="attr">type:</span> <span class="string">select</span>, <span class="attr">include-all:</span> <span class="literal">true</span>, <span class="attr">filter:</span> <span class="string">&#x27;(?i)JMS&#x27;</span>, <span class="attr">icon:</span> <span class="string">&#x27;https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Rocket.png&#x27;</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">Proxy</span>, <span class="attr">type:</span> <span class="string">select</span>, <span class="attr">proxies:</span> [<span class="string">🦈</span> <span class="string">JMS</span> <span class="string">专用</span>, <span class="string">🇭🇰</span> <span class="string">香港节点</span>, <span class="string">🇯🇵</span> <span class="string">日本节点</span>, <span class="string">🇰🇷</span> <span class="string">韩国节点</span>, <span class="string">🇸🇬</span> <span class="string">新加坡节点</span>, <span class="string">🇺🇸</span> <span class="string">美国节点</span>, <span class="string">🇬🇧</span> <span class="string">英国节点</span>, <span class="string">🇫🇷</span> <span class="string">法国节点</span>, <span class="string">🇩🇪</span> <span class="string">德国节点</span>], <span class="attr">icon:</span> <span class="string">&#x27;https://raw.githubusercontent.com/pompurin404/mihomo-party/master/resources/icon.png&#x27;</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">Apple</span>, <span class="attr">type:</span> <span class="string">select</span>, <span class="attr">proxies:</span> [<span class="string">DIRECT</span>, <span class="string">Proxy</span>, <span class="string">🦈</span> <span class="string">JMS</span> <span class="string">专用</span>, <span class="string">🇭🇰</span> <span class="string">香港节点</span>, <span class="string">🇯🇵</span> <span class="string">日本节点</span>, <span class="string">🇰🇷</span> <span class="string">韩国节点</span>, <span class="string">🇸🇬</span> <span class="string">新加坡节点</span>, <span class="string">🇺🇸</span> <span class="string">美国节点</span>, <span class="string">🇬🇧</span> <span class="string">英国节点</span>, <span class="string">🇫🇷</span> <span class="string">法国节点</span>, <span class="string">🇩🇪</span> <span class="string">德国节点</span>], <span class="attr">icon:</span> <span class="string">&#x27;https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Apple_1.png&#x27;</span>&#125;</span><br><span class="line">  <span class="comment"># ... 太长了，中间省略一部分</span></span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">GlobalMedia</span>, <span class="attr">type:</span> <span class="string">select</span>, <span class="attr">proxies:</span> [<span class="string">Proxy</span>, <span class="string">🦈</span> <span class="string">JMS</span> <span class="string">专用</span>, <span class="string">🇭🇰</span> <span class="string">香港节点</span>, <span class="string">🇯🇵</span> <span class="string">日本节点</span>, <span class="string">🇰🇷</span> <span class="string">韩国节点</span>, <span class="string">🇸🇬</span> <span class="string">新加坡节点</span>, <span class="string">🇺🇸</span> <span class="string">美国节点</span>, <span class="string">🇬🇧</span> <span class="string">英国节点</span>, <span class="string">🇫🇷</span> <span class="string">法国节点</span>, <span class="string">🇩🇪</span> <span class="string">德国节点</span>], <span class="attr">icon:</span> <span class="string">&#x27;https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/ForeignMedia.png&#x27;</span>&#125;</span><br><span class="line"></span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🇭🇰</span> <span class="string">香港节点</span>, <span class="string">&lt;&lt;:</span> <span class="string">*FallBack</span>, <span class="attr">filter:</span> <span class="string">*FilterHK</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🇯🇵</span> <span class="string">日本节点</span>, <span class="string">&lt;&lt;:</span> <span class="string">*FallBack</span>, <span class="attr">filter:</span> <span class="string">*FilterJP</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🇰🇷</span> <span class="string">韩国节点</span>, <span class="string">&lt;&lt;:</span> <span class="string">*FallBack</span>, <span class="attr">filter:</span> <span class="string">*FilterKR</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🇸🇬</span> <span class="string">新加坡节点</span>, <span class="string">&lt;&lt;:</span> <span class="string">*FallBack</span>, <span class="attr">filter:</span> <span class="string">*FilterSG</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🇺🇸</span> <span class="string">美国节点</span>, <span class="string">&lt;&lt;:</span> <span class="string">*Manual</span>, <span class="attr">filter:</span> <span class="string">*FilterUS</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🇬🇧</span> <span class="string">英国节点</span>, <span class="string">&lt;&lt;:</span> <span class="string">*FallBack</span>, <span class="attr">filter:</span> <span class="string">*FilterUK</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🇫🇷</span> <span class="string">法国节点</span>, <span class="string">&lt;&lt;:</span> <span class="string">*FallBack</span>, <span class="attr">filter:</span> <span class="string">*FilterFR</span>&#125;</span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="attr">name:</span> <span class="string">🇩🇪</span> <span class="string">德国节点</span>, <span class="string">&lt;&lt;:</span> <span class="string">*FallBack</span>, <span class="attr">filter:</span> <span class="string">*FilterDE</span>&#125;</span><br></pre></td></tr></table></figure><h3 id="分流规则">分流规则</h3><ul><li><code>rule-providers</code> 底下包含的是订阅得到的分流规则，这样可以避免在 <code>rules</code> 底下自己写一大堆东西。</li><li><code>rules</code> 底下是生效的规则。需要注意：<ul><li><code>rule-providers</code> 必须在 <code>rules</code> 里面使用，否则不会生效。<ul><li>例如 <code>RULE-SET,Apple,Apple</code>：<code>RULE-SET</code> 表示使用订阅得到的规则，第一个 <code>Apple</code> 为 <code>rule-providers</code> 底下的名称，第二个 <code>Apple</code> 为 <code>proxy-groups</code> 底下的名称。</li><li>这就完成了一个“节点”→“策略组”→“分流规则”的绑定。</li></ul></li><li>规则生效优先级：最前面的优先生效。<ul><li>例如最前面是 <code>&quot;DOMAIN, www.google.com, Gemini&quot;</code>，后面有一个 <code>RULE-SET,GlobalMedia,GlobalMedia</code>。而 <code>www.google.com</code> 同样被包含到 <code>GlobalMedia</code> 的订阅规则中。</li><li><code>www.google.com</code> 会被绑到 <code>Gemini</code> 策略组里，而非 <code>GlobalMedia</code> 策略组。</li></ul></li></ul></li></ul><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">rule-providers:</span></span><br><span class="line">  <span class="attr">Gemini:</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/5ad04fab096f94224879c77c4bf50ace8a393fb0/rule/Clash/Gemini/Gemini.list</span></span><br><span class="line">    <span class="attr">path:</span> <span class="string">./ruleset/Gemini.list</span></span><br><span class="line">    <span class="attr">behavior:</span> <span class="string">classical</span></span><br><span class="line">    <span class="attr">interval:</span> <span class="number">86400</span></span><br><span class="line">    <span class="attr">format:</span> <span class="string">text</span></span><br><span class="line">    <span class="attr">type:</span> <span class="string">http</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">GlobalMedia:</span></span><br><span class="line">    <span class="string">&lt;&lt;:</span> <span class="meta">*RuleSet</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">&#x27;https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/rule/Clash/GlobalMedia/GlobalMedia_Classical.yaml&#x27;</span></span><br><span class="line">    <span class="attr">path:</span> <span class="string">&#x27;./RuleSet/GlobalMedia.yaml&#x27;</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># ... 太长了，中间省略一部分</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">LAN:</span></span><br><span class="line">    <span class="string">&lt;&lt;:</span> <span class="meta">*RuleSet</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">&#x27;https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/rule/Clash/Lan/Lan.yaml&#x27;</span></span><br><span class="line">    <span class="attr">path:</span> <span class="string">&#x27;./RuleSet/LAN.yaml&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 分流规则指向</span></span><br><span class="line"><span class="attr">rules:</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">&quot;DOMAIN, www.google.com, Gemini&quot;</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">RULE-SET,Gemini,Gemini</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">RULE-SET,Apple,Apple</span></span><br><span class="line"> <span class="comment"># ... 太长了，中间省略一部分</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">GEOIP,CN,DIRECT</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">MATCH,Proxy</span></span><br></pre></td></tr></table></figure><h2 id="服务器使用-mihomo-内核">服务器使用 mihomo 内核</h2><div class="note default flat"><p>在服务器上新增软件、system 服务、修改配置并不是很方便的事情，尤其是当服务器比较多的时候。所以非必要还是不用使用这个方法，直接参考 服务器快捷使用代理 部分，让流量走本机代理。</p></div><p>Clash Verge rev 只是一个 GUI 客户端，其代理功能以及 <code>proxy-providers</code>、<code>proxy-groups</code>、<code>rule-providers</code>、<code>rules</code> 等配置功能都是 mihomo 内核提供的，所以在服务器这种非 GUI 的环境下就可以直接使用 mihomo 内核，且基本可以继承 Clash Verge rev 的配置文件。</p><h3 id="下载与安装">下载与安装</h3><p>比较直接的方式是直接从 GitHub 的 release 页面下载编译好的二进制文件，我这次下载的是 <code>mihomo-linux-amd64-v1-v1.19.21.gz</code>，下载到本地保存为 <code>mihomo.gz</code>，然后执行以下操作：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">gunzip -f mihomo.gz</span><br><span class="line"><span class="built_in">chmod</span> +x mihomo</span><br><span class="line"><span class="built_in">mv</span> mihomo ~/.local/bin/mihomo</span><br><span class="line">~/.local/bin/mihomo -v</span><br></pre></td></tr></table></figure><p>接下来再到 <code>~/.config/mihomo/config.yaml</code> 中加上配置文件。由于配置文件提到了 <code>proxy_provider,ruleset,RuleSet</code> 等路径，所以也需要提前创建：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/.config/mihomo/&#123;proxy_provider,ruleset,RuleSet&#125;</span><br></pre></td></tr></table></figure><p>此时就可以通过 <code>~/.local/bin/mihomo -d ~/.config/mihomo</code> 前台运行软件。</p><h3 id="systemd-服务">systemd 服务</h3><p>希望代理能够在后台稳定运行，所以创建一个用户身份运行的系统服务。实践时发现 <code>/etc/systemd/system/mihomo.service</code> 已经被其他用户创建并且使用了，所以换一个路径：<code>/etc/systemd/system/mihomo-zxz.service</code>，内容为：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Unit]</span></span><br><span class="line"><span class="attr">Description</span>=mihomo for zxz</span><br><span class="line"><span class="attr">After</span>=network-<span class="literal">on</span>line.target</span><br><span class="line"><span class="attr">Wants</span>=network-<span class="literal">on</span>line.target</span><br><span class="line"></span><br><span class="line"><span class="section">[Service]</span></span><br><span class="line"><span class="attr">Type</span>=simple</span><br><span class="line"><span class="attr">User</span>=zxz</span><br><span class="line"><span class="attr">Group</span>=zxz</span><br><span class="line"><span class="attr">ExecStart</span>=/home/zxz/.local/bin/mihomo -d /home/zxz/.config/mihomo</span><br><span class="line"><span class="attr">Restart</span>=<span class="literal">on</span>-failure</span><br><span class="line"><span class="attr">RestartSec</span>=<span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="section">[Install]</span></span><br><span class="line"><span class="attr">WantedBy</span>=multi-user.target</span><br></pre></td></tr></table></figure><p>然后执行下列命令启动服务以及查看状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl daemon-reload</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> --now mihomo-zxz</span><br><span class="line"><span class="built_in">sudo</span> systemctl status mihomo-zxz</span><br><span class="line">journalctl -u mihomo-zxz -f</span><br></pre></td></tr></table></figure><h3 id="配置文件调整">配置文件调整</h3><p>由于和其他用户使用了同一个内核，所以端口就有可能冲突。运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ss -lntup | grep -E <span class="string">&#x27;:(10809|9090|1053)\b&#x27;</span></span><br></pre></td></tr></table></figure><p>结果为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">udp UNCONN 0 0 *:1053 *:* users:((&quot;mihomo&quot;,pid=2427,fd=10)) </span><br><span class="line">tcp LISTEN 0 4096 *:1053 *:* users:((&quot;mihomo&quot;,pid=2427,fd=9)) </span><br><span class="line">tcp LISTEN 0 4096 *:9090 *:* users:((&quot;mihomo&quot;,pid=2427,fd=3))</span><br></pre></td></tr></table></figure><p>所以确实存在端口冲突。这里在配置文件里面修改一下即可。</p><p>然后对上面没有解释的配置内容进行一些补充：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># --- 端口设置  ---</span></span><br><span class="line"><span class="attr">mixed-port:</span> <span class="number">10809</span>        <span class="comment"># 混合端口：HTTP + SOCKS5 </span></span><br><span class="line"></span><br><span class="line"><span class="comment"># --- 基础设置 (保留原有的 allow-lan) ---</span></span><br><span class="line"><span class="attr">allow-lan:</span> <span class="literal">false</span>         <span class="comment"># 允许局域网连接 (手机连电脑代理)</span></span><br><span class="line"><span class="attr">mode:</span> <span class="string">rule</span>              <span class="comment"># 规则模式</span></span><br><span class="line"><span class="attr">log-level:</span> <span class="string">info</span>         <span class="comment"># 日志等级</span></span><br><span class="line"><span class="attr">ipv6:</span> <span class="literal">true</span>              <span class="comment"># 开启 IPv6 支持</span></span><br><span class="line"><span class="attr">udp:</span> <span class="literal">true</span>               <span class="comment"># 开启 UDP (打游戏/QUIC 必须)</span></span><br></pre></td></tr></table></figure><p><code>allow-lan: false</code>:</p><ul><li>意思是：不允许别的设备通过本机器的代理端口接入。只有把它改成 <code>true</code>，其他机器才能通过 Clash 的代理端口上网</li></ul><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># --- 控制器设置 ---</span></span><br><span class="line"><span class="attr">external-controller:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:9091</span> <span class="comment"># 允许所有 IP 控制 (比 :9091 更通用)</span></span><br><span class="line"><span class="attr">secret:</span> <span class="string">xxx</span></span><br><span class="line"><span class="attr">external-controller-cors:</span></span><br><span class="line">  <span class="attr">allow-origins:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&#x27;*&#x27;</span></span><br><span class="line">  <span class="attr">allow-private-network:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure><p><code>external-controller: 127.0.0.1:9091</code></p><ul><li>mihomo 的 REST API 控制端口。面板选节点、更新订阅、看连接、看日志，本质上都是在调这个 API。</li><li>现在绑本机回环地址，意味着只能本机访问，配合 SSH 端口转发来从本地浏览器管理，正合适。</li></ul><p><code>secret</code>:</p><ul><li>API 的访问密钥。</li><li>面板和 curl 调 API 时都要带这个 Bearer token。留空理论上也能工作，但安全性会差很多</li></ul><p><code>external-controller-cors</code></p><ul><li>给浏览器跨域访问 controller 用的。</li><li><code>allow-origins: ['*']</code> 表示允许任意来源</li><li><code>allow-private-network: true</code> 允许访问私网/本机地址时的相关私有网络请求。用在线 dashboard 连本地转发出来的 controller，这个配置通常有帮助。</li></ul><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 缓存设置</span></span><br><span class="line"><span class="attr">profile:</span></span><br><span class="line">  <span class="attr">store-selected:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">store-fake-ip:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure><p><code>profile.store-selected: true</code></p><ul><li>保存 API 对策略组的选择结果，下次启动继续用。比如在面板里把 OpenAI 组切到某个节点，下次重启还能记住。</li></ul><p><code>profile.store-fake-ip: true</code></p><ul><li>保存 fake-ip 映射表。官方说明是：某个域名下次再次连接时，尽量继续使用原来的映射地址。</li></ul><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 流量嗅探 (必须保留，否则无法精准分流)</span></span><br><span class="line"><span class="attr">sniffer:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">force-dns-mapping:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">parse-pure-ip:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">override-destination:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">sniff:</span></span><br><span class="line">    <span class="attr">HTTP:</span></span><br><span class="line">      <span class="attr">ports:</span> [<span class="number">80</span>, <span class="number">8080</span><span class="number">-8880</span>]</span><br><span class="line">      <span class="attr">override-destination:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">TLS:</span></span><br><span class="line">      <span class="attr">ports:</span> [<span class="number">443</span>, <span class="number">8443</span>]</span><br><span class="line">    <span class="attr">QUIC:</span></span><br><span class="line">      <span class="attr">ports:</span> [<span class="number">443</span>, <span class="number">8443</span>]</span><br></pre></td></tr></table></figure><p><code>sniffer.enable: true</code></p><ul><li>开启域名嗅探。作用：当连接本身没有明确给出“这个流量属于哪个域名”，或者 DNS / 目标地址不足以精确分流时，mihomo 会尝试从 HTTP/TLS/QUIC 流量里“闻”出域名，再按域名规则分流。</li></ul><p><code>force-dns-mapping: true</code></p><ul><li>官方说明：对 redir-host 类型识别的流量进行强制嗅探。</li></ul><p><code>parse-pure-ip: true</code></p><ul><li>官方说明：对所有没拿到域名的流量强制嗅探。意思是：哪怕连接目标看起来只是一个 IP，也尽量从握手内容里识别真实域名。</li></ul><p><code>override-destination: true</code></p><ul><li>使用嗅探结果作为实际访问目标。</li></ul><p><code>sniff.HTTP / TLS / QUIC</code></p><ul><li>这几段表示：只对这些协议、这些端口范围做嗅探。HTTP 默认是 80 和 8080-8880，TLS/QUIC 是 443 和 8443。</li></ul><h3 id="使用-dashboard-选择节点">使用 dashboard 选择节点</h3><p>节点选择通常还是有个 GUI 会方便一些。这个时候可以先用 ssh 做端口转发：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -L 9091:127.0.0.1:9091 你的用户名@你的服务器IP</span><br></pre></td></tr></table></figure><p>然后到任意一个 dashboard：</p><ul><li>d.metacubex.one</li><li>yacd.metacubex.one</li><li>board.zash.run.place</li></ul><p>填写前面 <code>external-controller</code> 与 <code>secret</code> 指定的内容，即可看到一个类似 Clash Verge rev 的 GUI 页面。</p><h3 id="订阅内容与规则文件更新">订阅内容与规则文件更新</h3><p>可以写一个脚本然后运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/usr/bin/env bash</span></span><br><span class="line">SECRET=<span class="string">&#x27;你的secret&#x27;</span></span><br><span class="line">API=<span class="string">&#x27;http://127.0.0.1:9091&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> p <span class="keyword">in</span> provider1 provider2; <span class="keyword">do</span></span><br><span class="line">  curl -fsS -X PUT -H <span class="string">&quot;Authorization: Bearer <span class="variable">$SECRET</span>&quot;</span> \</span><br><span class="line">    <span class="string">&quot;<span class="variable">$API</span>/providers/proxies/<span class="variable">$p</span>&quot;</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">&quot;updated proxy provider: <span class="variable">$p</span>&quot;</span></span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> r <span class="keyword">in</span> OpenAI Proxy China LAN GlobalMedia Apple Microsoft Netflix Disney+ YouTube TikTok Spotify Gemini; <span class="keyword">do</span></span><br><span class="line">  curl -fsS -X PUT -H <span class="string">&quot;Authorization: Bearer <span class="variable">$SECRET</span>&quot;</span> \</span><br><span class="line">    <span class="string">&quot;<span class="variable">$API</span>/providers/rules/<span class="variable">$r</span>&quot;</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">&quot;updated rule provider: <span class="variable">$r</span>&quot;</span></span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure><h2 id="服务器快捷使用代理">服务器快捷使用代理</h2><p>即不在服务器上运行代理软件，而是将服务器的流量转发到远程登录的本机上，使用本机的代理。开启该转发只需在 <code>~/.ssh/config</code> 文件中添加一条 <code>RemoteForward</code> 指令即可，例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Host 4090</span><br><span class="line">  HostName &lt;ip&gt;</span><br><span class="line">  User &lt;user_name&gt;</span><br><span class="line">  Port &lt;port_num&gt;</span><br><span class="line">  RemoteForward 127.0.0.1:10809 127.0.0.1:10809</span><br></pre></td></tr></table></figure><p>就是将服务器上的 <code>10809</code> 端口的流量转发到本机的 <code>10809</code> 端口，其中第一个端口是服务器上的端口，第二个端口是本机的端口（在 vscode 里面手动开一个端口转发应该也是相同的效果）。</p><p>然后设置服务器的 <code>.zshrc</code> （或者 <code>.zprofile</code> 之类的其他配置），总之让服务器需要走代理的流量转发到服务器的 <code>10809</code> 端口上，流量接着会转发到本机，并且被本机的代理软件捕获。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/9b558b7c.html</id>
    <link href="https://zhuxz0299.github.io/posts/9b558b7c.html"/>
    <published>2026-01-17T11:11:10.000Z</published>
    <summary>介绍 Clash 配置文件的部分写法以及 Clash Verge rev 软件使用时需要注意的一点问题</summary>
    <title>Clash Verge rev 使用与配置</title>
    <updated>2026-06-29T08:53:51.637Z</updated>
  </entry>
  <entry>
    <author>
      <name>Zhuxz</name>
    </author>
    <category term="System &amp; Hardware" scheme="https://zhuxz0299.github.io/categories/System-Hardware/"/>
    <category term="Network Practice" scheme="https://zhuxz0299.github.io/categories/System-Hardware/Network-Practice/"/>
    <category term="tailscale" scheme="https://zhuxz0299.github.io/tags/tailscale/"/>
    <category term="nat" scheme="https://zhuxz0299.github.io/tags/nat/"/>
    <category term="vpn" scheme="https://zhuxz0299.github.io/tags/vpn/"/>
    <category term="firewall" scheme="https://zhuxz0299.github.io/tags/firewall/"/>
    <category term="stun" scheme="https://zhuxz0299.github.io/tags/stun/"/>
    <content>
      <![CDATA[<h2 id="Tailscale-的工作原理">Tailscale 的工作原理</h2><div class="note info flat"><p>内容来自 Tailscale 官网博客 <a href="https://tailscale.com/blog/how-tailscale-works">How Tailscale works</a></p></div><h3 id="数据平面：WireGuard">数据平面：WireGuard</h3><p>Tailscale 的基础层是 WireGuard 包。WireGuard 能在用户的计算机、虚拟机或容器（WireGuard 称之为“端点(endpoint)”，Tailscale 称之为“节点(node)”）与网络中任何其他节点之间建立一套极其轻量级的加密隧道。</p><p>下面讨论一下数据平面组织的拓扑结构</p><h4 id="星型网络-hub-and-spoke-networks">星型网络 (hub-and-spoke networks)</h4><img src='../../figure/network/Tailscale工作原理与NAT穿透/image.webp' width=500 style="display: block; margin-left: auto; margin-right: auto;"><ul><li>中心节点 (hub node) 通常拥有静态 IP 地址，并在防火墙上开放端口，便于所有节点访问。随后它可以接收来自其他 IP 地址节点的传入连接——即使这些节点本身位于防火墙之后——这种方式与客户端-服务器互联网协议的常规工作原理一致。</li><li>优点：需要连接的节点对比较少，并且由于中心节点有静态 IP、开放防火墙特定端口，因此便于实现节点间通信。</li><li>缺点：这种传统配置难以扩展。首先，远程用户与 VPN 集中器的距离可能较远；若距离过远，连接时将产生高延迟。其次，他们需要访问的数据中心可能也远离 VPN 集中器；若距离遥远，则再次产生高延迟。<img src='../../figure/network/Tailscale工作原理与NAT穿透/image-1.webp' width=500 style="display: block; margin-left: auto; margin-right: auto;"></li></ul><h4 id="网状网络-mesh-networks">网状网络 (mesh networks)</h4><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-2.webp' width=500 style="display: block; margin-left: auto; margin-right: auto;"><ul><li>WireGuard 隧道非常轻量级，可以支持构建这样的网络。</li><li>优点：节点直接之间通信比较优雅。</li><li>缺点：<ul><li>需要配置非常多密钥对：这个问题由下一节的登录认证解决。</li><li>用户设备通常没有静态 IP 地址，可能随时移动（IP 地址变化），并且防火墙不是随时都方便打开的：这个问题由 NAT 穿透与 DERP 解决。</li></ul></li></ul><h3 id="控制平面：密钥交换与协调">控制平面：密钥交换与协调</h3><p>简而言之，有一个协调服务器（就是登录 Tailscale 的网站 <a href="http://login.tailscale.com">login.tailscale.com</a>）充当用于公钥交换的共享投递箱：</p><ol><li>每个节点会为自己生成一对随机的公钥/私钥，并将公钥与其身份标识相关联（详见下文登录部分）。</li><li>节点联系协调服务器，留下其公钥以及关于该节点当前可被找到的位置及其所属域的记录。</li><li>节点下载其域内的公钥和地址列表，这些信息由其他节点留在协调服务器上。</li><li>节点使用适当的公钥集配置其 WireGuard 实例。</li></ol><h4 id="登录与双重认证（2FA）">登录与双重认证（2FA）</h4><p>公钥的管理与 ssh 类似：用户无需对自己的 ssh 公钥保密，但选择将哪些公钥放入 <code>authorized_keys</code> 文件中很重要。因此协调服务器将哪些公钥发送给哪些节点这件事情需要身份认证，Tailscale 将身份验证的工作外包给 OAuth2、OIDC（OpenID Connect）或 SAML 提供商。身份提供商维护着用户账户下的用户列表、密码、双因素认证（2FA）设置等信息。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-3.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></p><h4 id="NAT-穿透">NAT 穿透</h4><div class="note default flat"><p>这里仅进行简单解释，详细信息在后面的 NAT 穿透的工作原理部分。</p></div><p>由于通常用户没有静态 IP 地址（就是在 NAT）后面，并且不太可能为 WireGuard 流量开放防火墙端口。为此，Tailscale 采用了基于互联网 STUN 和 ICE 标准的多种先进技术，使得用户在 NAT 阻隔的情况下也能实现 p2p 连接，并且避免了防火墙额外配置开放端口的需求。</p><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-4.webp' width=450 style="display: block; margin-left: auto; margin-right: auto;"><h4 id="加密的-TCP-中继（DERP）">加密的 TCP 中继（DERP）</h4><p>某些特别严苛的网络会完全封锁 UDP 协议，以至于单纯依靠 STUN 和 ICE 技术无法实现穿透。为此，Tailscale 部署了名为 DERP（数据包专用加密中继）的服务器网络，简而言之就是穿透失败的话就让流量走中转服务器。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-5.webp' width=500 style="display: block; margin-left: auto; margin-right: auto;"></p><h2 id="NAT-穿透的工作原理">NAT 穿透的工作原理</h2><div class="note info flat"><p>内容来自 Tailscale 官网博客 <a href="https://tailscale.com/blog/how-nat-traversal-works">How NAT traversal works</a></p></div><p>实现 NAT 穿透有两个基本条件：</p><ul><li>协议应基于 UDP（利用了 UDP 不需要握手的性质，从而让 NAT 背后的两台设备有同时开放 UDP 端口的机会）</li><li>用户需要直接控制用于发送和接收网络数据包的网络套接字（和后面 STUN 协议相关）<ul><li>套接字就是“IP地址 + 端口号 + 协议（TCP/UDP）”的组合，如果向 STUN 服务器发送请求用的套接字和后续向另一个节点通信用的套接字不同，那么在公网上就不能得到一个稳定的 <code>ip:port</code>。</li></ul></li></ul><p>然后需要解决两大障碍：有状态防火墙和 NAT 设备。</p><p>用户设备的“有状态防火墙”其实通常包含两层：</p><ul><li>NAT 设备（通常是路由器）本身具备流量过滤功能</li><li>电脑上安装的 Windows Defender 防火墙、Ubuntu 的 ufw（使用 iptables/nftables）、BSD 的 pf（macOS 也采用）以及 AWS 的安全组之类的软件。</li></ul><h3 id="状态防火墙的工作原理">状态防火墙的工作原理</h3><div class="note default flat"><p>这一部分暂时先不考虑 NAT，先直接认为两个节点在公网上都有一个 <code>ip:port</code> 对。</p></div><p>电脑上的防火墙都具有高度可配置性，但最常见的配置是允许所有&quot;出站&quot;连接并阻止所有&quot;入站&quot;连接。状态防火墙能够记住之前见过的数据包，并利用这些信息来决定如何处理新到达的数据包。</p><p>对于 UDP，规则为：防火墙在遇到一个入站的 UDP 数据包时，会检查这个数据包是否能对应上之前的某个出站数据包，如果能对应上，则允许该入站数据包进入。例如，如果笔记本从 <code>2.2.2.2:1234</code> 端口发送了一个 UDP 数据包到 <code>5.5.5.5:5678</code> 端口，这个通信过程会被记录；后续如果遇到来自 <code>5.5.5.5:5678</code> 并且发向 <code>2.2.2.2:1234</code> 的数据包，则允许传入。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-6.webp' width=500 style="display: block; margin-left: auto; margin-right: auto;"></p><h4 id="防火墙启动">防火墙启动</h4><div class="note default flat"><p>标题的原文叫做“Firewall face-off”，其中 face-off 在这里可以理解成冰球比赛的开球。这个标题想表达的意思是：两个设备分别在自己的防火墙后面时，如何开启 UDP 通信？</p></div><p>利用前面讲到的防火墙对 UDP 的规则，已经可以实现一个 hub-and-spoke 的网络：作为 hub 的服务器有公网 IP，并且在防火墙上开放了对应端口，因此其他的 Client 能否方便地和 hub 建立连接，从而通过这个中转实现通信。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-7.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></p><p>但是问题在于 Tailscale 希望构建任意两个 Client 都能 p2p 互联的网络，但是这个时候就遇到了问题：两个设备都不知道对方的 IP 以及在防火墙上对应的端口，因此不知道应该如何启动这个 UDP 通信过程。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-8.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></p><h4 id="巧妙应对棘手的防火墙">巧妙应对棘手的防火墙</h4><p>为了让对等节点提前知晓对方使用的 <code>ip:port</code>，Tailscale 可以继续利用协调服务器：协调服务器可以用来给各个设备之间交换密钥，那么也可以用来交换 <code>ip:port</code> 信息。</p><p>接下来需要构架两个节点之间的 UDP 连接：对等节点开始互相发送 UDP 数据包，并且可以预料到部分数据包会丢失，不过这些数据包也不用携带重要信息。</p><p>假设两个节点分别为笔记本电脑和工作站，具体的构建流程为：</p><ol><li>笔记本电脑发出的首个数据包，从 <code>2.2.2.2:1234</code> 发往 <code>7.7.7.7:5678</code>，经过 Windows Defender 防火墙并传输至互联网。另一端的公司防火墙因无 <code>7.7.7.7:5678</code> 与 <code>2.2.2.2:1234</code> 通信记录而拦截该数据包。但此刻 Windows Defender 已记住：需预期并允许来自 <code>7.7.7.7:5678</code> 发往 <code>2.2.2.2:1234</code> 的响应数据包。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-9.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></li><li>接下来，工作站的第一个数据包从 <code>7.7.7.7:5678</code> 发送至 <code>2.2.2.2:1234</code> 通过企业防火墙并穿越互联网。当它到达笔记本电脑时，Windows Defender 认为“啊，这是对我之前看到的那个出站请求的响应”，于是放行该数据包。此外，企业防火墙现在会记住，它应该期待从 <code>2.2.2.2:1234</code> 到 <code>7.7.7.7:5678</code> 的响应，并且这些数据包也是允许通过的。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-10.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></li><li>收到来自工作站的包后，笔记本电脑受到激励，发送另一个包返回。它穿过 Windows Defender 防火墙，经过企业防火墙（因为这是对先前发送包的“响应”），最终抵达工作站。双向连接成功建立。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-11.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></li></ol><h4 id="创建连接注意事项">创建连接注意事项</h4><p>上述连接想要建立，需要两个端点必须大致在同一时间尝试通信，以便在双方仍在线时所有中间防火墙都能开放。一种方法是让对等方持续重试，但这很浪费资源，因此希望双方都知道在同一时间开始建立连接。</p><p>在 Tailscale 中，协调服务器与 DERP（Detour Encrypted Routing Protocol，迂回加密路由协议）服务器共同构成旁路信道系统，用于帮助两个节点同时尝试通信。</p><p>除此之外，状态防火墙的内存有限，如果一段时间内未检测到数据包（UDP 的常见超时值为 30 秒），防火墙会忘记该会话。这意味着节点之间需要定期通信以保持连接活跃，比如使用计时器定期发送数据包，或者通过某种带外方式按需重新启动连接。</p><h3 id="NAT-的本质">NAT 的本质</h3><p>可以将 NAT（Network Address Translator，网络地址转换）设备视为带有额外烦人功能的状态防火墙：除了所有状态防火墙的功能外，它们还会在数据包通过时修改其内容。</p><h4 id="穿越-NAT-网络">穿越 NAT 网络</h4><p>NAT 被用来解决私有 IP 无法在互联网访问数据的问题：比如用户的笔记本希望从 <code>192.168.0.20:1234</code> 发送数据到 <code>7.7.7.7:5678</code>，由于前者是私有 IP，因此提供 NAT 的机器会从其公网 IP 地址中选取一个未使用的 UDP 端口（以 <code>2.2.2.2:4242</code> 为例），并创建 NAT 映射来建立对应关系：局域网侧的 <code>192.168.0.20:1234</code> 等同于互联网侧的 <code>2.2.2.2:4242</code>。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-12.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></p><h4 id="STUN-协议研究">STUN 协议研究</h4><p>如果两个对等节点都在 NAT 背后，那么面临的问题比仅仅有状态防火墙更麻烦：在对方发送数据包之前，根本不存在 <code>ip:port</code> 映射，因为 NAT 映射仅在向互联网发送出站流量时才会创建。这就导致双方都需要先发送数据，但彼此都不知道该向谁发送，且必须等到对方先发送数据才能确定目标。</p><p>这个时候可以利用 STUN 协议来帮助解决这个问题。STUN 协议的工作是：用户设备向 STUN 服务器发送一个“从你的视角看，我的终端地址是什么？”的请求，服务器则回复“这是我看到你的 UDP 数据包来源的 <code>ip:port</code>”。这样就获得了双方设备在公网上的 <code>ip:port</code> 对。<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-13.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></p><h4 id="不同的-NAT-设备">不同的 NAT 设备</h4><p>前面在借助 STUN 协议的时候其实用到了一个假设：当 STUN 服务器告诉节点其 <code>ip:port</code> 为 <code>2.2.2.2:4242</code> 时，整个互联网都会将节点视为 <code>2.2.2.2:4242</code>。从这一角度来看，任何人都可以通过 <code>2.2.2.2:4242</code> 访问 NAT 后的节点。</p><p>但是某些 NAT 比较复杂，当通信目标不同时，它们会创建不同的 NAT 映射，导致前面的假设失效。如下图所示<br><img src='../../figure/network/Tailscale工作原理与NAT穿透/image-14.webp' width=600 style="display: block; margin-left: auto; margin-right: auto;"></p><p>不同的 NAT 可以依据防火墙和 NAT 映射策略分成如下几类：</p><table><thead><tr><th style="text-align:left"></th><th style="text-align:left">Endpoint-Independent NAT mapping</th><th style="text-align:left">Endpoint-Dependent NAT mapping (all types)</th></tr></thead><tbody><tr><td style="text-align:left"><strong>Endpoint-Independent firewall</strong></td><td style="text-align:left">全锥型 NAT <br>(Full Cone NAT)</td><td style="text-align:left">N/A*</td></tr><tr><td style="text-align:left"><strong>Endpoint-Dependent firewall</strong><br>(dest. IP only)</td><td style="text-align:left">受限锥型 NAT <br>(Restricted Cone NAT)</td><td style="text-align:left">N/A*</td></tr><tr><td style="text-align:left"><strong>Endpoint-Dependent firewall</strong><br>(dest. IP+port)</td><td style="text-align:left">端口受限圆锥型 NAT <br>(Port-Restricted Cone NAT)</td><td style="text-align:left">对称型 NAT <br>(Symmetric NAT)</td></tr></tbody></table><p>* <em>理论上可能存在，但在实际中不会出现</em></p><p>不过对于 NAT 穿透的难度来说，实际上只用关心 NAT 的映射策略。因此只有对称性 NAT 会对 NAT 穿透产生巨大的难度。</p>]]>
    </content>
    <id>https://zhuxz0299.github.io/posts/ae6ce9cc.html</id>
    <link href="https://zhuxz0299.github.io/posts/ae6ce9cc.html"/>
    <published>2026-01-10T08:42:06.000Z</published>
    <summary>详解 Tailscale 原理：基于 WireGuard 的网状网络架构、密钥交换控制平面，以及利用 STUN 和 DERP 实现 NAT 穿透的核心机制。</summary>
    <title>Tailscale工作原理与NAT穿透</title>
    <updated>2026-04-27T13:48:22.014Z</updated>
  </entry>
</feed>
